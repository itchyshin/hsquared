# check-log — 2026-09-02 h2 A20 experimental 0.5.0 version bump

**Arc:** A20 — hsquared `0.1.0.9000` → `0.5.0` (experimental number only)  
**Lane:** `cursor/a20-experimental-050` @ `~/local-scratch/lanes/hsquared-a20-050-20260902`  
**Base:** `origin/main` `44260a73` (includes #141 `efe4c476` + pkgdown #142)  
**Fence:** draft PR only. No merge. No CRAN submit. No covered flip. `public_covered_count` stays **5**.

## Changes

- `DESCRIPTION` — `Version: 0.5.0`. Experimental Description sentence kept.
- `NEWS.md` — `# hsquared 0.5.0` heading + experimental / count-5 / not-CRAN-yet preamble. Development-version bullets kept under that heading.
- `inst/CITATION` — fallback version string `0.1.0.9000` → `0.5.0`. Twin DOI still pending. No invented DOI.
- D-41 channels left on: README lifecycle badge + warning, `_pkgdown.yml` home callout, `lifecycle` on exports, `.onAttach`, DESCRIPTION experimental line.

## Commands

```sh
cd ~/local-scratch/lanes/hsquared-a20-050-20260902
Rscript -e 'devtools::load_all(".", quiet = TRUE); cat(as.character(utils::packageVersion("hsquared")), "\n"); testthat::test_file("tests/testthat/test-d41-experimental-honesty.R", reporter = "summary")'
Rscript -e 'meta <- as.list(read.dcf("DESCRIPTION")[1,]); print(utils::readCitationFile("inst/CITATION", meta = meta))'
Rscript -e 'devtools::test(reporter = "summary")'
```

## Results

| Check | Outcome |
|-------|---------|
| `packageVersion("hsquared")` after `load_all` | **0.5.0** |
| `.onAttach` | still says experimental; first CRAN target 0.5.0, not 1.0 |
| `testthat::test_file(...test-d41-experimental-honesty.R)` | **PASS** (all dots; DONE) |
| `readCitationFile(inst/CITATION)` | **OK** — `R package version 0.5.0`; twin DOI pending; no invented DOI |
| `devtools::test(reporter = "summary")` | **DONE**, 0 failures, 0 warnings; **73** expected live-Julia / missing-Suggests skips |
| `public_covered_count` in capability-status / claims register | stays **5**; multivariate stays `partial` |

No `air format` (no `R/` edits). No `devtools::document()` (no roxygen change). No CRAN submit. No win-builder.

## Remaining

- Draft PR only. Merge after **A19 GO** (Julia 0.5.0 in General, or owner-willing General PR) then **A20 GO**.
- Also **A13 SIGNED** (Darwin ink on the scratch sheet).
- `cran-comments.md` SHA / win-builder stay TBD. Do not submit.
