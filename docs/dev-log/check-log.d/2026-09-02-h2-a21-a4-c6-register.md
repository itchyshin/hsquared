# check-log — 2026-09-02 h2 A29 A4 + A21 C6 leftover (claims register)

**Arc:** leftover Rose wording on `docs/design/06-public-claims-register.md`.
**Lane:** R — `claude/lane-h2-twin-20260901` @ `~/local-scratch/lanes/hsquared-h2-twin-20260901`
**Panel:** A29 A4 (`h2-a29-rose-preflip-2026-09-02.md` §7); A21 C6 leftover (`h2-a21-c6-ram-identity.md` next action)
**Lenses:** Rose / Falconer (perspectives; no spawned subagents)
**Lease:** `docs/design/06-public-claims-register.md` + this test/evidence pair.

**No covered flip. No `validation_status()` row added. `public_covered_count` stays 5.**
Live `validation_status()` after the change: **21 rows / 4 covered**. C5/C6/C9 files were not edited.

## What was wrong

1. **A4.** The multivariate allowed-wording cell said "Covered numeric claim scoped
   to k=2 unstructured" with no conditional. Live
   `validation_status()` / `formula_status()` already say
   "Covered numeric claim **(when flipped)** is scoped…". Unconditioned, the
   register reads as if a covered numeric claim exists today. The row is still
   `partial`.
2. **C6 leftover.** C6 named the two `m2` denominators on
   `capability-status.md` and skipped the register (A4 treated it as busy).
   The register still omitted the cross-route trap: two-effect
   `maternal_proportion()` uses `σ²_a + σ²_m + σ²_e` (no covariance);
   covered `m2_maternal` uses `σ_P` including `σ_dm`.

## What changed

| File | Change |
|---|---|
| `docs/design/06-public-claims-register.md` | A4 conditional + the two C6 `m2` sentences, copied from the capability-status fences. Status words and `public_covered_count` untouched. |
| `tests/testthat/test-claims-register-a4-c6.R` | new source-tree-only guard (docs/ is `.Rbuildignore`d). Did **not** re-edit `test-direct-maternal.R`. |

## Commands and results

```
devtools::test(filter = "claims-register", reporter = "check")
  FAIL 0 | WARN 0 | SKIP 0 | PASS 6

devtools::test(filter = "^random-regression$", reporter = "check")
  FAIL 0 | WARN 0 | SKIP 1 | PASS 84
  (pre-existing live-Julia skip)

pkgload::load_all(); validation_status()
  rows=21 covered=4
```

## Tests of the tests

Against a **temp copy** of the register (repo file not mutated): stripping
`(when flipped)` and `(no covariance)` makes both pins fail; the original copy
still passes. Repo file re-read after: zero `MUTATED` leftover.

## Claim boundary

- No estimand, status word, or `public_covered_count` moved.
- No `validation_status()` row added (G-AG-5).
- No edit to `04-validation-canon.md` (C5), `test-direct-maternal.R` (C6),
  or the C9 after-task / coordination board.
- No Dropbox / v07 edit. No merge.
