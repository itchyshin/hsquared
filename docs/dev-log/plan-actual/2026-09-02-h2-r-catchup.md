# Plan-vs-actual — R catch-up R1–R4 (2026-09-02)

**Melissa.** Source plan: `~/local-scratch/h2-r-catchup-finish-2026-09-02.md`.
Full verify: `~/local-scratch/h2-r-catchup-dod-verify-2026-09-02.md`.

**Actual:** tip `ef54db4` on `claude/lane-h2-twin-20260901` / draft #141.
R1 `98a1c87` · R2 `86f7e36` · R3 `ef54db4` · R4 `85c4884` all ancestors.
Attach `71852aa`, `formula_status` `d54e4d8`, `hs_control` `385a1e3`,
pkgdown `c609d86` attached.

**DoD:** MET. **Drift: none. Unclear: none.**

Material deviations (all **adaptive**):

- R3 reject hooked from validate/spec (`conditions.R`) because R1 leased `R/hsquared.R`. Same public rule.
- R5/R6 not run (optional; Ada STOP after R1–R4).
- Tip CI still in progress (plan allowed).
- Eight unstaged leftover `man/*.Rd` ASCII regen (R0 not folded).
- Getting started still says ML is rejected “on the fit path” after R3 also rejects validate.

**Safety / claims / routing:** fences standing; count 5; no covered flip; no merge; `push = local-only (owner)`.

**Recommendation:** STOP. Hand Rose nothing as a defect.
