# Retry-5 terminal handoff and Retry-6 entry gate

> 🎯 **GOAL — COPY/PASTE INTO A FRESH CODEX SESSION**
>
> Codex alone will deliver a prospectively admissible Retry-6 D0F entry or a
> seed-free retired proposal. **HEADLINE:** prove the runtime-tree and typed-
> mutation contract before any new phenotype. **IN PARALLEL:** read-only R and
> Julia contract checks, then Fisher/Noether review batch A and Hopper/Grace/Rose
> batch B, with the two batches durably separated. **DEFER:** the quarantined
> downstream scaffold, D1/D2, activation, capability/count changes, merge,
> release, and G10. **DISCIPLINE:** exact typed-cause mutations, exact-head local
> checks and CI, clean hash-matched deployment, retired-packet preflight,
> historical seed-lock before preseal-last, create-once bootstrap, deployed-
> Julia zero-seed preflight, independent chronology audit, Totoro at no more
> than 96 single-threaded workers or scheduled DRAC, and a Rose-validated
> after-task/handoff closure; never GitHub Actions for simulation evidence.

Date: 2026-07-15 MDT
Branch in both twins: `codex/2026-07-13-v07-performance-localization`

## Mission

Retry 5 is closed permanently as an immutable negative infrastructure endpoint.
Continue only the separately preregistered Retry-6 repair. Do not generate a
fresh phenotype until the repaired contract, typed mutation controls, exact
reviews in two durably recorded batches, clean hash-matched deployment, fixed
retired-packet preflight, historical seed-lock, preseal written last,
create-once bootstrap, deployed-Julia zero-seed preflight, and independent
chronology audit are all green in that order.

## Read first

1. `AGENTS.md` and the current phase snapshot.
2. `docs/dev-log/recovery-checkpoints/2026-07-15-v07-d0f-retry5-post-preseal-tree-blocker.md`.
3. R twin `docs/design/49-v07-genomic-recovery-v3-sample-size-ladder.md`,
   especially the Retry-6 amendment.
4. This handoff's Landing State ledger before touching any dirty file.

## Landed state

| Repo | Last implementation/status head before this emergency handover | Pull request | State |
| --- | --- | --- | --- |
| `HSquared.jl` | `d3ed51f8bd602ae380d5f37710b7401fabdd5370` | draft PR #274 | Retry-5 closure landed; initial Retry-6 Julia repair was already pushed at ancestor `d1914951` |
| `hsquared` | `8dea0ad9fb9b56ea4457bf9d1f25c7fa64af1570` | draft PR #137 | Retry-5 closure landed at ancestor `3ac5646`; subsequent Retry-6 R admission hardening is now pushed through this head |

No PR was merged. No activation, capability promotion, count change, release,
G10, D1, or D2 occurred. `public_covered_count` remains 5 and only the supplied-
`Ginv` estimator remains covered.

## Emergency coordination consolidation

Two Codex tasks accidentally shared these worktrees during the Retry-5 closure
and Retry-6 entry work. Their changes are adjacent and compatible, but the
concurrency violated the one-platform/one-writer rule:

- archived task `H2-2` (`019f659e-87f7-77d1-abff-038c411d0f41`) owned the
  Retry-5 terminal audit, status synchronization, after-task evidence, and this
  handoff draft. Its audited closure commits are R `3ac5646` and Julia
  `d3ed51f8`;
- task `019f6584-1113-7501-b7fd-35eaae29ecb9` owned the prospective Retry-6 R
  phase-admission hardening, tests, clean deployment, and retired-packet
  diagnostic preflight. Its landed R implementation head is `8dea0ad`; the
  compatible Julia repair is ancestor `d1914951` of `d3ed51f8`.

The collision was detected before any Retry-6 preseal, bootstrap
materialization, phenotype, evidence root, or review receipt was created.
`H2-2` was sent a coordination stop and archived. No task is authorized to
resume the arc merely from this handoff: first assign one Codex task sole
ownership and re-read the dirty-file ledger. The quarantined scaffold remains
outside both slices.

No committed work needs to be discarded. The Retry-5 documentation closure and
Retry-6 implementation are logically separable. The uncommitted after-task,
check-log, and handoff refinements are useful consolidation material, not
admission evidence; land or revise them only under the next sole owner.

## Retry-6 gate state at the coordination stop

| Gate/evidence | State | Exact evidence and consequence |
| --- | --- | --- |
| R phase-aware runtime contract | **GREEN, landed** | `8dea0ad9fb9b56ea4457bf9d1f25c7fa64af1570`; official fitting admits only `attempts/` and `packets/`, later callers bind cumulative phase allowlists |
| Production two-admission regression | **GREEN** | real `v3d_validate_bound_stage()` passes pristine admission and a second admission after one complete synthetic attempt/packet publication; premature base-R, Julia, summary, review, and adjudication members remain red |
| Recovery-v3 R checks | **GREEN** | all seven recovery-v3 test files passed; driver, preseal, and recomputation selftests passed; refreshed driver/recomputer sidecars verified |
| Julia typed replay contract | **GREEN, landed** | repair `d1914951` is an ancestor of exact Julia head `d3ed51f8`; replay selftest and sidecar verification passed |
| Exact-head CI | **GREEN** | R PR #137 run `29417502924`; Julia PR #274 run `29417277571`; docs run `29417277473`; live-draw job skipped as designed |
| Clean Totoro deployment | **TECHNICALLY GREEN, NON-ADMITTING** | `/home/snakagaw/hsq_work/v07-genomic-recovery-v3-code-8dea0ad-d3ed51f8`; clean exact checkouts, sidecars, and synthetic selftests. It occurred before the required exact-review batches, so repeat/renew it after those reviews rather than treating chronology as satisfied |
| Retired Retry-4 16-packet diagnostic | **TECHNICALLY GREEN, NON-ADMITTING** | external directory `/home/snakagaw/hsq_work/v07-genomic-recovery-v3-admission-8dea0ad-d3ed51f8`; log SHA-256 `c70fa24cb9f622b65599f0d8bb5c83958d99aecc55ab7914ca654c27660b1713`; source maximum `7.3896444519050419e-13`, component maximum `1.1102230246251565e-16`, no RNG. It also preceded the required reviews and must not be used to skip the ordered gate |
| Exact Retry-6 review batches | **NOT COMPLETE** | the earlier review bound R `efda17e`, not `8dea0ad`; the renewed batch was interrupted at the coordination stop. No Retry-6 review receipt may be minted from it |
| Preseal/bootstrap/phenotype | **ABSENT** | no Retry-6 evidence root or preseal exists; no `2041` bootstrap seed or `2040` phenotype seed was consumed |

The next sole owner resumes at exact Retry-6 reviews, not at preseal or compute.
Because the deployment and diagnostic preflight ran early, preserve their logs
as useful diagnostics but repeat the ordered post-review gates.

## Retry-5 terminal truth

- Root:
  `/home/snakagaw/hsq_work/v07-genomic-recovery-v3-d0f-retry-r5-fcfde69-06941997`.
- Classification: `UNADJUDICATED — POST-PRESEAL TREE-VALIDATION BLOCKER
  (ADMISSION CONTRACT NOT PROVEN)`.
- One phenotype/packet/attempt exists for seed `2038101001`; the official fit
  was successful, converged, and interior. Runtime tree validation stopped
  before a second seed.
- No corpus lock, base-R summary, Julia replay/summary, adjudication receipt,
  D1, or D2 exists.
- The complete phenotype/bootstrap bases `2038000000` / `2039000000` and the
  root are retired. Never reuse an unused seed, repair a member, or adjudicate a
  subset.
- Final recheck: sorted tree digest
  `f97d1c15600307238eef794c80bfc3644715421ee93f0812527f951727cc1b02`,
  zero writable files/directories, and no process under three self-excluding
  probes.
- Post-run chronology audit: identity pins, ratio contract, exact-head CI,
  clean deploy, five receipts, and preseal passed. Typed failure gate was RED;
  mutation execution was RED/UNKNOWN; durable fixed-preflight and two-batch
  proof were UNKNOWN. Later Retry-6 repairs cannot cure this chronology.

## Landing State ledger

The handoff gate was run after the closure commits were pushed. It correctly
reported the following carried-over state.

| Repo/path | State | Why not landed | Resume |
| --- | --- | --- | --- |
| `HSquared.jl/sim/phase2_v07_genomic_recovery_v3_downstream_replay.jl` | **CARRIED-OVER — quarantined untracked scaffold**, SHA-256 `30838979b9f3aad7d3442204fb4a4a30345f24950000d7ecb23a20d63cad6155` | Predates this closure, is incomplete, and is not part of Retry-5 or the Retry-6 admission repair | Inspect separately; never stage from `git add -A` |

At the coordination stop, the R Retry-6 implementation is clean and pushed at
`8dea0ad`, but both twins retain uncommitted H2-2 handoff/after-task/check-log
refinements. Julia additionally retains the quarantined scaffold. Preserve all
of them until one task is assigned ownership; do not infer a clean worktree
from the pushed implementation heads.

The gate also reports old unpushed commits on unrelated local branches. They
predate this arc, were not inspected or changed here, and are out of scope; do
not land or delete them without a separate ownership audit.

## Retry-6 pre-RNG ultra-plan

Every phase is fail-closed. A RED or UNKNOWN verdict stops the arc before RNG.

### Operating shape

| Slice | Member/lens | Codex tier + effort | Estimate | Output / dependency |
| --- | --- | --- | --- | --- |
| S0 orient + ownership | Ada + Shannon | Sol, high | 10–15 min | exact live-state ledger; none |
| S1 repair reconciliation | Hopper + Boole + Emmy | Terra, high | 30–45 min | reviewed R/Julia contract diff; S0 |
| S2 typed mutations + concurrency | Noether + Hopper | Terra builders, Sol verifier; high | 45–75 min | genuine typed-cause and two-worker proof; S1 |
| S3 local checks + exact-head CI | Grace | Terra, high | 30–60 min plus CI | pushed exact heads and green run IDs; S2 |
| S4 review batch A | Fisher + Noether | Sol, high | 20–30 min | two exact eight-column receipts plus external batch-A ledger; S3 |
| S5 review batch B | Hopper + Grace + Rose | Sol, high | 30–45 min | three exact receipts plus later external batch-B ledger; S4 |
| S6 deploy + retired preflight | Grace + Hopper | Terra, high | 30–45 min | clean deploy receipt and 16-packet receipt; S5 |
| S7 lock + preseal + zero-seed gates | Ada + Grace + Rose | Sol, ultra | 30–60 min | seed lock, preseal, bootstrap, deployed-Julia preflight, chronology verdict; S6 |
| S8 admitted smoke/compute | Curie + Fisher | Terra, high | variable | first phenotype only after S7 PASS |
| S9 verify + consolidate | Rose | Sol, high | 20–30 min | after-task, check-log, handoff, landing ledger; S8 or seed-free stop |

Fan-out: five specialist review agents in two sequential batches; independent
members within each batch run in parallel. One writer owns each twin at a time.
Search: repo/brain prior-work sweep only; no external literature search is
needed for this infrastructure contract. Estimate: about 4–7 hours pre-RNG
including CI, usually one long session but hand off if CI or deployment waits.
Plan review: Hilbert (dependency/executability) plus Volta/Rose (claim and
contract boundary) before execution.

1. **Rehydrate and freeze ownership.** Re-read this handoff, doc 49, both
   statuses, exact heads, dirty hashes, PR state, and Totoro process/root state.
   Preserve Retry 4, Retry 5, and the untracked scaffold unchanged.
2. **Reconcile the repair diff.** Review Julia `d1914951` and R `efda17e` plus
   `8dea0ad` term by term. The runtime projection may accept only
   authenticated immutable inputs plus allowed generated members; pristine
   creation validation remains strict.
3. **Prove typed failure semantics.** Every synthetic disagreement and
   infrastructure mutation must raise its declared typed error for its intended
   cause. An arbitrary exception is a failure. Run the standalone Julia replay
   selftest in the exact test path used for admission.
4. **Exercise concurrency and immutability.** Add/run the two-worker regression;
   prove legitimate first output cannot poison seed 2; prove foreign files,
   extra boundary rows, altered inputs, and partial failure cannot mutate an
   admitted root.
5. **Run local exact checks.** Targeted R tests, Julia replay selftest, package
   tests proportionate to the changed contract, sidecar verification,
   `git diff --check`, and preamble cap. Commit and push the repaired exact heads.
6. **Exact-head CI gate.** Wait for and record green package-check/docs CI at
   the newly pushed R and Julia heads. CI may run checks and docs only; never
   simulation evidence. A cancelled, skipped, stale-head, or merely queued run
   is not green.
7. **Exact reviews, batch A.** Fisher + Noether over the exact doc and heads.
   Keep the implemented eight-column reviewer-receipt schema unchanged.
   Separately persist a content-addressed external batch-A ledger with time,
   batch, exact identities, receipt hashes, and reviewed commands. It is a gate
   record outside the pristine evidence root, not a preseal member. Any UNKNOWN
   is RED.
8. **Exact reviews, batch B.** Hopper + Grace + Rose only after batch A is
   durably closed. Keep the same receipt schema and write a later external
   batch-B ledger with the same audit fields. Do not materialize both batches
   together.
9. **Clean deploy.** Deploy clean clones of the reviewed heads; verify HEAD,
   clean status, tools, sidecars, Julia environment, and no foreign process.
10. **Durable retired-packet preflight.** Run the frozen read-only 16-packet boundary
   preflight against retired packets. Persist stdout/result receipt, complete
   boundary inventory equality, mutation outcomes, and before/after source
   hashes. No root mutation.
11. **Historical seed-lock, then preseal last.** Write and verify
    `historical_seed_lock.tsv` and its checksum sidecar before the preseal.
    Only after steps 1–10 are green, mint the schema-3 preseal as the last
    staging write. Bind exactly the fields supported by the frozen 42-key
    schema, including doc/head identities, historical seed-lock hash,
    deployment/root identities, disjoint bases `2040000000` / `2041000000`,
    and the required absence of a bootstrap manifest. The exact-head CI,
    review-batch, and retired-packet records remain separately hash-verified
    external admission gates; do not claim the preseal encodes fields it lacks.
    Validate the preseal from a clean process without adding another staging
    member.
12. **Create-once bootstrap materialization.** Only after a valid preseal,
    materialize bootstrap seeds `2041000001:2041000003` exactly once, bind the
    manifest and sidecar, and revalidate the bound stage. Do not generate a
    phenotype here.
13. **Deployed-Julia zero-seed preflight.** From the exact deployed heads run
    `tools/run-v07-genomic-recovery-v3.sh preflight` after bootstrap
    materialization. Persist its receipt and prove it generates no phenotype.
    This is separate from the retired 16-packet diagnostic in step 10.
14. **Phenotype-RNG admission decision.** Independently re-audit the complete
    chronology. Only an all-green, durable record admits the first Retry-6
    phenotype. Otherwise retire the proposed root without spending a phenotype
    seed.
15. **Compute after admission.** Use Totoro at no more than 96 workers with
    `OPENBLAS_NUM_THREADS=1`, or DRAC scheduled jobs. Never GitHub Actions for
    simulation evidence. Freeze and retire any failed root whole.

## Hard guards

- No bootstrap RNG before the valid step-11 preseal and no phenotype RNG before
  step 14.
- No Retry-4/Retry-5 replay, salvage, repair, pooling, or seed reuse.
- No `git add -A`, wildcard staging, or scaffold staging.
- No review receipt without exact hashes and durable batch/time evidence.
- No generic “throws” assertion where a typed cause is required.
- No simulation campaign on GitHub Actions.
- No D1/D2, activation, merge, capability/count move, release, or G10 from D0F
  infrastructure evidence.

## Paste-ready continuation prompt

```text
Rehydrate from docs/dev-log/handover/2026-07-15-retry5-terminal-retry6-entry.md,
the AGENTS.md snapshot, and R doc 49. Continue only the Retry-6 pre-RNG
ultra-plan after confirming this task has sole ownership and H2-2 remains
archived. Preserve the immutable Retry-4/Retry-5 roots and retired seed spaces
and the quarantined Julia scaffold.
Resume at the two exact-review batches. Treat the earlier deployment and
retired-packet preflight as non-admitting diagnostics and repeat their ordered
post-review gates.
Do not spend a 2041 bootstrap seed before the valid preseal. Do not spend a
2040 phenotype seed until typed mutation semantics, concurrency and
immutability controls, exact local checks, two durably separate exact-review
batches, exact-head CI, clean deployment, retired 16-packet preflight,
historical seed-lock, preseal, create-once bootstrap validation, the deployed-
Julia zero-seed preflight, and an independent chronology audit are all green.
```
