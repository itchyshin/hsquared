# Handover → Codex (hsquared / HSquared.jl)

From: Claude (R lane + coordinator), 2026-06-14.
Repo is the source of truth — rehydrate from git + `docs/dev-log/` before trusting this.

## TL;DR
The R lane has surfaced every model the engine supported, hardened the parser, run a
package-wide honesty audit, and — **maintainer-authorized** — **fast-forwarded Julia
`main` to the Phase 4 multivariate engine (`f9da6bb`)**. Merge is **verified landed**.
The immediate next job is to **build the R multivariate slice** (design + build-notes
ready). There is also a twin-lane SHOULD-FIX backlog.

## Merge: VERIFIED LANDED
- `HSquared.jl origin/main = f9da6bb` ✓ ; `fit_multivariate_reml` present ✓ ;
  `genetic_correlation` exported ✓ ; V4-MULTIVARIATE / V4-MV-REML rows on main (`partial`).
- PR #11 = MERGED. **PR #12 / #14 / #15 still OPEN but ORPHANED** — their commits are all
  in `main` (the FF landed everything); GitHub didn't auto-close them because their bases
  are the stacked branches, not `main`. **Action (Julia lane / maintainer): close
  #12/#14/#15 and delete the merged branches** (`roadmap-status-reconcile`,
  `phase4-multivariate-mme`, `phase4-multivariate-missing`, `phase4-multivariate-reml`).

## State snapshot
- **hsquared `main`** ≈ `5871907`+ (this handover commit on top), clean, CI green.
  `validation_status()` 20 rows (3 covered / 10 partial / 7 planned). Surfaced: v0.1 default
  animal fit (covered) + 6 opt-in experimental models (repeatability, common-env, maternal,
  genomic GREML [Ginv + markers], single-step, SNP-BLUP).
- **HSquared.jl `main` = `f9da6bb`**: adds `fit_multivariate_reml` (G0/R0 REML, genetic/
  residual correlations, per-trait h², cross-trait EBVs, missing-trait), `multivariate_mme`,
  `genetic_correlation`. V4 gates `partial`.
- **Merge evidence:** full twin `Pkg.test()` green locally (Julia 1.10; Phase 4
  26/26 + 8/8 + 24/24, all prior phases green); 6-lens review `wf_113bd991-2b0` → **0
  blockers** (math verified live: MME-vs-GLS EBV ~5e-14, fitted G0 = LLᵀ always PSD).

## Immediate next: build the R multivariate slice
Spec: **`docs/design/09-multivariate-plan.md`** — read its "Build notes from the
merge-readiness review" section (non-negotiable):
- Grammar `cbind(t1, t2, …) ~ fixed + animal(1 | id, pedigree = ped)` → engine `Y` (n×t).
- Reuse univariate `X`/`Z`/`Ainv`; add `Y` as a numeric matrix. **`NA` → Julia `NaN`**
  (default `julia_assign`; engine masks `NaN`). Add an `NA`-round-trips-to-dropped-cell test.
- `initial` is a **named tuple** `(G0 = ..., R0 = ...)`, not a bare 2-tuple.
- **Guard rank-deficient `X`** up front; do **not** present `loglik` for LRT/AIC when
  `converged = false` (engine returns a non-REML fallback loglik there).
- New `target = "multivariate"`, opt-in/experimental, REML-only, mirroring `V4-MV-REML`
  (`partial`). Extractors: `genetic_correlation()`, `genetic_covariance()` (G), per-trait
  `heritability()`, cross-trait `breeding_values()`.
- Keep the validation row **`partial`** (no recovery claim) until the twin commits a `t≥2`
  recovery test.
- Standard slice discipline: TDD, full local gate (`air`, `document`, pkg::-grep,
  `check_pkgdown()`, full `testthat` with juliaup + `NOT_CRAN`, `rcmdcheck --as-cran`),
  multi-lens review, after-task report, check-log + board, CI.

## Twin-lane SHOULD-FIX backlog (Julia lane; none blocked the merge)
Full list in `docs/dev-log/after-task/2026-06-14-phase4-multivariate-merge-readiness.md`:
1. Commit a known-truth `t≥2` G0/R0 + r_g + per-trait-h² recovery test (the cited
   12-rep sim is uncommitted) — **required before any gate moves off `partial`**.
2. `_chol_params_to_cov`/`_cov_to_chol_params` roundtrip unit test (incl. `t≥3`).
3. Reject rank-deficient `X` up front (mirror the univariate guard).
4. Document/guard the `inv(Ainv)` deep-inbreeding conditioning dependence in the GLS path.
5. PSD guard on exported `genetic_correlation` for user-supplied matrices.
6. Sync hand-maintained `docs/src/validation-status.md` to live `validation_status()`
   (it omits the V4 rows).

## Conventions
- **Lane discipline:** R lane = `hsquared/`, Julia lane = `HSquared.jl/`. Don't cross-edit
  without maintainer authorization (the multivariate merge was a one-time exception).
  Coordinate via repo memory (board / check-log / issues), not chat. **Whoever builds the R
  multivariate slice: claim it on the coordination board first to avoid R-lane conflict.**
- **Honesty:** only `covered` rows may be advertised as working; fence opt-in/experimental;
  mirror twin gate states.
- **Commits:** plain imperative subjects, **no `Co-Authored-By` trailer**. Local checks first.
- **Frontier after multivariate:** Phases 4 (factor-analytic G) / 6 (non-Gaussian/GLLVM) /
  7 (inheritance) / 8 (GPU) are unstarted in the engine — twin-gated.
