# Session Handoff: v0.7 genomic GREML cross-twin activation arc

Meta: 2026-07-12 19:30 MDT · from Codex · durable closeout checkpoint

## Critical Context

The scientific endpoint is deliberately negative. A 432-seed diagnostic pilot over the nine
preregistered cells, generated by the pre-repair Julia-only harness, found three low-convergence cells
and two precision blockers. Confirmation did not run. Default R genomic routing was removed; only the
explicit experimental `engine = "julia", target = "genomic"` route remains. Do not call the 432 rows
authoritative end-to-end R-route recovery, do not change a capability/count, and do not merge an
activation/status change without fresh repaired-harness evidence, Rose review, and maintainer G10.

## What Was Accomplished

- Froze the cross-twin VanRaden1/sample-frequency/ridge-0.01 construction and public terminology.
- Added deterministic Julia construction fixtures, hashes, supplied-Q route identity, validation,
  comparator evidence, and a repaired future recovery harness.
- Added narrow explicit R routing, fail-closed validation/provenance, result labelling, independent
  base-R construction and recovery recomputation, and a commit-pinned local live gate.
- Ran a fresh exact-Q BLUPF90 point comparison and banked its exact precision fingerprint.
- Ran the Totoro diagnostic pilot and obeyed the preregistered stop rather than launching confirmation.
- Fixed a real false-green live gate: testthat printed a failure while returning status zero. The gate
  now parses failures and skips, with FAIL 0/1 and SKIP 0/1 mutation controls.
- Closed Fisher/Noether, Darwin, Grace, and Rose reviews. Final verdicts: Darwin CLEAN, Grace CLEAN,
  Rose CLEAN-WITH-LIMITATIONS.

## Current Working State

- Working: explicit marker and supplied-Ginv genomic routes; exact cross-twin fixture; independent
  construction; future fresh-campaign harness; fail-closed local integration gate.
- In progress: GitHub package/docs CI for the two draft PRs.
- Held: default/public activation, confirmation, capability/count change, and G10 status merge.
- Raw diagnostic output remains local at Totoro `~/hsq_work/results/v07_genomic_activation` and Mac
  `/Users/z3437171/hsquared-results/v07_genomic_activation`; never commit or upload it.

## Key Decisions and Rationale

- The ratio is `sigma_g2 / (sigma_g2 + sigma_e2)`, labelled a genomic variance-component ratio on
  the declared relationship scale. It is not pedigree/population narrow-sense heritability or generally
  an average marginal phenotypic-variance fraction.
- The 432 rows are valid for a conservative STOP but invalid for retrospective activation because the
  run bypassed the public R formula and preceded harness immutability/resume/sizing repairs.
- Reusable hardening may be reviewed and merged; broad/default activation remains a separate future G10
  decision after fresh end-to-end evidence.

## Landing State

The landing gate was run in both repositories. It failed only because each repository contains many
pre-existing, out-of-scope local refs with commits not on their configured upstreams. This arc did not
create, edit, switch to, or assume ownership of those refs.

| Artifact / branch | Committed | Pushed | PR | State |
|---|---|---|---|---|
| `HSquared.jl codex/2026-07-12-v07-genomic-activation d3ff927e` | yes | yes | #272 draft | CARRIED-OVER |
| `hsquared codex/2026-07-12-v07-genomic-activation e663537` | yes | yes | #135 draft | CARRIED-OVER |
| Other refs reported by `handoff_gate.sh` | pre-existing | mixed | unrelated | CARRIED-OVER, out of scope |

Why the two arc branches are carried over: CI and maintainer review are pending; the final activation is
explicitly held. Resume with `git switch codex/2026-07-12-v07-genomic-activation && git pull --ff-only`
inside the relevant repository.

Why other refs are carried over: they belong to prior or parallel lanes and were not authorized for
mutation here. Reproduce the exact ledger with
`/Users/z3437171/shinichi-brain/tools/handoff_gate.sh`; resume any named ref only after identifying its
owner, then use `git switch <reported-branch>`.

## Next Immediate Steps

1. Wait for PR #272 and PR #135 package/docs checks; diagnose only failures caused by these branches.
2. Review the negative endpoint and reusable hardening. Do not merge default activation because none is
   present in the final R delta.
3. If a future activation arc is authorized, first localize optimizer behavior on the three failed cells
   using the same failed seeds plus a fresh holdout, with frozen convergence/denominator rules.
4. Only after choosing a predeclared optimizer policy, run a fresh external 16-worker end-to-end
   public-R-route pilot from the committed repaired harness. Size confirmation from the corrected
   upper-SD rule; never reuse the diagnostic rows.
5. Require independent R/Julia recomputation, all nine passing cells, interpretive review, Rose re-audit,
   and explicit maintainer G10 before any default/status change.

## Blockers / Open Questions

- Scientific blocker: three cells below 95% observed convergence and two cells with required
  confirmation N above 2,000 in the diagnostic pilot.
- Governance blocker: no maintainer G10 exists.
- CI was pending when this checkpoint was written.

## Gotchas and Failed Approaches

- `devtools::test()` can print a failed test yet return shell status zero; always use the semantic
  failure/skip parser in `tools/run-v07-genomic-live-gate.sh`.
- Independently reconstructed floating-point matrices can agree at 1e-10 without byte-identical hashes.
  Require exact hashes only for exact engine objects; use tolerance for independent implementations.
- Do not run the campaign on GitHub Actions or upload raw output as an Actions artifact.
- A broad all-live package run encountered an unrelated existing JuliaCall multivariate segfault. The
  dedicated zero-failure/zero-skip genomic gate is the evidence for this arc.

## How to Resume

```sh
cd "/Users/z3437171/Dropbox/Github Local/HSquared.jl"
git switch codex/2026-07-12-v07-genomic-activation
git pull --ff-only
sed -n '1,280p' docs/dev-log/after-task/2026-07-12-v07-genomic-activation-arc.md
sed -n '1,260p' docs/dev-log/recovery-checkpoints/2026-07-12-v07-genomic-activation-pilot-stop.md
gh pr checks 272

cd "/Users/z3437171/Dropbox/Github Local/hsquared"
git switch codex/2026-07-12-v07-genomic-activation
git pull --ff-only
sed -n '1,280p' docs/design/44-v07-genomic-public-activation.md
gh pr checks 135
```

