# After-task report — R twin v0.7 genomic boundary holdout

## 1. Goal

Bind the R public surface and independent oracle to the sealed Julia boundary
candidate, activate only if every scientific and runtime gate passed, and bank
an honest negative endpoint otherwise. The endpoint is negative: scientific
classification passed, but one frozen per-cell runtime gate failed.

## 2. Implemented

- Bound the explicit experimental genomic bridge to the reviewed Julia core.
- Added a standalone base-R boundary oracle and commit-pinned live tests.
- Preserved scale-labelled genomic-ratio and construction provenance wording.
- Synchronized R capability, validation-debt, claims, NEWS, roxygen, vignette,
  coordination, and check-log surfaces to the sealed negative result.
- Kept ordinary default routing rejected and the nine-cell campaign held.

## 3a. Decisions and Rejected Alternatives

- Accepted the conjunctive `BOUNDARY_HOLDOUT_FAIL`; did not relax the 3x limit,
  average across cells, delete a cell, or reuse opened seeds.
- Banked the 30-to-0 classification improvement without calling it activation.
- Kept the public label as a genomic variance-component ratio on the declared
  relationship scale, not pedigree, population, or narrow-sense heritability.

## 4. Files Touched

`R/hs_control.R`, generated `man/hs_control.Rd`, `NEWS.md`, the capability,
validation-debt, and public-claims registers, the genomic/model-status/package
vignettes, coordination board, check log, and this report. Earlier commits on
the branch contain the bridge, extractor, tests, and independent oracle.

## 5. Checks Run

- Frozen R engine-free suite: 1,896 pass, 0 fail, 0 warn, 68 ordinary skips.
- Frozen commit-pinned live genomic R-Julia suite: 265 pass, 0 fail, 0 warn,
  0 skip.
- Independent base-R aggregation reproduced every classification/count and all
  five p95 ratios.
- Final tree: `devtools::document()` regenerated `man/hs_control.Rd`; full
  `devtools::test()` completed with 0 failures, 0 warnings, and 68 expected
  skips; the focused oracle test passed; `pkgdown::check_pkgdown()` found no
  problems; repaired `R CMD check --no-manual` finished 0 errors, 0 warnings,
  and 0 notes; `git diff --check` and the close-out compiler passed.

## 6. Tests of the Tests

The oracle rejects altered parameters, hashes, arm identities, endpoint rules,
file sets, nonfinite inputs, and sidecars. The conjunctive gate turned red even
with a 30-to-0 classification win. The post-seal launcher fix has a synthetic
argument-wiring negative control and changed no scientific output.

## 7a. Issue Ledger

| Issue | Disposition |
| --- | --- |
| Boundary classifications missed by default AI-REML | Resolved on 30 sealed endpoints; independent oracle matched. |
| One cell exceeded runtime cap | Retained as blocker; no activation or rerun. |
| Original oracle launcher passed literal `{}` | Fixed post-seal; orchestration-only and mutation-tested. |
| Older public surfaces stopped at the 432-row diagnostic | Superseded with the sealed 240-row result. |
| Built-package check could not find the repo-only oracle tool | Added an explicit installed-package skip before normalization; local oracle tests still run. |

## 8. Consistency Audit

The R-facing formula, result, documentation, and claim surfaces keep genomic
fitting explicit, experimental, Gaussian REML-only, dense, and validation-scale.
No surface claims broad recovery, production performance, calibrated intervals,
ordinary heritability, default activation, release, or G10. The covered count
remains 5.

Memory receipt: the ultra-plan, ask-brain, rehydration/team-dispatch,
validation-harness, R-package-engineer, validation-canon, Rose pre-public, and
after-task contracts were loaded and applied. They caused the git-first sweep,
exact cross-twin binding, local-only Totoro compute, tests-of-tests, independent
review, and negative-endpoint discipline.

## 9. What Did Not Go Smoothly

The scientifically better boundary candidate failed the operational contract.
An oracle fan-out shell bug produced zero oracle files, but did not affect the
sealed packets; the same frozen oracle was run after a mechanical correction.
Independent Fisher/Darwin and Rose review then found stale status surfaces and
a missing per-cell paired table, which were repaired before closeout. The first
final `R CMD check` also found a repository-path assumption in the oracle test;
the repaired rerun was clean.

## 10. Known Residuals

- `n120_m600_r050` was 5.99x at p95 versus the frozen 3x cap.
- The 240 holdout seeds are spent and cannot adjudicate a revised candidate.
- A new arc owes component/allocation profiling on open discovery data,
  output-equivalent performance work, a new preregistration, and a fresh
  untouched holdout before recovery can resume.
- Default activation, nine-cell recovery, G10, and release remain held.

## 11. Team Learning

Correctness and activation are distinct. Operational gates must stay
conjunctive, and a scientifically superior estimator can still require
performance engineering before public routing.

Golden Set: not run because memory retrieval/routing code did not change. The
in-scope recurring failures were exercised directly through provenance,
boundary, oracle, seal, denominator, runtime, and launcher negative controls.

## 12. Cross-Product Coverage

- R bridge covers the explicit single-genomic-effect Gaussian REML route; it
  does not cover ML, non-Gaussian, slopes, multiple effects, or default routing.
- Construction covers sample-frequency VanRaden1 plus ridge 0.01 and exact
  provenance; it does not cover alternate methods, frequencies, weights, or
  production panels.
- Validation covers 240 sealed five-cell holdouts, 30 wins, 0 losses, and net
  gain `0.125`; cell attempted/wins/losses were 48/11/0, 48/2/0, 48/11/0,
  48/6/0, and 48/0/0. It does not cover a passing activation gate or recovery.
- Execution covers local Totoro packets and independent R oracles; it does not
  cover GitHub Actions simulation artifacts, reused seeds, threshold changes,
  automatic merge, count promotion, or release.
