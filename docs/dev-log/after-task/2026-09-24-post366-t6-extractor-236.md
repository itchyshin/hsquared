# After-task: post-#366 T6 extractor return-shape (#236)

## 1. Goal

Make `heritability_standard_error()` return the same public table shape as
`heritability()` (`data.frame(term, se)`), without changing estimands,
version, or `public_covered_count`.

## 2. Implemented

- Emmy/Boole call: issue option (1). Extractor wraps a stored bare numeric
  into `data.frame(term, se)`; also accepts an already-shaped frame.
- Roxygen / NEWS document the merge-on-`term` contract and the breaking
  change for callers that treated the return as a length-1 numeric.
- Fixture tests pin shape + `merge(heritability(fit), ...)` and update the
  sparse-route and bridge-errors live expectations to use `$se`.

## 3a. Decisions and Rejected Alternatives

Rejected option (2) (pin asymmetry) and (3) (combined accessor only): the
goal was consistent public shapes. Rejected changing bridge storage or
`summary()` printing of the internal numeric field. No bridge / formula /
genomic / FA / MV+PE / cran-comments edits (lane OWNS).

## 4. Files Touched

- `R/extractors.R`
- `man/variance_component_standard_errors.Rd`
- `tests/testthat/test-variance-component-se.R`
- `tests/testthat/test-repeatability-sparse-352.R`
- `tests/testthat/test-bridge-errors-351.R`
- `NEWS.md`
- `docs/dev-log/check-log.md`
- `docs/dev-log/after-task/2026-09-24-post366-t6-extractor-236.md`

## 5. Checks Run

- `devtools::document()` (man page only; NAMESPACE style churn reverted)
- `testthat::test_file("tests/testthat/test-variance-component-se.R")` →
  PASS 14
- `testthat::test_file("tests/testthat/test-summary-uncertainty.R")` →
  PASS 9 (summary still prints internal bare numeric)

## 8. Consistency Audit

Version **0.9.0**; `public_covered_count` **7**. No covered flip. No estimand
change. Experimental badge unchanged.

## 9. What Did Not Go Smoothly

`air format` rewrote unrelated lines in live-Julia test files; those
drive-by reformats were reverted before commit.

## 10. Rose claim-vs-evidence

CLEAN for this slice: shape-only public API honesty; no capability status
row flip; no coverage claim.

## Lenses

Active lenses: Emmy, Boole, Rose (perspective). Spawned subagents: none.
Current lane: R (`cursor/post366-t6-extractor-236`).
