# After-task — Pat leftover: shorten ?hsquared front door

**Date:** 2026-09-02
**Lane:** R (`hsquared`) — campaign worktree
`~/local-scratch/lanes/hsquared-h2-twin-20260901`, branch
`claude/lane-h2-twin-20260901`.
**Active lenses:** Pat (applied-user walk). Ada/Shannon/Rose as
perspectives.
**Spawned subagents:** none
**Current lane:** R `?hsquared` roxygen only

**Fence held:** `public_covered_count` **5** · no `validation_status()`
row · draft #141 · no G10 · no merge-to-main claim.

---

## Task goal

Finish the remaining first-screen leftover: make `?hsquared` a short
user front door (live path + limits link), not a genomic / Laplace /
Willham audit.

## Files changed

- `R/hsquared.R` — short default-path description; limits link in
  Details; family/return no longer say "v0.1 parser accepts only
  gaussian". ASCII only.
- `man/hsquared.Rd` — regenerated by `document()`.
- `tests/testthat/test-hsquared-help-frontdoor.R` — pins live path +
  limits URL; forbids VanRaden / Laplace / Willham /
  `genomic_variance_ratio`; length-caps the description.
- this report + `docs/dev-log/check-log.d/2026-09-02-h2-pat-hsquared-frontdoor.md`
- `docs/dev-log/coordination-board.md` (leftover row closed)

## Checks run and exact outcomes

- `devtools::test(filter = "hsquared-help-frontdoor")` → **FAIL 0 /
  WARN 0 / SKIP 0 / PASS 12**.
- Related honesty filters → **FAIL 0 / PASS 57**.
- `tools::showNonASCIIfile` clean on the two source files.

## Public claim audit

Wording only. No capability-status, validation-debt, or
`validation_status()` edit. The page points at the limits article, not
at `validation_status()` as the user list.

## Tests of the tests

The new Rd assertions fail if the audit paragraph returns, or if the
live formula / validate / current-limits tokens drop.

## Coordination notes

Stale Grace lease `cursor:hsquared:grace-ascii-141` released after the
ASCII session ended at `cd13ac9` (its own `--release` was blocked by
Auto-review). This slice claimed
`cursor:hsquared:pat-hsquared-frontdoor` on `R/hsquared.R` and
`man/hsquared.Rd` only. Sibling dirty `man/*.Rd` left unstaged.

Foreign Codex v0.7 performance lane remains live — not touched. Its
`R/hsquared.R` still carries the long genomic audit plus held
activation copy; that is a different branch.

## What did not go smoothly

`lane_liveness_check.py` classed the Grace lease UNKNOWN (no
`LANE_PID`). Holder-gone was judged from the finished Grace transcript
and clean Grace paths, not from the helper PID.

## Known limitations

- `?hsquared` has no examples (same as before).
- Live github.io still serves `main`, not this branch.

## Next actions

Keep the fence. No covered flip.
