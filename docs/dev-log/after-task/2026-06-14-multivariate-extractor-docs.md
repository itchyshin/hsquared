# Multivariate extractor documentation examples

Date: 2026-06-14

## Task goal

Make the multivariate extractor reference topic more useful by showing the
opt-in `cbind()` fit workflow and the G/R covariance and correlation extraction
calls in the generated Rd help page.

## Active lenses and spawned agents

Active lenses: Emmy, Pat, Rose, Grace.

Spawned agents: none.

Current lane: R/docs.

## Files created or changed

- `R/extractors.R`: added `@examplesIf FALSE` workflow examples and a short
  convergence-diagnostics note to the multivariate extractor topic.
- `man/multivariate_extractors.Rd`: regenerated by roxygen2.
- `docs/design/11-next-50-slices.md`, `docs/dev-log/check-log.md`,
  `docs/dev-log/coordination-board.md`: slice status and evidence.

## Checks run and exact outcomes

- `command -v air || true`: no `air` binary on PATH.
- `/Library/Frameworks/R.framework/Resources/bin/Rscript -e "devtools::document()"`:
  passed; wrote `man/multivariate_extractors.Rd`.
- `RSTUDIO_PANDOC=/Applications/RStudio.app/Contents/Resources/app/quarto/bin/tools/aarch64 /Library/Frameworks/R.framework/Resources/bin/Rscript -e "pkgdown::check_pkgdown()"`:
  passed, "No problems found."
- `/Library/Frameworks/R.framework/Resources/bin/Rscript -e "devtools::test(filter = 'multivariate|fit-object')"`:
  0 failures / 0 warnings / 2 live-Julia skips / 96 passes.
- `/Library/Frameworks/R.framework/Resources/bin/Rscript -e "devtools::test()"`:
  0 failures / 0 warnings / 27 live-Julia skips / 561 passes.
- `git diff --check`: passed.
- `RSTUDIO_PANDOC=/Applications/RStudio.app/Contents/Resources/app/quarto/bin/tools/aarch64 /Library/Frameworks/R.framework/Resources/bin/Rscript -e "devtools::check(document = FALSE, args = '--no-manual')"`:
  0 errors / 0 warnings / 0 notes; examples checked OK.
- Remote checks for commit `21161a5`:
  - GitHub Actions R-CMD-check `27499072097`: passed.
  - GitHub Actions pkgdown `27499072089`: passed.
  - GitHub Pages build/deployment `27499111169`: passed.

## Public claim audit

Rose verdict: clean.

No new capability was claimed. The examples are wrapped in `@examplesIf FALSE`
because the workflow requires local Julia, `JuliaCall`, and `HSquared.jl`.

## Tests of the tests

`R CMD check` reached `checking examples ... OK`, confirming that the generated
examples are valid Rd examples and do not accidentally execute the Julia bridge.

## Coordination notes

This is R documentation only. No Julia contract changed.

## What did not go smoothly

`air` is still unavailable on PATH. No formatting command was run.

## Known limitations

The examples are illustrative and non-running. The multivariate model remains
partial, opt-in, and gated on Julia-side recovery/comparator evidence.

## Next actions

- Commit and push; watch R-CMD-check, pkgdown, and Pages.
- After the Julia twin writes the deterministic multivariate fixture, consume it
  in the R lane.
