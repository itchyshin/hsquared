# After-task — A29 A4 + A21 C6 leftover (claims register)

**Date:** 2026-09-02
**Lane:** R (`claude/lane-h2-twin-20260901`)
**Active lenses:** Rose, Falconer (perspectives)
**Spawned subagents:** none
**Checks:** `docs/dev-log/check-log.d/2026-09-02-h2-a21-a4-c6-register.md`

## Goal

Close leftover Rose wording on the claims register: A4 `(when flipped)`
conditional, and the C6 two-`m2` sentences C6 left when the register was busy.

## Files changed

- `docs/design/06-public-claims-register.md` — three allowed-wording clauses
- `tests/testthat/test-claims-register-a4-c6.R` — new source-tree-only guard
- this report and the check-log shard

## Checks

`devtools::test(filter = "claims-register")` → **FAIL 0 / SKIP 0 / PASS 6**.
RR claim-surface suite still **84 / 0 / 1 skip**. Live `validation_status()`
**21 / 4 covered**.

## Public-claim audit

No status word moved. `public_covered_count` stays **5**. Multivariate stays
`partial`. The new sentences distinguish two already-documented `m2`
estimands and condition the covered-scope claim on a flip that has not happened.

## Tests of the tests

Temp-copy mutation of `(when flipped)` and `(no covariance)` → both pins fail;
repo file restored/untouched.

## Coordination

Lease: register + this test/evidence pair. C5 (`04-validation-canon.md`),
C6 (`test-direct-maternal.R`), and C9 (after-task `a31a0a8` + board) were not
edited. G-AG-5 still owner-gated. Foreign v07 branch also touches this file
and does **not** carry these sentences; not imported.

## What did not go smoothly

A sandboxed mutate-in-place of the register was refused (correctly). The
test-of-tests ran against a temp copy instead.

## Known limitations

`formula_status()` / `validation_status()` already had the A4 conditional;
only the register was stale. Extractor fences were already named in C6.

## Next actions

No further A4/C6 register work. Remaining agent-safe leftovers are thin
(C8 = keep `R` absent). Owner: Darwin / G10 / DP-10 / G-AG-5. No covered
flip, no merge from this slice.
