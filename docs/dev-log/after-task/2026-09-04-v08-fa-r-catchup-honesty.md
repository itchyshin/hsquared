# After-task — R FA S0 honesty catch-up (not R-public)

Pointer follow-up to #169. Count stays **7**. R FA stays **planned**.
No `cov = fa` parse. No 0.8.0 bump. No G10.

Julia `V4-FA` is engine-covered at `60895208`. Rejects now say that,
instead of stale “experimental #292 / 8/10 PASS”. The R bridge still
rejects `"factor_analytic"`.

Plan: `~/local-scratch/h2-r-fa-catchup-plan-2026-09-04.md`.
Activation (S2+) stays blocked on Boole / Hopper / owner.
