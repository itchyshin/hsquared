# After-task — Cut the v0.1.0 git tag + GitHub release (2026-06-19)

## Task goal

Close the one remaining R-lane action carried in the session handoff: the
`v0.1.0` git tag and GitHub release object did not exist yet. The package was
already version-bumped (`DESCRIPTION: Version: 0.1.0`), pushed, and CI-green, but
nothing tagged the released state or published a release page. Maintainer
authorized "Tag + GitHub release".

## Active lenses and spawned agents

- Active lenses: Ada (release readiness), Grace (release hygiene / CI), Rose
  (honesty audit), Shannon (coordination).
- Spawned subagents: none. This was a single maintainer-authorized packaging
  action; no multi-lens review workflow was warranted.
- Current lane: R / release.

## Files changed

- `docs/dev-log/check-log.md` — new "Cut the v0.1.0 git tag + GitHub release"
  evidence section (exact commands + outcomes).
- `docs/dev-log/coordination-board.md` — new release row.
- `docs/dev-log/after-task/2026-06-19-session-handoff.md` — "what's left" item 1
  struck through and marked DONE.
- `docs/dev-log/after-task/2026-06-19-v0.1.0-release-tag.md` — this report.

No `R/`, `tests/`, `man/`, `NEWS.md`, `DESCRIPTION`, or vignette change. The tag
and release reference existing committed state; the release body is `NEWS.md`
verbatim.

## Checks run and exact outcomes

- Pre-state: `git tag -l` empty, `gh release list` empty (confirmed the tag/
  release genuinely did not exist).
- `git tag -a v0.1.0 -m "hsquared 0.1.0"` at HEAD `6d25c7d`.
- `git push origin v0.1.0` -> `* [new tag] v0.1.0 -> v0.1.0`.
- `gh release create v0.1.0 --verify-tag --title "hsquared 0.1.0" --notes-file NEWS.md`
  -> release URL returned.
- `gh release view v0.1.0` -> tag=v0.1.0, name="hsquared 0.1.0", draft=false,
  prerelease=false, commit=main, published 2026-06-19T11:08:08Z.
- `gh release list` -> "hsquared 0.1.0 | Latest | v0.1.0".
- URL: https://github.com/itchyshin/hsquared/releases/tag/v0.1.0
- Standing package evidence (unchanged by this action): from the release commit,
  `devtools::test()` 793 pass / 0 fail / 0 warn / 27 skip; `devtools::check(--no-manual)`
  0 errors / 0 warnings / 0 notes; R-CMD-check dispatched green at `b0153aa`
  (`27803010380`), pkgdown + Pages green, site HTTP 200.

## Public claim audit (Rose lens)

- The release introduces **no new capability claim**. The release notes are the
  already-audited `NEWS.md` 0.1.0 section (multiple prior Rose honesty sweeps;
  opt-in/experimental/partial framing intact, no over-claim). Nothing was
  promoted from planned/partial to covered.
- `prerelease=false` is correct: v0.1.0 is a real, CI-green, validated release of
  the v0.1 contract (default Gaussian animal model), not a preview.
- `validation_status()` rows and `capability-status.md` are untouched and remain
  the live source of truth.

## Tests of the tests

- Not applicable — no test or fixture was added or changed. The packaging action
  was verified directly against the GitHub API (`gh release view`/`list`) and
  the git remote (tag push confirmation), which are the authoritative checks for
  a tag/release object.

## Coordination notes

- Lane discipline held: R lane only; `HSquared.jl` untouched.
- Tag target choice surfaced to the maintainer before acting: HEAD `6d25c7d` vs
  the dispatched-green checkpoint `b0153aa` differ only by two docs-only commits
  (CI-evidence record + handoff doc), both at version 0.1.0. Tagged HEAD as the
  conventional released-state choice.
- The R-lane maintainer queue is now empty.

## What did not go smoothly

- The first `gh release view` verification used an invalid JSON field
  (`isLatest`), exiting non-zero **after** the release was already created
  successfully. Re-ran with valid fields (`isDraft`/`isPrerelease` + a
  `gh release list` "Latest" check) to confirm. No impact on the release.

## Known limitations / uncertainty

- This is a packaging milestone, not a capability change. v0.1's honest scope is
  unchanged: the default fit needs a local Julia + `JuliaCall` + an `HSquared.jl`
  checkout; public CI exercises the pure-R REML reference and skip-guards the
  live-engine tests.
- The release was cut at HEAD, two docs-only commits past the commit where full
  R-CMD-check was explicitly dispatched green (`b0153aa`). Those commits cannot
  affect R-CMD-check (no `R/`, test, `man/`, or `DESCRIPTION` change), but
  R-CMD-check was not re-dispatched at `6d25c7d`.

## Next actions

All remaining work is twin-gated or Julia-lane (R lane must not act):

1. **Land `HSquared.jl` PR #17** (factor-analytic G, DRAFT) — unblocks the
   prepared R slice (`cov = diag()/lowrank()/fa()` +
   `genetic_loadings()`/`specific_variance()`/`latent_breeding_values()`/
   `eigen_G()`). R must not self-merge.
2. **Julia-lane reword** `HSquared.jl/docs/design/03-engine-contract.md:277`
   (the "ratio ~0.99 on a 250-animal simulation" string) — exact replacement and
   backing test recorded in the handoff and coordination board.
3. Phases 3/5/6/7/8 frontier (twin-led; R surfaces + validates after landing).
