# After-task — WS2 slice #11: experimental `heritability_interval()` bridge (2026-06-19)

## Task goal

First WS2 "bridge what's been done" slice: surface the engine's exported
`heritability_interval` (on `HSquared.jl origin/main`; row `V1-HERIT-CI` = `partial`) as an
**experimental** R extractor on the default Gaussian animal-model fit. Honest, additive, and
verifiable on the R side without a live engine.

## Active lenses and spawned agents

- Lenses: Hopper (bridge marshalling — spawned subagent), Emmy/Fisher/Falconer (extractor/
  estimand — applied as review perspectives), Rose (honesty — self-applied + grep).
- **Spawned subagent:** `hopper-r-julia-translator` — adversarial review of the un-runnable
  Julia path. Verdict **SHIP-WITH-NITS**, 0 fixes required.
- Current lane: R.

## Files changed

- `R/extractors.R` — new `heritability_interval()` generic + `.default` (errors) +
  `.hsquared_fit` (via `hs_fit_result`), experimental/partial roxygen.
- `R/julia-bridge.R` — default `engine="fit"` (`fit_ai_reml`) path: guarded Julia enrichment
  calling `HSquared.heritability_interval(hsq_fit)` inside a `try` (the engine throws on a
  boundary h²); `hs_normalize_julia_result()` passthrough + new `hs_normalize_heritability_interval()`
  helper (1-row data frame; `se` → `NA` for the profile method).
- `NAMESPACE`, `man/heritability_interval.Rd` — generated by `document()`.
- `_pkgdown.yml` — `heritability_interval` added to the reference index.
- `tests/testthat/test-heritability-interval.R` — fixture extractor test, `.default` + missing-
  field error tests, and a normalizer test for both delta and profile shapes.
- `DESCRIPTION` — version `0.1.0` → `0.1.0.9000` (post-release dev version).
- `NEWS.md` — new `# hsquared (development version)` section with an honest experimental bullet.
- `docs/design/capability-status.md` — extractor row notes the new experimental extractor.

## Checks run and exact outcomes

- `air format` clean; `devtools::document()` (registered the new exports; only created the new
  `.Rd`, no `RoxygenNote` bump, no other `man/*` churn — verified via `git status`).
- `devtools::test()` — **804 pass / 0 fail / 0 warn / 27 skip** (+11 from the new tests; the 27
  skips are the live-engine tests, which skip cleanly — the live bridge was not active this run).
- `pkgdown::check_pkgdown()` — clean (after adding the topic to the reference index).
- `devtools::check(args="--no-manual")` — **0 errors / 0 warnings / 1 note**; the note is the
  benign environmental "unable to verify current time" (no time server), not a package issue.
- Committed `56f8fb5`; pushed; pkgdown deploy **green** at `56f8fb5` (CI policy: push deploys
  pkgdown only). Local `check` is the gate per the local-checks-over-CI policy.

## Public claim audit (Rose lens)

- No capability promoted. The extractor is documented **experimental**, explicitly mirrors the
  twin `partial` row `V1-HERIT-CI`, and states it is asymptotic, REML-only, not coverage-
  calibrated, "not a validated capability". NEWS + capability-status carry the same framing.
  Honesty grep over the touched surfaces found no bare capability assertion.

## Tests of the tests

- The normalizer test asserts the **profile** shape (no `se`) normalizes to `NA` rather than
  erroring — the failure mode if the helper assumed `se` always present.
- The missing-field test confirms a fit without the interval errors with the documented message
  (not a silent `NULL`).

## Coordination notes

- Hopper confirmed the one un-runnable risk — nested-NamedTuple → R named-list marshalling — is
  **already load-bearing** in `julia-bridge.R` (`variance_components`, `breeding_values`,
  `random_effects.animal` cross the same way), so the new flat NamedTuple field rides the verified
  mechanism; no Julia-side `Dict` conversion needed.
- Verified the engine function is exported on `HSquared.jl origin/main` (`4e8ffde`); the
  `isdefined`/`applicable` guard means graceful no-op against an engine that lacks it.

## What did not go smoothly

- Nothing material. `check_pkgdown()` initially flagged the new exported topic missing from the
  reference index (expected); fixed by adding it.

## Known limitations / uncertainty

- **Live path not exercised this run** (the live-engine tests skipped — Julia bridge inactive).
  The R-side logic is fully fixture-verified; the actual Julia→R marshalling of the
  `heritability_interval` NamedTuple is verified by inspection + by the identical existing nested-
  NamedTuple paths, not by a live run this session. This matches the project's standard
  skip-guarded live-bridge practice; a live confirmation is part of the ordinary per-slice cadence.
- **Scope (intentional, per Hopper nit 2):** added to the default `engine="fit"` (`fit_ai_reml`)
  path only — the path users actually hit. The opt-in `sparse_reml`/`henderson_mme`/`fit_animal_model`
  AnimalModelFit paths could surface it too; recorded as a follow-up, not an oversight.
- Stays `partial`: `V1-HERIT-CI` is twin-owned and partial (no coverage calibration, no profile/
  bootstrap alternative validated). R cannot promote it.

## Next actions

- WS2 Class-A remaining slices (same pattern): #12 `repeatability_interval`, #14 `single_step`
  routing, #13 REML genomic variants, #21 PEV/reliability unconditional. Class-B (#22/#23/#18)
  stays twin-gated on result-payload exposure.
- Optional follow-up: extend `heritability_interval` enrichment to the opt-in REML AnimalModelFit
  paths.
