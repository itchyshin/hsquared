# After-task — R pointer after Julia V4-FA G10

Pointer only, count **7**. R FA stays planned. No 0.8.0.

Julia engine `V4-FA` covered at `60895208`. This lane records the twin
pointer so R does not silently look uncovered-or-covered. The reject of
`"factor_analytic"` stays.

SS not flipped. Rose was CLEAN pre-flip on the Julia packet.
