# After-Task: 0.2.0 code-arc baseline assembly merge

**Date:** 2026-07-11 · **Lane:** R coordinator / release-assembly ·
**Owner:** Ada / Shannon / Rose

## Task goal

Merge the five already-reviewed, CI-green PRs of the `0.2.0` code arc onto
`main` in the correct order, resolving conflicts faithfully, and verify the
assembled baseline is honest and green — **without** any capability promotion or
version bump (both deferred). Maintainer authorized the merge; the release *cut*
itself remains separately gated.

## Active lenses and spawned agents

- Lenses: Ada (integration), Shannon (branch/overlap coordination), Rose
  (pre-public honesty audit).
- Spawned subagents: **1** — `rose-systems-auditor` (background, pre-public audit
  of the assembled whole). Verdict **CLEAN — no blockers**.
- Current lane: coordinator / R.

## Files created or changed

- `main` gained 5 `--no-ff` merge commits: #125 (`065e128`), #127 (`f7aa2d8`),
  #128 (`ebe101f`), #129 (`60350bc`), #130 (`f43c93a`); base `34dfb6d`.
- Conflict resolutions: `man/direct_maternal_extractors.Rd` (generated → took
  #128's badged version; `document()` confirmed source-consistent),
  `docs/dev-log/coordination-board.md` (hand-merge: #125 header correction +
  both 2026-07-09 and 2026-07-10 rows, newest-first).
- Close-out (this commit): `docs/dev-log/check-log.md` (2026-07-11 entry),
  `docs/dev-log/coordination-board.md` (assembly row + header scope-fix from Rose
  note 1), this report.

## Checks run and exact outcomes

- Pre-merge cumulative simulation (`git merge-tree --write-tree`, chained):
  #125/#127/#130 clean; #127∩#128's six shared R/man files auto-merged clean;
  only two conflicts surfaced (the generated `.Rd`, the board).
- `Rscript -e 'devtools::document()'` on assembled `HEAD`: **zero delta**.
- `air format` on the 20 merged R files: delta only in pre-existing
  non-conformant code (identical on pristine `origin/main`) → **reverted** as
  out-of-scope; merged R tree preserved as-is.
- Full `Rscript -e 'devtools::test()'`: **exit 0, 0 failures**, 9 expected skips
  (live JuliaCall/Julia/`pedigreemm` absent locally).
- `_R_CHECK_FORCE_SUGGESTS_=false rcmdcheck(args = c("--as-cran","--no-manual"),
  error_on = "never")`: **0 errors / 0 warnings / 2 notes** — (1) new-submission
  / dev-version `0.1.0.9000` (expected); (2) top-level `scratchpad/` (untracked
  local dir, not in git, absent from clean CI checkout).

## Public claim audit

- `public_covered_count` reads **5** on every public surface (capability-status,
  public-claims register, NEWS, handover); no surface states 4 or 6 as the
  current total; the single "5→6" is an explicit *future* candidate.
- `validation_status()` covered rows **4**, byte-identical between `origin/main`
  and assembled `HEAD` (#128's `validation-status.R` diff is a badge doc-line
  only).
- `DESCRIPTION` Version stays `0.1.0.9000`; no `# hsquared 0.2.0` NEWS header —
  no shipped-0.2.0 claim.
- `relmat()`/`precision()` marked partial / experimental / NOT-covered
  everywhere; intervals + repeatability remain experimental, repeatability's
  banked-negative ~0.2% point-bias caveat intact and not overstated.
- Rose independent audit: all five invariants PASS; no leftover conflict markers;
  `git diff --check` clean.

## Tests of the tests

- The assembled tree's zero-delta `document()` is itself a test of the
  conflict resolution: it proves the merged `man/` matches merged source, so the
  `--theirs` choice on the generated `.Rd` introduced no drift.
- The `air format` cross-check against pristine `origin/main` proved the
  formatting delta was pre-existing, not merge-induced — preventing a spurious
  "surgical" reformat of unrelated code.

## Coordination notes

- The 0.2.0 R baseline pairs semantically with the twin `HSquared.jl` 0.1
  honesty PRs `#267/#268`; the R merge has no git dependency on the twin, but
  release parity does — flagged for the maintainer before the cut.
- Board updated with the assembly row; check-log carries the exact commands.

## What did not go smoothly

- GitHub's per-PR "MERGEABLE/CLEAN" is measured against *current* `main` only;
  since the four feature PRs are parallel branches (not stacked), that flag did
  not predict the two sequential conflicts. The `merge-tree` simulation caught
  them before touching `main`.
- `mapfile` is unavailable in the local bash 3.2 / zsh; array-driven scripts
  were rewritten portably.
- `air format` reformats pre-existing non-conformant code (documented gotcha);
  handled by reverting the out-of-scope delta.

## Known limitations

- This is assembly only — no promotion, no version bump, no NEWS finalization.
- The release *cut* remains maintainer-gated: twin `#267/#268` parity,
  stale-`v0.1.0`-tag call, `SystemRequirements` wording, release-go sequencing
  (Julia registers `v0.x` first).
- Pre-existing `air` non-conformance in `R/extractors.R`, `R/gwas.R`,
  `R/validation-status.R` remains (flagged separately, not fixed here).

## Next actions

1. Maintainer settles the three release decisions; confirm twin `#267/#268`.
2. Final assembly pass on the merged baseline: NEWS `0.2.0` (+ a `relmat()`/
   `precision()` experimental bullet — Rose note 2), `DESCRIPTION` → `0.2.0`
   (last), engine-free vignette/man build audit, 3-leg `--as-cran` + check-log,
   `cran-comments.md`.
3. Release-go: Julia registers `v0.x` first (3-day AutoMerge), then R to CRAN.
