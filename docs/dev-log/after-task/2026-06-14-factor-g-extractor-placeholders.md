# Reserved factor/G-matrix extractor names

Date: 2026-06-14

## Task goal

Reserve future factor-analytic and G-matrix extractor names without implying
that factor-analytic G matrices, loading interpretation, latent breeding values,
rank selection, or uncertainty are implemented.

## Active lenses and spawned agents

Active lenses: Ada, Shannon, Boole, Kirkpatrick, Fisher, Rose, Pat.

Spawned agents: none.

Current lane: R.

## Files created or changed

- `R/extractors.R`: added `loadings()`, `specific_variance()`,
  `latent_breeding_values()`, and `eigen_G()` as reserved extractors with
  planned/rotation-aware messages for `hsquared_fit`.
- `tests/testthat/test-fit-object.R`: added tests for the planned errors and
  the stats fallback for `loadings.default()`.
- `NAMESPACE` and `man/factor_g_extractors.Rd`: regenerated by roxygen2.
- `_pkgdown.yml`: indexed the new extractor topic.
- `docs/design/11-next-50-slices.md`: current-status update and row 20 marked
  done.
- `docs/dev-log/check-log.md`: command evidence recorded.
- `docs/dev-log/coordination-board.md`: added this slice row.
- `docs/dev-log/after-task/2026-06-14-factor-g-extractor-placeholders.md`: this
  report.

## Checks run and exact outcomes

- `/Library/Frameworks/R.framework/Resources/bin/Rscript -e
  "devtools::document()"`: passed; regenerated `NAMESPACE` and added
  `man/factor_g_extractors.Rd`.
- `/Library/Frameworks/R.framework/Resources/bin/Rscript -e
  "devtools::test(filter = 'fit-object')"`: passed, 0 failures / 0 warnings /
  0 skips / 81 passes.
- `command -v air`: no `air` binary on PATH.
- `git diff --check`: passed.
- `/Library/Frameworks/R.framework/Resources/bin/Rscript -e
  "devtools::test()"`: passed, 0 failures / 0 warnings / 27 live-Julia skips /
  628 passes.
- `RSTUDIO_PANDOC=/Applications/RStudio.app/Contents/Resources/app/quarto/bin/tools/aarch64
  /Library/Frameworks/R.framework/Resources/bin/Rscript -e
  "pkgdown::check_pkgdown()"`: passed, "No problems found."
- First package check:
  `RSTUDIO_PANDOC=/Applications/RStudio.app/Contents/Resources/app/quarto/bin/tools/aarch64
  /Library/Frameworks/R.framework/Resources/bin/Rscript -e
  "devtools::check(document = FALSE, args = '--no-manual')"`: failed with 1
  warning because the generated `factor_g_extractors.Rd` documented method-only
  `effect` and `rotate` arguments outside the generic usage signatures.
- Roxygen fix: moved the method-only argument explanation into prose and reran
  `/Library/Frameworks/R.framework/Resources/bin/Rscript -e
  "devtools::document()"`: passed.
- Recheck after roxygen fix:
  - focused tests passed, 0 failures / 0 warnings / 0 skips / 81 passes;
  - `git diff --check`: passed;
  - `pkgdown::check_pkgdown()`: passed, "No problems found";
  - full tests passed, 0 failures / 0 warnings / 27 live-Julia skips / 628
    passes;
  - `devtools::check(document = FALSE, args = '--no-manual')`: passed, 0 errors
    / 0 warnings / 0 notes.

## Public claim audit

Rose verdict: clean, conditional on the methods continuing to error for
`hsquared_fit` until validation exists.

No factor-analytic fitting, loading interpretation, specific-variance
estimation, latent breeding values, eigen-G evidence, standard errors,
confidence intervals, LRT/rank-selection guidance, or GPU capability is added.

## Tests of the tests

The new tests construct a minimal `hsquared_fit` and call all four reserved
extractors directly. They check for planned/not-implemented wording and the
rotation-nonunique warning.

## Coordination notes

This closes next-50 row 20 on the R side. The Julia twin still owns structured
recovery, loading metadata hardening, rotation policy, and result-shape
promotion.

## What did not go smoothly

`loadings()` already exists in `stats` as a non-S3 helper. The package now
defines a generic for hsquared use, with `loadings.default()` delegating to
`stats::loadings()` for non-`hsquared` objects.

## Known limitations

The names are reserved only. They are not evidence that `cov = fa(K)` fits, that
loadings are interpretable, or that `eigen_G()` has a validated rank-selection
meaning.

## Next actions

- Commit and push.
- Continue with documentation/claim audit or the next R-safe placeholder slice.
