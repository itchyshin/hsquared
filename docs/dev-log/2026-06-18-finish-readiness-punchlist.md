# Finish-readiness review — confirmed punch-list (2026-06-18)

Multi-lens ultracode review (`wf_c6e56a2d-5bc`): **25 confirmed** of 29 raw findings across 8 lenses, each adversarially verified. R lane only; HSquared.jl cross-referenced read-only. Severity is the verifier's corrected severity.


## R-safe (actionable in the R lane) — 23

| # | Severity | Lens | Finding | Recommendation |
|---|---|---|---|---|
| 1 | major | Pat | No documented path from install to a working fit: the engine-location env var and override are buried in internal source | Add a short 'Engine setup' block to README Installation and the Getting-started vignette: how to obtain HSquared.jl and register it via Sys.setenv(HSQUARED_JULIA_PROJECT=...) or engine_control=list(julia_project=...). W… |
| 2 | major | Grace | Version is still 0.0.0.9000 while DESCRIPTION/README advertise 'Version 0.1' and there is no 0.1.0 NEWS section - not release-ready for v0.… | For a v0.1.x release, bump DESCRIPTION Version to 0.1.0, add a `# hsquared 0.1.0` NEWS heading (move the current dev bullets under it), and `devtools::document()` so the version propagates. The prose already commits to … |
| 3 | should | Boole | `engine = "validate"` and `model_spec()` misreport the Julia fit target/method for every fitted non-default model | Make the two inspector surfaces read the single computed truth. (a) In R/hsquared.R replace the hardcoded validate string with `spec$bridge$target` (it already carries the correct fn name and `method = :REML/:ML`). (b) … |
| 4 | should | Boole | `hs_stop_planned_marker()` error is stale and contradicts `formula_status()` and the live opt-in fit paths | Rewrite the message to (1) name the unsupported term, (2) state the currently fitted vocabulary accurately — pedigree `animal()` by default; opt-in `permanent()`/`common_env()`/`maternal_genetic()`/`genomic()`/`single_s… |
| 5 | should | Emmy | Release prep is being assembled on a dirty tree whose passing check predates the doc edits | Before cutting v0.1.x: commit the pending mission-control retirement, re-run `devtools::document()` + `pkgdown::check_pkgdown()` + `devtools::check()` on the committed tree, record the exact commands/outcome in check-lo… |
| 6 | should | Fisher | Boundary/identifiability flag silently dropped on genomic and single-step fits (relabeled VC component) | Make `hs_fit_boundary_flag()` robust to the relabeled primary component: identify the genetic component by position or a stable role tag rather than the literal string "animal" (e.g. pass the relationship label, or trea… |
| 7 | should | Rose | The headline `covered (v0.1)` default-fit claim is not exercised by any CI-runnable test — every engine-fit test is skip-guarded on local J… | This is defensible (the evidence is real and the numbers in the registers match `data-raw/dgp-recovery-study.R:44-68` exactly), but for a release the gap should be explicit, not implicit. Add one short sentence to the `… |
| 8 | should | Pat | Install-failure error names the requirement but gives no actionable next step (no link, no clone command, no env-var) | Extend this stop() (and the engine=julia bridge-unavailable stop() at R/julia-bridge.R:30-36) to name HSQUARED_JULIA_PROJECT / engine_control=list(julia_project=...) and link https://github.com/itchyshin/HSquared.jl, so… |
| 9 | should | Pat | Package version (0.0.0.9000) contradicts the repeated 'Version 0.1 fits ...' claim; no release tag exists | To finish toward v0.1.x: bump DESCRIPTION Version to 0.1.0 (or 0.1.0.9000 for ongoing dev), add a versioned NEWS heading, and tag the release — or, if intentionally pre-release, change the prose to 'the development vers… |
| 10 | should | Curie | Predicate item-3 recovery evidence (120-rep engine DGP) is not reproducible from the test suite; the CI-runnable recovery test uses a weake… | Add a CI-runnable (or at least documented-and-runnable) regression test that re-derives the predicate-form summary on the engine study output: assert the recorded per-component bias has 0 inside ±2·MCSE over the stated … |
| 11 | should | Grace | Working-tree slice (mission-control vignette retirement) is uncommitted and unverified on CI; head is green but in-flight state is not | Do not claim the current tree is green: the pushed head (3666363) is green, but the mission-control retirement is unpushed. Either commit+push the slice and watch R-CMD-check/pkgdown/Pages on the new head before any rel… |
| 12 | should | Grace | CI triggers on every push to main, violating the project/global 'local checks over CI, PR + workflow_dispatch only' policy | Trim R-CMD-check.yaml trigger to `on: [pull_request, workflow_dispatch]` (drop push), keep pkgdown deploy as-is or also move to workflow_dispatch + release. Optionally pin a release-time matrix (macos/windows) behind wo… |
| 13 | should | Grace | README/DESCRIPTION/NEWS attribute gryphon and DGP recovery to 'the engine'/'the fit', but the only CI-running validation is the pure-R refe… | Align the top-level prose with what runs by default. In README/DESCRIPTION/NEWS, say the gryphon/DGP recovery is verified by hsquared's independent pure-R REML reference on CI, and that the Julia engine reproduces the g… |
| 14 | should | Hopper | `engine = "julia"` with no target silently uses a different REML estimator (dense NelderMead) than the validated default `engine = "fit"` (… | Make the `engine = "julia"` default target `"ai_reml"` (or have `fit_animal_model` default forward to the AI-REML path), so the validated estimator is the default in both engines. If the dense path is intentionally reta… |
| 15 | should | Hopper | Undocumented user-reachable estimator: `target = "fit_animal_model"` (dense path) and its result are not mislabeled but are invisible in do… | Either document `target = "fit_animal_model"` in `hs_control()` (what optimizer it runs, and that it is the dense REML path, not AI-REML), or drop it from the accepted-target list and the default. Also add a provenance … |
| 16 | minor | Emmy | DESCRIPTION Version is still the dev string 0.0.0.9000, not a release version | Bump to `Version: 0.1.0` in DESCRIPTION and retitle the NEWS.md top section to `# hsquared 0.1.0` before tagging/release. Confirm the v0.1 contract (docs/design/01-v0.1-contract.md) and the validation_status() `covered`… |
| 17 | minor | Rose | DESCRIPTION Version is 0.0.0.9000 while README/NEWS/DESCRIPTION prose all say "Version 0.1 fits..."; no release tag exists | Pick one story before tagging v0.1.x. Either (a) bump DESCRIPTION `Version:` to `0.1.0` and add a matching `# hsquared 0.1.0` NEWS heading at release time, or (b) keep `0.0.0.9000` and reword the prose to "the developme… |
| 18 | nice | Fisher | Boundary flag does not detect a residual- or second-effect-variance boundary (h2 -> 1 or sigma_e2 -> 0) | If/when the opt-in multi-component paths are promoted past `partial`, generalize the boundary check to flag any variance component at/near zero (e.g. `any(est/total <= tol)`), not just the additive ratio. No action need… |
| 19 | nice | Fisher | Default-path heritability estimand, uncertainty scope, and inference blocks are correctly specified and enforced | No change required. This row documents that the inference dimension is otherwise clean so the single actionable item (the genomic/single-step boundary-flag gap above) is not lost among padding. The only display nit (sum… |
| 20 | nice | Pat | `engine = "validate"` aborts with stop() instead of returning the validated spec users expect to inspect | Either return the parsed spec/bridge payload invisibly (so it can be assigned and inspected), or reword the docs to say the validate path signals success via a message/condition and direct inspection users to model_spec… |
| 21 | nice | Curie | Validation-canon suite has no negative control / test-of-test: recovery tests never demonstrate they fail for a wrong estimator | Add one cheap test-of-test for the recovery band: compute the same h2/bias statistic from a perturbed/wrong estimate and `expect_false` it passes the band the real test uses. This proves the 0.06 (and the data-raw bias±… |
| 22 | nice | Curie | Previously-flagged twin integrity bug (V1-AI-REML unbacked '250-animal' evidence at validation_status.jl:97) is resolved on HSquared.jl mai… | Close out the integrity-flag rows on the coordination board (mark resolved with the twin commit that fixed the string) so the board does not carry a stale BLOCK against any v0.1 promotion that cites V1-AI-REML. No code … |
| 23 | nice | Hopper | Dead/duplicated default-fit code path: `hs_fit_julia_payload()` duplicates fit logic but is reachable only via the undocumented julia-defau… | After resolving the estimator-default decision, collapse the default univariate path onto a single function so there is one marshalling/merge block to keep in sync with `result_payload`. If `fit_animal_model` is dropped… |

## Twin-gated / needs HSquared.jl engine work — 2

| # | Severity | Lens | Finding | Recommendation |
|---|---|---|---|---|
| 1 | should | Emmy | CI never exercises any fit or validation test; the headline "validated" claim is local-only | Either (a) add a separate, possibly workflow_dispatch-triggered CI job that installs Julia (julia-actions/setup-julia), checks out HSquared.jl, sets `NOT_CRAN=true`, and installs the comparator Suggests, so the gryphon/… |
| 2 | should | Curie | The shipped default `engine = "fit"` path has zero numeric coverage under CRAN/CI; recovery transfer rests on skip-guarded equivalence tests | This is inherent to the bridge architecture and is honestly disclosed, so treat it as a documentation/CI-config item rather than a code change: (a) note explicitly in the test files or a testing README that production-p… |
