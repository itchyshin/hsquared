# Doc-site audit — pkgdown (hsquared) + Documenter (HSquared.jl)

**2026-07-11 · first doc-site audit · doc-only, NO build was run.**
Seven review lenses (Grace, Pat+Emmy, Rose, Grace+Karpinski, Rose+Hopper,
Jason/brain-best-practice, Florence/visual) read the rendered/source docs only.
`pkgdown::check_pkgdown()` was reported clean by multiple lenses; no Documenter
build was executed, so no `@example`/doctest was actually exercised. Both sites
are honest (no capability **overclaim** found on either) — every real defect is
either an **under-claim** (docs lag shipped opt-in features), a **positioning**
problem (developer working-board, not user-first docs), or a **demonstration**
gap. Verdict per honesty lenses: **CLEAN-WITH-LIMITATIONS, no hard block.**

Counts after de-duplication: **6 high · 12 medium · 12 low · 4 nice-to-have (34 total).**

---

## Prioritised findings (all lenses merged, de-duplicated)

Surface key: **PK** = hsquared pkgdown · **DOC** = HSquared.jl Documenter · **VIS** = visual layer.

| # | Sev | Surface | Area | Issue | Concrete fix | Evidence (lens) |
|---|-----|---------|------|-------|--------------|-----------------|
| H1 | high | PK | onboarding / runnable examples | Nothing on the landing/getting-started path is runnable. README, `hsquared.Rmd`, `fitting-models.Rmd`, gryphon are all `eval=FALSE` against undefined `dat`/`ped`; no `data/` ships; `hsquared()` & `model_spec()` Rd have empty `\examples`; extractor examples wrapped in `\dontshow{if(FALSE)}`. A package-only (no-Julia) user sees zero output even though `model_spec`/`formula_status`/`validation_status` run engine-free. | Lift the 4-animal toy ped + 3-row `dat` from `man/hs_data.Rd` into `hsquared.Rmd` with `eval=TRUE` engine-free output (`formula_status()`, `validation_status()`, `model_spec(...) |> summary()`); add a runnable `model_spec()` example to `man/model_spec.Rd`; consider shipping a `data()` toy pedigree. | `vignettes/hsquared.Rmd` L15-22,51-63; `man/hsquared.Rd`,`man/model_spec.Rd` empty `\examples`; no `data/`; `man/hs_data.Rd` L1-14 (Pat+Emmy H, Jason M) |
| H2 | high | PK | landing page invitingness | README (the pkgdown home) buries "how do I fit an animal model" under ~60 lines of validation-atom jargon; first runnable code fence at L69. Contradicts the project's own "Users are gold… easy, easy, easy" mantra. Over-qualification, **not** overclaim. | Restructure: (1) one sentence of what it does; (2) minimal copy-paste animal-model example in the first screen; (3) a 5-row works-now/experimental/planned summary; (4) push validation-atom prose (README L128-140) into the Validation-evidence article. | README.md home; first ```r at L69; dense block L8-58; L128-140 (Pat+Emmy H) |
| H3 | high | PK | article organization | All 17 articles dumped into one flat surface: `articles:` index has a single "Start here" title and the navbar dropdown is a flat 15-item list mixing getting-started with developer/validation notes and `*-comparator` dev notes. This is the exact anti-pattern brain-recorded as the gllvmTMB v1.0.0 gate. | Regroup `articles:` into semantic buckets mirroring drmTMB's 8 groups (Getting started → Worked examples → Using the models → Comparators & validation [Developer] → Roadmap); move `model-status`/`validation-evidence`/`*-comparator` out of the first-click path. | `_pkgdown.yml` L20-52,119-138; drmTMB `_pkgdown.yml` article groups (Jason H) |
| H4 | high | DOC | validation-status page | The honesty-critical Validation Status page contradicts its own source of truth: the live `@example` renders 55 rows, but the hand-maintained "Current Rows" table lists only 33 — dropping 5 rows the live example renders as `covered` (V3-NEFFECT-REML, V3-RR-REML, V4-DIRECT-MATERNAL, V6-ORDINAL, V6-GAMMA), so "covered" appears with its claim-boundary caveat nowhere visible. Two listed rows (V2-GREML, V5-MARKER-THRESHOLD) say `partial` but source says `covered`. | Auto-generate the table from `VALIDATION_STATUS_DATA` at build (`@eval`/`@example` iterating `validation_status()` → id\|capability\|phase\|status\|claim_boundary), or delete the static table. A hand-copied mirror of a 55-row source will always drift. | `docs/src/validation-status.md` L7-16 (live) vs L20-54 (33-row table), L41,L52; `src/validation_status.jl` = 55 rows (Rose+Hopper H+M) |
| H5 | high | DOC | reader-facing nav | "Mission control" — self-described internal engine dashboard (check log, tests, CI, status tables) — is the #2 nav item and the first "Start Here" bullet. Violates the brain reader-facing rule (internal bookkeeping never on a reader-facing surface). | Remove Mission control from top nav and the index.md Start-Here list; relocate to a Developer/Internals section at the end (or drop from the published site). Lead the reader path with "Get started". | `docs/make.jl:23`; `docs/src/index.md:144`; `docs/src/mission-control.md:1-6` (Jason H) |
| H6 | high | PK/VIS | honest-uncertainty display | `visualizing-models.Rmd` promises the low-h² imprecision flag on the G-correlation heatmap (L146-148) but the rendered figure never shows it: the illustrative `fit_mv` (L74-84) carries `genetic_correlation`/`genetic_covariance` but **no `heritability`**, so the flag path finds h²=NULL. The caption claims a safety feature the figure does not exhibit. | Attach per-trait `heritability` to `fit_mv` with ≥1 trait below the 0.1 default (e.g. horn=0.06) so the dagger flag fires on the low-h² off-diagonal cells and the imprecision caption renders. | `visualizing-models.Rmd` L146-148 vs L74-84; render: `ANY_DAGGER_FLAG: FALSE`; flag logic `R/autoplot.R:467-504` (Florence H) |
| M1 | medium | PK | navbar ↔ articles sync | **[4-lens consensus]** Navbar Articles dropdown omits two rendered articles that ARE in the `articles:` index: `visualizing-models` (the only autoplot how-to / visual-layer showcase) and `multi-effect-comparator` (comparator for a *covered* model). Hand-kept href list `check_pkgdown` does not validate → silent drift (15 menu vs 17 index). | Add both to the navbar `articles.menu`; also add a "Visualizing results" link to the gryphon example's See-also. Longer-term drop the hand-kept list and let pkgdown auto-populate from `articles:`. | `_pkgdown.yml` L22-52 vs L125,L135 (Grace M, Florence M, Pat+Emmy L, Jason L) |
| M2 | medium | PK | honesty — doc drift | `relmat()`/`precision()` are documented as **planned / "currently error as not implemented"** on 3 pages, but they are **shipped opt-in experimental** REML targets. Under-claim, but an internal contradiction with the man docs and `formula_status()` shown live on the same site. | Move both out of the planned/rejected lists into the opt-in experimental section on all 3 pages (REML-only, experimental, NOT covered, does not change the covered count), keyed to `capability-status.md` row 33. | `hsquared.Rmd:84-88`, `model-status.Rmd:55-57`, `inheritance-systems.Rmd:91-96` vs `R/hsquared.R:630`, `R/formula-status.R:153`, `test-relmat-precision.R` (Rose M) |
| M3 | medium | PK | honesty — doc drift | Articles say **no calibrated genome-wide GWAS exists / calibration remains planned**, but `gwas(fit, markers, method="single", genome_wide=TRUE)` ships a permutation-backed `genome_wide_p` column, documented in `man/gwas.Rd`. Reference and articles contradict each other on the same site. | Replace blanket "no permutation-backed cutoff / calibration remains planned" wording with the `man/gwas.Rd` framing (scoped experimental add-one permutation; fixed-effect/intercept-only scope; mixed/LOCO still planned). Keep the "don't report from nominal columns" caveat. Keyed to `capability-status.md` row 46. | `qtl-gwas-eqtl-status.Rmd:35,83-92`, `genomic-prediction.Rmd:248-255` vs `R/gwas.R:60-129,203-271`, `man/gwas.Rd:81-95` (Rose M) |
| M4 | medium | PK | reference index grouping | (1) "Start here" lists `genomic_markers` & `qg_effect_markers` — **inert/reserved formula markers** (rejected/planned) that don't belong under Start here. (2) "Extractor contract" is a 38-item wall mixing covered / experimental-opt-in / reserved with no per-item status signal. | Move the reserved markers into a labelled "Formula terms (several reserved/planned)" group; split "Extractor contract" into subsections (core h²/variance/EBV; uncertainty; multivariate & G; direct-maternal; random regression; markers/GWAS) each with a one-line status tag. | `_pkgdown.yml` L55-67 (Start here incl. L64-65), L69-109 (38 entries) (Pat+Emmy M) |
| M5 | medium | PK/DOC | cross-site maturity signalling | The two twin landings tell different maturity stories with no reconciling note: R "Version 0.1 **fits** the univariate Gaussian animal model by REML" vs Julia engine "**does not yet provide** production animal-model fitting". Also, the Julia-engine dependency (needs local Julia + from-source `HSquared.jl` + env var) is under-signposted relative to the R "fits" lead (spelled out ~220 lines down). | Put a single shared "what fits today (v0.1) vs experimental/opt-in vs planned" box on both landings, phrased identically, cross-linked; add a short callout distinguishing "works with just the R package" vs "needs the Julia engine". | README.md L8-10; `index.md:14-15`; `quickstart.md:2-3`; README engine-setup L224-271 (Jason M, Pat+Emmy M) |
| M6 | medium | PK | user-first / model choice | No task→model decision guide. `fitting-models.Rmd` is a catalog by model type; `model-status.Rmd` is a status list — neither maps "I have this data / this question → use this model → this exact call → what it returns and its limits". | Add a short "Choosing your model" guide (data situation → recommended model → exact call → returns/limits), placed high in the regrouped article set. Templates: drmTMB (choose-your-model), gllvmTMB (model guides). | `fitting-models.Rmd` headings L25-206; sister-pkg `_pkgdown.yml` groups (Jason M) |
| M7 | medium | PK/DOC | citation | No `inst/CITATION` in hsquared (→ no pkgdown Citation page / sidebar) and no `CITATION.bib` on the Julia site. Every sister package (drmTMB, gllvmTMB, orchaRd) and best-in-class quant-gen package (sommer, MCMCglmm, brms) ships one. | Add `inst/CITATION` to hsquared (pkgdown auto-renders a Citation page) and a "Citing HSquared.jl" section + `CITATION.bib` on the Julia site. | `ls inst/CITATION` → missing; sister-pkg `inst/CITATION` present (Jason M) |
| M8 | medium | DOC | API reference completeness | 10 **exported, docstring'd** public symbols are absent from the single `api.md` `@docs` block, so they never render on the Reference page: `bootstrap_variance_component_interval`, `variance_component_interval`, `genetic_correlation_interval`, `nested_lrt`, `structured_genetic_payload`, `nongaussian_heritability`, `gpu_fit_gblup`, `parse_payload_v2`, `fit_payload_v2`, `result_payload_v2`. | Add the 10 to the `api.md` `@docs` block (or, if any bridge-v2/gpu helper is deliberately non-public, drop it from `export` so the surface and Reference agree). | export list `src/HSquared.jl:8-215` vs `api.md:3-206`; docstrings confirmed present (Grace+Karpinski M) |
| M9 | medium | DOC | Documenter config health | `makedocs(warnonly = true)` globally downgrades **every** build error to a warning: missing-from-manual docstrings, broken `@ref`/`@docs`, failed `@example`/doctest. This is why M8 ships green and why any of the ~30 executed `@example` blocks could error without failing CI. | Scope the suppression (e.g. `warnonly = [:missing_docs]` only while api.md is completed); let `:cross_references` and `:doctest` fail. Remove `warnonly` entirely once M8 lands. | `docs/make.jl:14`; no checkdocs/doctest override; `quickstart.md` ~30 `@example` blocks (Grace+Karpinski M) |
| M10 | medium | DOC | phase-number consistency | Mission Control's Phase Board numbers phases differently from the rest of the site: genomic=Phase 5 / repeatability=Phase 2 / multivariate=Phase 3, but `roadmap.md` and the ledger phase column put genomic=Phase 2 / two-effect=Phase 3 / multivariate=Phase 4. A reader cross-referencing gets conflicting labels for the same capability. | Renumber the board to match `roadmap.md` + ledger: 2 genomic, 3 two-effect/repeatability/maternal, 4 multivariate G, 5 markers/QTL, 6 non-Gaussian/GLLVM. | `mission-control.md:224-228` vs `roadmap.md:155-160` + `src/validation_status.jl` phase counts (Rose+Hopper M) |
| M11 | medium | PK/VIS | figure glyph vs house contract | Default uncertainty display for the variance/heritability + recovery forests uses a **filled point + horizontal error bar** — both prohibited by default under the house Confidence Eye contract (hollow point-estimate circle). hsquared has its own ratified standard, but `24-plotting-standard.md` neither adopts nor documents departing from the eye glyph → silent cross-package inconsistency. | Make it a documented decision in `24-plotting-standard.md §5` (state and justify the divergence) OR align `hs_gg_forest()` to `shape=21, fill=NA`. Do not leave it silent. | `R/autoplot.R:113-123`; contract `~/.claude/memory/memory_summary.md:157-160` (Florence M) |
| M12 | medium | PK/VIS | figure provenance | Every figure in `visualizing-models.Rmd` is drawn from hand-fabricated `class="hsquared_fit"` objects with invented numbers, disclosed once in prose at the top (L43-45). A reader who deep-links to a section sees concrete numbers with no on-figure "illustrative" marker — a screenshot reads as a real result. | Add a lightweight per-figure signal (callout above each block, or an "illustrative data" tag in the subtitle/caption). At minimum repeat the disclosure as a short caption on the first figure of each section. | `visualizing-models.Rmd` L43-45, fabricated objects L55-113, figures L122-183 (Florence M) |
| L1 | low | PK | CI / release trigger | `pkgdown.yaml` deploys on `push:[main,master]` with no PR-time build-only job → a broken article/href/Suggests only surfaces post-merge. Inconsistent with `R-CMD-check.yaml` (PR + dispatch) and the local-first CI policy. | Add a lightweight PR-triggered `build_site()`-only job (no deploy) on ubuntu-latest, **or** record a local `build_site()` in `check-log.md` before each merge. Keep the push-to-main deploy as-is. | `.github/workflows/pkgdown.yaml:1-4` vs `R-CMD-check.yaml:1-4` (Grace L) |
| L2 | low | PK | home page badges | README (home) carries no status badges — no `lifecycle: experimental`, no R-CMD-check status. For a deliberately-experimental pre-1.0 pkg (D-41) an at-a-glance visual signal reinforces the prose hedging. | Add a badge block under the title: lifecycle-experimental (already vendored `man/figures/lifecycle-experimental.svg`) + an R-CMD-check workflow badge. | grep of README for badge markup → none (Grace L) |
| L3 | low | PK | reference alias surfacing | Alias surfacing is inconsistent: `direct_maternal_extractors` lists all 5 members as index rows, but other umbrella pages list only the umbrella — so `genetic_correlation()`, `evolvability()`, `g_max()` have no index row (still reachable via search). Discoverability polish, not breakage. | Pick one convention and apply to all grouped pages: either add key member names as index rows everywhere, or standardize on umbrella-only and drop the direct_maternal per-member rows. | `_pkgdown.yml` L93-98 vs L76,L112-114; NAMESPACE exports (Grace L) |
| L4 | low | PK | status discoverability | The clearest honest artifact — `docs/design/capability-status.md` (a scannable status table) — is **not published** on the site; the user-facing Model-status article is prose bullets with zero tables. Applied users adopt on exactly this scan. | Add a compact covered/experimental-opt-in/planned status table to the Model-status article (or a new Capability-status article) mirroring `capability-status.md`. | `model-status.Rmd` (0 table rows); `capability-status.md` not a vignette (Pat+Emmy L) |
| L5 | low | PK | honesty — mislabel | `hsquared.Rmd` L78-82 lists `common_env()` among terms "mirroring a `partial` validation gate", but the two-effect common-environment leg is **covered** at validation scale, not partial. Under-claim, inconsistent with every other page. | Split the sentence: list `common_env()` (two-effect leg) as covered-at-validation-scale; keep `permanent()`/`maternal_genetic()`/`genomic()`/`single_step()` as the partial-gate opt-in terms. | `hsquared.Rmd:78-82` vs `capability-status.md:29`, `model-status.Rmd:153-159` (Rose L) |
| L6 | low | PK | honesty — formatting | `genomic-prediction.Rmd` L60-63: a stray full stop leaves "A … marker scan is now available." as a standalone unqualified assertion, detaching the "experimentally / not genome-wide calibrated" qualifiers on the next fragment. | Join the two lines into one sentence and update the calibration wording per M3. | `genomic-prediction.Rmd:60-63` (Rose L) |
| L7 | low | PK/VIS | figures absent where expected | The gryphon worked example (first end-to-end read for an applied user) renders **zero** figures (`eval=FALSE`, uses base `plot()` not `autoplot()`), and `g-matrix-interpretation.Rmd` — entirely about interpreting G — shows no heatmap/scree though hsquared ships exactly those. | Render ≥1 figure in gryphon from a cached/illustrative fit and reference `autoplot()`; embed `autoplot(fit_mv,'g_matrix')` + `'g_geometry'` in `g-matrix-interpretation.Rmd`. | `gryphon-worked-example.Rmd:6,80-83`; `g-matrix-interpretation.Rmd` (no figure calls) (Florence L) |
| L8 | low | DOC | reference navigability | `api.md` is one flat `@docs` block of 200+ entries under a single H1 — no sections, no `@index`, no `@contents`. Unnavigable wall for a package this size. | Split into thematic `@docs` blocks under `##` headings (Pedigree & Ainv, Genomic, Likelihood/REML, Multivariate & G, Random regression, Non-Gaussian, Backends/GPU, Diagnostics, Extractors) + add `@index`. | `api.md`: 1 heading, 1 `@docs` block L3-206 (Grace+Karpinski L) |
| L9 | low | DOC | dashboard theming | `mission-control.md` `@raw html` hardcodes a light-mode palette (fixed hex on `--ink`/`--panel`/tags) with no `prefers-color-scheme`/VitePress tokens → forced light islands under the dark theme toggle. Visual-coherence, not content. | Swap hex for VitePress tokens (`var(--vp-c-bg-soft)`, `--vp-c-text-1`, `--vp-c-border`, `--vp-c-brand`) or wrap overrides in `@media (prefers-color-scheme: dark)`. (Flag to Florence if H5 keeps the page.) | `mission-control.md:10-21,60-65,100-103` (Grace+Karpinski L) |
| L10 | low | DOC | honesty — doc drift | `genomic-models.md` still frames genomic REML as "comparator-free" / "no external-comparator parity yet" (L8-10,104,116,130), but the ledger V2-GREML row now records an executed **blupf90+ 2.60** same-estimand AI-REML comparator (PR #200) and is flipped to covered. Under-claim. | Move genomic REML out of the "comparator-free" list, note the blupf90+ 2.60 leg; scope the header's "no parity yet" to G-construction/GBLUP/SNP-BLUP/single-step (still partial). | `genomic-models.md:8-10,104,116,130` vs `src/validation_status.jl:222-225` (Rose+Hopper L) |
| L11 | low | DOC | honesty — doc drift | `index.md` L103-104 lists GPU execution / CPU-GPU agreement tests under "Planned, but not implemented yet", but the ledger V2-GRM-GPU row records them RUN and PASSED (device-resident GBLUP CPU/GPU agreement ~1e-14 on tamIA H100 + Narval A100) via the weakdep CUDA ext. Under-claim. | Reword: an opt-in weakdep GPU extension exists and its CPU/GPU agreement + benchmark were validated off-CI, while `/src` stays GPU-free by default and no production-GPU claim is made (V2-GRM-GPU partial). | `index.md:103-104` vs `src/validation_status.jl:177-180` (Rose+Hopper L) |
| L12 | low | DOC | landing index drift | The hand-maintained "Start Here" list on `index.md` (10 links) omits user-relevant pages that ARE in the nav — Validation status, Genomic models, Standard QG models, Multivariate models, Fitting at scale, Changelog. | Reconcile the Start-Here list against `make.jl`'s page list (or replace with grouped links) and keep them in sync. | `index.md:142-153` vs `make.jl:26,28,29,30,33,37` (Jason L) |
| N1 | nice-to-have | PK | branding | No logo / favicon / opengraph social card / author-footer block (`man/figures` holds only lifecycle SVGs). Cosmetic, part of best-in-class polish and link-preview quality (gllvmTMB ships hex + opengraph). | Add `man/figures/logo.png`, run `pkgdown::build_favicons()`, add a `template: opengraph: image:` block — defer until structural fixes land. | `man/figures/` lifecycle SVGs only; no opengraph in `_pkgdown.yml` (Grace NTH, Jason NTH) |
| N2 | nice-to-have | PK | navbar redundancy | The Get-started vignette is linked twice — as the auto-added `intro`/"Get started" and again as "Getting started" in the Articles dropdown. | Optionally drop the "Getting started" dropdown entry (`_pkgdown.yml:23-24`) since `intro` already surfaces it. | `_pkgdown.yml:16,23-24` (Grace NTH) |
| N3 | nice-to-have | design-doc | covered-count wording nit | **User surface is honest** ("four opt-in models covered" matches the ledger; "stays 5" guard correctly excludes partial relmat/precision). Only a design-doc wording nit: `capability-status.md:30` / `register:22` call direct-maternal "the 5th opt-in `public_covered_count`", which reads as if 5 opt-in models are covered when only 4 are. | Optional: reword to "the 5th public_covered_count (4th opt-in)". No change on pkgdown pages. **RESOLVED 2026-09-02** (both surfaces reworded; `06-public-claims-register.md` now also enumerates the five so the number is checkable). | `capability-status.md:30`, `06-public-claims-register.md:22` (Rose NTH) |
| N4 | nice-to-have | DOC/VIS | engine site renders no figures | The Julia site renders no figures — but this is **honestly disclosed** (engine "does not draw figures"; Makie mirror is roadmap; mission-control cards are labelled counts, no misleading data bars). Informational only. | No honesty fix now. When the Makie ext lands, add one rendered example per figure kind so the cross-lane visual-parity aspiration becomes visible. | `docs/src` (no savefig/Makie output); roadmap disclaimers (Florence NTH) |

---

## Quick wins (≤ 1 hr each, low risk)

- **M1** — add the two missing navbar entries (`visualizing-models`, `multi-effect-comparator`) + a See-also link from gryphon. One-line YAML.
- **H6** — attach `heritability` to the illustrative `fit_mv` so the low-h² imprecision flag actually renders. One-line object edit.
- **M2 / M3 / L5 / L6** — the four under-claim doc-drift wording fixes (relmat/precision, GWAS calibration, common_env label, orphaned qualifier), all keyed to `capability-status.md` rows 33 & 46.
- **M8** — paste the 10 missing exported symbols into `api.md`.
- **M10 / L10 / L11 / L12** — Documenter wording/number reconciliations (phase board, genomic comparator, GPU line, Start-Here list).
- **L2** — add lifecycle + R-CMD-check badges to README.
- **N2** — drop the duplicate "Getting started" dropdown entry.

## Bigger projects (design + build)

- **H4 / M9** — auto-generate the Documenter validation-status table from `VALIDATION_STATUS_DATA`, then scope/remove `warnonly` so drift + example errors fail the build. (Do H4 via codegen so M10-style drift can't recur.)
- **H3 + M4** — regroup pkgdown articles into user-first semantic buckets (drmTMB shape) and re-section the reference index (reserved markers out of "Start here"; split the 38-item extractor wall).
- **H1** — add runnable engine-free examples + ship a `data()` toy pedigree; precompute one small fit (brms cached-vignette pattern) so ≥1 page shows real output.
- **H2 + M5** — restructure the README landing page and add a shared, cross-linked maturity box to both twin landings.
- **H5** — decide + execute the Mission-control demotion (Developer/Internals or unpublish).
- **M6** — write a "Choosing your model" decision guide.
- **M7** — add `inst/CITATION` + `CITATION.bib`.
- **M11 / M12 / L4 / L7** — resolve the Confidence-Eye glyph divergence (document or align), add illustrative-data markers, publish the capability-status table, render figures in gryphon + g-matrix-interpretation.
- **L8 / L9 / N1** — Documenter reference sectioning, dashboard dark-theme tokens, logo/opengraph polish.

## Best-practice gaps (what the best sites have that we lack)

- **Real fitted output on the getting-started page** — sommer/brms/MCMCglmm show a `summary()`/estimates/plot; we show code only.
- **Semantic article grouping + a "Choose your model" guide** — drmTMB (8 groups), gllvmTMB (model guides). Ours is a flat dump.
- **How-to-cite** (`inst/CITATION` / `CITATION.bib`) — every sister and best-in-class package ships one; we ship none.
- **A figure gallery / visual-layer showcase** — drmTMB foregrounds one; ours is one weakly-linked article.
- **Ships example data** so every article starts from a named dataset.
- **Reader-first surfaces** — no internal dashboards/register codes on the first-click path (the recorded gllvmTMB v1.0.0 gate).
- **Auto-generated status/reference tables** instead of hand-maintained mirrors that drift (H4 is the live example).
- Logo/hex + opengraph social card (gllvmTMB) — deferred polish.

## Brain-recorded standards to honor (validate edits against these)

- **gllvmTMB v1.0.0 pkgdown audit (2026-07-09):** user-first, not a dump of every draft; separate developer validation notes; consolidate topics. → drives H3, M4.
- **Reader-facing rule (2026-07-11):** reader-facing surfaces contain ONLY what makes sense to the reader; internal bookkeeping (register codes, dashboards) never appears. → drives H5.
- **Confidence Eye figure contract** (`memory_summary.md:157-160`): hollow point-estimate circle; filled points and horizontal CI bars prohibited by default. → drives M11.
- **Standard-Tier Covered-Flip Gate + `capability-status.md`/`validation_status()` as source of truth:** validate every wording edit against the ledger so a regrouping/positioning edit never reads as flipping an experimental target to covered. → gates M2, M3, L5, L10, L11 and any article rewrite.
- **Engine-covered ≠ R-public-covered discipline** (`capability-status.md`) and **D-41 experimental stance**: keep the two-lane split visible; keep lifecycle=experimental honest.
- **Local checks over CI** (global policy): record any `build_site()` / `check_pkgdown()` run in `docs/dev-log/check-log.md`. → relevant to L1.
- **Never read a repo-internal signal as external state:** a "release"/"covered" flag in-repo is not proof of an external comparator or deploy — relevant when re-wording comparator/GPU claims (L10, L11).

---

## Start here next session

Do the **quick-win batch first** — it is all wording/one-liners keyed to
`docs/design/capability-status.md` (rows 33 & 46) and `validation_status()`, no
code-path risk: M1 (navbar), M2/M3/L5/L6 (under-claim doc drift), H6 (render the
imprecision flag), M8 (10 missing api.md symbols), M10/L10/L11/L12 (Documenter
reconciliations). Then take the one true honesty-page defect —
**H4: auto-generate the `validation-status.md` table from the ledger** so it can
never drift again — and pair it with **M9** (scope `warnonly`). Validate every
wording edit against `capability-status.md` + `validation_status()` before
committing. Larger positioning work (H1/H2/H3/H5) is a separate slice.
