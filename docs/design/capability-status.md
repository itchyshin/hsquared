# Capability Status

| Capability | Status | Evidence | Notes |
| --- | --- | --- | --- |
| R package scaffold | covered | local and GitHub Actions R-CMD-check passed after Phase 0 edits | Package identity and operating memory exist. |
| Team operating system | covered | Phase 0 docs, skills, agents, board, closeout report, issues, milestones, labels | Use this before Phase 1 work. |
| `hs_control()` | covered | local tests | Default `engine = "fit"` fits the v0.1 univariate Gaussian animal model when Julia is available; `engine = "validate"` previews the contract; `engine = "julia"` exposes opt-in experimental targets. Backend vocabulary includes planned CPU threads, CUDA, AMDGPU, Metal, and oneAPI controls but does not execute those backends. |
| `backend_info()` | partial | local tests | Reports planned backend rows and marks execution unavailable for every backend. |
| `formula_status()` | partial | local tests | Reports parsed, reserved, and planned grammar rows, including the `hs_data()` pedigree shorthand and planned `cov = us()/diag()/lowrank()/fa()` structured-covariance rows; diagnostic only. |
| `validation_status()` | partial | local tests | Reports current validation atoms and planned comparator lanes; diagnostic only. |
| `data_status()` | partial | local tests over `hs_data()` diagnostics | Reports data-container component, ID-overlap, pedigree-status, expression-status, genotype-status, marker-status, annotation-feature, and environment-key diagnostics; no fitting. |
| `model_spec()` | partial | local tests inspect dimensions, fixed columns, sparse `Z`, normalized IDs, and Julia targets | Preview helper only; does not fit models. |
| `animal()` formula marker | partial | local parser tests | Inert syntax marker; accepts optional `pedigree` only as parsed formula context; not a standalone modelling helper. |
| Genomic/QTL formula markers | partial | local tests | `markers()`, `marker_scan()`, and `qtl_scan()` are inert markers the parser rejects as planned, not implemented. `genomic()` and `single_step()` are inert on the default path but fit through opt-in experimental targets (see the genomic GREML / single-step row). |
| Quantitative-genetic effect markers | partial | local tests | `permanent()`, `common_env()`, and `maternal_genetic()` are inert marker functions by themselves but fit only through the opt-in experimental targets described below. Paternal, maternal-environment, dominance/epistasis, cytoplasmic/imprinting, `relmat()`, and `precision()` remain syntax reservations rejected as planned, not implemented. |
| `hsquared()` fit entry point | covered (v0.1 contract) | the default `engine = "fit"` fits the v0.1 univariate Gaussian animal model by REML through the HSquared.jl engine; validated by known-truth recovery, the published gryphon anchor, and sommer agreement (see the recovery/gryphon atoms); requires local Julia + HSquared.jl | Fits the v0.1 contract (`y ~ fixed + animal(1 \| id, pedigree = ped)`, Gaussian, REML; `REML = FALSE`/ML is rejected on the fit path). Genomic, repeatability, two-effect, SNP-BLUP, and multivariate models are separate opt-in experimental targets, not the default. Non-Gaussian (`poisson`/`binomial`) models fit through the experimental opt-in `target = "nongaussian"` path (latent-scale, no heritability; mirrors twin `V6-LAPLACE`, partial) with the marginal selected by `engine_control$marginal`: `"laplace"` (Laplace approximation, default) or `"variational"` (variational/ELBO; aliases `"la"`/`"va"`), both engine-validated and live-verified element-wise. `binomial(logit)` accepts a binary 0/1 response (Bernoulli) **or** a `cbind(successes, failures)` counts response (Binomial); the engine uses one common trial count, so the `cbind` row totals must be equal (per-record varying trials are a planned engine follow-up). A `cbind(successes, failures)` binomial response is family-aware — it is never silently treated as a two-trait multivariate Gaussian. |
| R formula parser | partial | local tests parse `animal(1 \| id, pedigree = ped)` and `animal(1 \| id)` with an `hs_data()` pedigree bundle, and reject unsupported future syntax | Production bridge execution and general fitting remain planned. |
| R-to-Julia bridge payload | partial | local tests build `y` or multivariate `Y`, `X`, sparse `Z`, normalized pedigree parent indices, method, family, and Julia target metadata | Tiny live smoke exists separately; production bridge execution remains planned. |
| opt-in experimental Julia engine | partial | local JuliaCall tests against sibling `HSquared.jl` return variance components, EBVs, h², logLik, fitted values, dense validation-path PEV, and reliability for a tiny example | Requires `hs_control(engine = "julia")`; sparse `Z` CSC marshalling now used, production bridge still planned. |
| opt-in supplied-variance Henderson MME bridge | partial | local JuliaCall tests against sibling `HSquared.jl` compare the R validation fixture with Julia `henderson_mme()` through `hsquared()` and attach optional dense validation-path PEV/reliability when Julia exposes the methods | Requires `hs_control(engine = "julia", engine_control = list(target = "henderson_mme", variance_components = ...))`; supplied-variance validation path only, no optimizer, log-likelihood, or production sparse reliability. |
| tiny deterministic Ainv validation fixture | partial | local tests pin R payload ordering, sparse `Z`, and live Julia `pedigree_inverse()` agreement when `HSquared.jl` is available | Internal validation atom only; not Mrode or production fitting coverage. |
| Mrode9 pedigree Ainv comparator | partial | optional local tests compare Julia `pedigree_inverse()` with `nadiv::makeAinv()` for `nadiv::Mrode9` when `nadiv` and sibling `HSquared.jl` are available | Pedigree-Ainv comparator only; full Mrode animal-model outputs remain planned. |
| supplied-variance Henderson MME validation fixture | partial | local R reference solve and live Julia `henderson_mme()` comparison when sibling `HSquared.jl` is available | Internal validation atom only; variance-component optimization and production sparse fitting remain planned. |
| sparse REML likelihood identity | partial | optional live Julia tests compare dense REML and sparse REML likelihood paths at supplied variance components on a tiny three-founder fixture | Internal validation atom only; no sparse optimizer, AI-REML, fitted Mrode validation, or production sparse fitting. |
| Mrode-style supplied-variance output fixture | partial | local R reference calculations and optional live Julia tests pin a twelve-animal example for Ainv, fixed effects, EBVs, fitted values, PEV, reliability, h2, ML logLik, and dense/sparse REML logLik | Supplied-variance validation atom only; no variance-component estimation, ASReml parity, or general fitted Mrode output coverage. |
| experimental sparse REML estimator bridge | partial | opt-in `hs_control(engine = "julia", engine_control = list(target = "sparse_reml"))` surfaces the Julia-owned `HSquared.fit_sparse_reml()`; skip-guarded live tests check positive estimated variances, finite REML logLik, h2 in (0,1), that the optimizer reaches the same REML optimum from two different starts (start-independence), that it agrees with the dense REML optimizer (`fit_variance_components`), that it matches an independent pure-R REML optimizer, and that it is at least as good (by REML log-likelihood) as the external `pedigreemm` package on a replicated animal-model dataset | Experimental, opt-in, REML-only; the estimator is Julia-owned and R only surfaces it; gated on the twin's green `validation_status()`; not the default, not variance-component estimation via the public R interface, not production sparse fitting, AI-REML, or ASReml parity. |
| AI-REML estimator (default + explicit `target`) | covered (v0.1) | the default `engine = "fit"` path uses `HSquared.fit_ai_reml()` (average-information REML); `hs_control(engine = "julia", engine_control = list(target = "ai_reml"))` exposes the same estimator with explicit `initial`/`iterations`; skip-guarded live tests check positive estimated variances, finite REML logLik, h2 in (0,1), explicit `estimated_ai_reml` provenance, and agreement with the sparse REML optimizer (`fit_sparse_reml`); known-truth/external recovery in the rows below | This is the validated v0.1 estimator (twin `V1-AI-REML` covered). REML only (ML rejected); not production sparse diagnostics or ASReml multi-trait parity. |
| repeatability (permanent-environment) model | partial | opt-in `hs_control(engine = "julia", engine_control = list(target = "repeatability"))` surfaces the Julia-owned `HSquared.fit_repeatability_reml()` REML-only optimizer on `animal(1 \| id) + permanent(1 \| id)`; a skip-guarded live test on a repeated-records fixture checks three positive variance components (animal/permanent/residual), `repeatability()` and h2 in range, permanent-environment effects, and `estimated_repeatability_reml` provenance | Experimental, opt-in, REML-only; mirrors the twin `V3-REPEAT-REML` gate (partial). Not the default, not ML, not production or comparator-validated. σ²a and σ²pe are identifiable only with repeated records per individual. |
| two-effect models (common-environment, maternal) | partial | opt-in `hs_control(engine = "julia", engine_control = list(target = "two_effect"))` surfaces the Julia-owned `HSquared.fit_two_effect_reml()` REML-only optimizer on `animal(1 \| id) + common_env(1 \| group)` (IID environment) or `animal(1 \| id) + maternal_genetic(1 \| dam)` (pedigree maternal genetic effect); skip-guarded live tests check three positive variance components (animal/second/residual), h2 in range, the second-effect predictions, and `estimated_two_effect_reml` provenance | Experimental, opt-in, REML-only; mirrors the twin `V3-TWOEFFECT-REML` gate (partial). Two independent effects (additive + either IID common environment or pedigree maternal genetic); not the default, not ML, not production or comparator-validated; correlated direct–maternal (2×2 G) is planned. |
| genomic GREML / single-step models | partial | opt-in `hs_control(engine = "julia", engine_control = list(target = "genomic"))` on `genomic(1 \| id, Ginv = Ginv)`, or `target = "single_step"` on `single_step(1 \| id, Hinv = Hinv)`, surfaces `HSquared.fit_ai_reml()` on a user-supplied relationship inverse; skip-guarded live tests check two positive variance components (effect/residual), h2 in range, breeding values, and `estimated_<genomic\|single_step>_ai_reml` provenance | Experimental, opt-in, REML-only; mirrors the twin `V2-GREML` (genomic) and `V2-SSHINV` (single-step) gates (partial). Genomic accepts a supplied `Ginv` or a marker matrix; the R `single_step` bridge currently needs a supplied `Hinv`. Single-step H⁻¹ **construction** from pedigree + markers is now R-surfaced via `single_step(1 \| id, pedigree = ped, markers = M)` + `target = "single_step_construct"` (see the construction row below). Other construction, low-rank m≫n solves, and AGHmatrix/sommer/BLUPF90 comparator parity are planned. Not the default, not ML, not production or comparator-validated. |
| SNP-BLUP / RR-BLUP marker effects | partial | opt-in `hs_control(engine = "julia", engine_control = list(target = "snp_blup"[, variance_components = c(sigma_g2 = ..., sigma_e2 = ...)]))` on `genomic(1 \| id, markers = M)`; **with** supplied variances surfaces `HSquared.fit_snp_blup()`, **without** them surfaces `HSquared.fit_snp_blup_reml()` (REML-estimated σ²g/σ²e); local normalizer tests check descriptive marker contribution/proportion algebra; skip-guarded live tests check per-marker effects (`marker_effects()`, one per marker, labelled), per-individual genomic breeding values, supplied-variance h2 + `variance_components_source = "supplied"`, **and** the REML path (estimated σ²g/σ²e, `variance_components_source = "estimated_snp_blup_reml"`, parity with a direct `fit_snp_blup_reml`) | Experimental, opt-in; supplied-variance (the user supplies σ²g/σ²e) **or** REML-estimated when variances are omitted; `marker_variance_explained()` is a descriptive fitted-marker share, not a marker-scan p-value/QTL statistic/causal decomposition under LD; mirrors the twin `V2-SNPBLUP` gate (partial; GBLUP↔SNP-BLUP GEBV equivalence). Weighted/Bayesian priors, low-rank m≫n solves, and JWAS/sommer/BLUPF90 parity are planned. Not the default, not comparator-validated. |
| multivariate Gaussian animal model | partial | opt-in `hs_control(engine = "julia", engine_control = list(target = "multivariate"))` on `cbind(trait1, trait2) ~ animal(1 \| id, pedigree = ped)` surfaces `HSquared.fit_multivariate_reml()`; local tests check the cbind parser, NA-preserving `Y` payload, rank-deficient-X guard, named `initial = list(G0 = ..., R0 = ...)`, non-converged logLik/AIC fence, G/R covariance and correlation extractors, `G_matrix()` / `R_matrix()` aliases over those covariance fields, per-trait h2, cross-trait EBVs, skip-guarded live Julia bridge smoke, shared R/Julia fixture parity, an optional `sommer` diagonal-residual comparator for G0, diag(R0), and diagonal-target h2, the rotation-free `genetic_structure = "diagonal"` control (off-diagonal genetic covariances fixed at zero) fixture-verified against the twin `structured_covariance_parity` target, and `covariance_structure_lrt()` for the diagonal-vs-unstructured test (df, boundary = FALSE, χ²) | Experimental, opt-in, REML-only, animal-model-only, dense validation-scale; mirrors the twin `V4-MULTIVARIATE` / `V4-MV-REML` gates (partial). Missing trait cells are allowed. Not the default and not ASReml-style production multivariate fitting. Two reproducible R-lane studies now back this row but do **not** promote it (promotion is twin-gated): a 100-replicate **cold-start** known-truth recovery study (`data-raw/multivariate-recovery-study.R`; all six G0/R0 elements, rg, and both per-trait h2 within bias ± 2·MCSE, 100/100 converged, EBV accuracy 0.79/0.74 — no detectable bias) and a **full-unstructured-residual** `sommer` external comparator (`data-raw/multivariate-comparator-study.R`; G0/R0/β/h2/EBV agree with the engine's serialized `phase4_multitrait_parity` target to ≤ 8e-5, recovering the off-diagonal residual R0[2,1] that the in-suite diagonal-residual `sommer` check cannot). The Julia engine currently inverts Ainv internally, so deep-inbreeding/high-condition-number pedigrees remain a twin-side hardening item. |
| random-regression (reaction-norm) model | partial | opt-in `hs_control(engine = "julia", engine_control = list(target = "random_regression"))` on `weight ~ fixed + animal(rr(age, order = k) \| id, pedigree = ped)` surfaces `HSquared.fit_random_regression_reml()`; local tests check the `rr()` parser (spec/payload shape, default `order = 2`, unsupported-syntax rejection, opt-in fences, and the `random_regression` target validation), the result normalizer (`K_g`, residual variance, per-animal coefficients, `loglik`/`df`/`nobs`/`fixef`/`fit_diagnostics`), the R-side normalized-Legendre basis against the engine convention, the reaction-norm trajectory extractors (`rr_genetic_variance()`/`rr_heritability()` in [0,1]/`rr_correlation()` against `phi(t)' K_g phi(t)`), the non-RR extractor-rejection contract, and a skip-guarded live Julia bridge smoke (K_g k×k PD, residual > 0, q×k coefficients, h² trajectory in [0,1], converged) | Experimental, opt-in, REML-only, single-effect, univariate, dense validation-scale; mirrors the twin `#54` engine. `rr_eigenfunctions()` adds the rotation-invariant eigen-decomposition of `K_g` as covariate functions (live-verified `== HSquared.rr_eigenfunctions()` to ~1e-15). The `rr()` grammar is **ratified by the twin** on `HSquared.jl#61` (normalized Legendre on standardized t ∈ [-1,1]). The residual is HOMOGENEOUS and there is NO permanent-environment term (both planned), so `rr_heritability()` can OVERSTATE h²(t) for repeated-records designs. Basis convention fixed to normalized Legendre on standardized t ∈ [-1,1] (Kirkpatrick/Meyer/Schaeffer); `K_g` values are not comparable across normalization conventions. Not the default, not ML, not production or comparator-validated, not a known-truth recovery claim. Multivariate random regression and combining `rr()` with a second random effect are planned. The Julia engine inverts Ainv internally, so deep-inbreeding/high-condition-number pedigrees remain a twin-side hardening item. |
| external published-REML recovery (gryphon) | covered | skip-guarded tests (`enhancer` CRAN data + `sommer`): hsquared's pure-R REML reference recovers the published gryphon birth-weight REML variance components and h2 (Wilson et al. 2010) and agrees with `sommer` within the signed-off band; **and the Julia engine (`fit_sparse_reml` + `fit_ai_reml`) recovers the published estimates within the signed-off band via supplied `A_gryphon`** (engine-vs-pure-R agreement is to machine precision) | Maintainer-signed-off `V1-MRODE-FIT` anchor; backs the twin's `V1-MRODE-FIT` validation_status row (`covered_external`). The engine correctly rejects the pathological raw pedigree, so the anchor uses the published relationship matrix. Recovery is band-level (not bit-exact) against the published numbers. |
| known-truth DGP variance-component recovery | covered | ADEMP recovery study (`data-raw/dgp-recovery-study.R`): data simulated from known variance components over a clean pedigree; the engine (`ai_reml`) is near-unbiased (0 within bias ± 2·MCSE for σ²ₐ, σ²ₑ, h² over 120 reps, 100% converged), EBVs track true breeding values (accuracy ≈ 0.74), and the engine matches the independent pure-R reference to machine precision; recovery is near-unbiased across an h² grid (0.2/0.4/0.6) and the near-boundary cell (h²=0.1) shows mild upward bias + 94% convergence + 5% pinning; a skip-guarded pure-R regression test guards a small-N case | Statistical-correctness (recovery) evidence for the default `ai_reml` estimator, R-lane study via the read-only bridge; backs the twin-owned `V1-AI-REML` gate (`covered`). Studied across an h² grid plus a near-boundary cell and a fixed-effect model; no interval-coverage or production-robustness claim beyond the studied grid. |
| `hsquared_fit` object/extractors | partial | local tests over internal mock fit results and opt-in tiny Julia result | `EBV()`/`BLUP()` aliases, `G_matrix()`/`R_matrix()` aliases, `coef()`/`nobs()` S3 methods, PEV/reliability, derived accuracy, fitted values, residual extraction, and `fit_diagnostics()` exist for `hsquared_fit` objects with the needed fields; `fit_diagnostics()` and `summary()` surface an `at_boundary` flag when a variance component is at/near zero (the surfacing half of promotion-predicate item 4; engine boundary-stability is twin work); the opt-in bridge enriches tiny local Julia results from exported dense validation extractors when available; a set of **experimental**, asymptotic REML uncertainty surfaces — `heritability_interval()` and `variance_component_standard_errors()`/`heritability_standard_error()` (mirroring `V1-HERIT-CI`, partial), `repeatability_interval()` (`V3-REPEAT-REML`, partial), and multivariate `covariance_standard_errors()` (`V4-MV-REML`, partial) — are surfaced when the engine returns them, printed by `summary()`/`print()` and drawn by `plot(type = "variance")` (±1.96·SE whiskers), all clearly labelled experimental, REML-only, and not coverage-calibrated. |
| visualization / autoplot layer | partial | local `test-autoplot` (ggplot object/layer/`hsquared_meta` tests) + skip-guarded live engine parity (`test-plot-data-parity`) | `ggplot2` `autoplot()` figures: variance+h² forest, EBV caterpillar (PEV bands), rotation-invariant genetic-correlation heatmap with a **low-h² imprecision flag** (heuristic; default `low_h2 = 0.1`, off-diagonal cells only, degrades to no-flag when h² is absent/mismatched — not a calibrated precision statement), a rotation-invariant genetic-eigenstructure **scree** (`g_geometry`; eigenvalues + % variance explained, axis directions/loadings never drawn), reaction-norm trajectories, and an uncalibrated Manhattan + QQ (`autoplot(scan, "qq")` with a `lambda_GC` genomic-inflation diagnostic; nominal p-values, not genome-wide calibrated); plus `hs_recovery_forest()`, `theme_hsquared()`, and the machine-readable `hsquared_meta` honest-status attribute (tested for the built figures; the §3 `rotation_status` binding rule is pinned for the rotation-invariant members). The engine `*_plot_data` path is auto-detected and now bridge-attached at fit time where preparers are available: standard animal-model fits attach variance + EBV payloads, multivariate fits attach genetic-correlation + PCA payloads, and random-regression fits attach genetic-variance/eigenfunction/surface payloads on the default grid. The recompute fallback remains live for older/missing payloads and user-specified RR grids; skip-guarded live tests cover all seven preparers and attached fit payloads. Experimental/partial; not a calibrated-uncertainty claim. Base-graphics `plot()` remains the low-dependency fallback tier. |
| `hs_data()` container | partial | local tests over phenotype, pedigree, genotype-status, expression-status, annotation-feature diagnostics, environment-key diagnostics, pedigree status, marker-map validation, genotype-marker alignment, ID overlap summaries, marker-status summaries, and v0.1 parser use including default pedigree lookup for `animal(1 \| id)` | No file-backed storage, genotype parsing, marker imputation, marker scanning, eQTL/omics models, environmental model terms, or automatic genotype/omics/environment model construction yet. |
| simple Gaussian animal model | covered (v0.1) | fits by default via the HSquared.jl engine; validated by known-truth DGP recovery (near-unbiased, 120 reps), the published gryphon REML anchor (engine recovers it within the signed-off band), and sommer agreement | Univariate Gaussian, REML (ML rejected on the fit path), single record per animal. Requires the Julia engine. Boundary surfacing via `at_boundary`. |
| sparse Ainv | partial | Julia lane has tiny `pedigree_inverse()` tests; R live validation fixtures check the three-animal bridge path and optional Mrode9/nadiv comparator when available | R-side construction and production sparse fitting remain planned. |
| EBVs/BLUPs | partial | opt-in tiny Julia bridge returns breeding values | Experimental tiny/local only. |
| PEV/reliability | partial | R extractor contract tests over mocked `hsquared_fit` fields and opt-in tiny Julia bridge enrichment when available | Dense validation-path only; production sparse PEV/reliability and comparator validation remain planned. |
| marker/QTL/eQTL extractor names | partial | local tests over mocked `hsquared_fit` fields and default planned-not-implemented errors; SNP-BLUP normalizer tests check `marker_variance_explained()` algebra | `marker_effects()` and `marker_variance_explained()` are live for the opt-in SNP-BLUP path only. `gwas(fit, markers)` runs an experimental post-fit relatedness-corrected mixed-model Wald marker scan (reusing the fit's variance components; nominal/Bonferroni/BH p-values, **NOT genome-wide calibrated** — engine gate `HSquared.jl#48`; live-verified against the engine), with `method = "single"` for the relatedness-uncorrected single-marker (OLS) contrast and `method = "loco"` (with `marker_groups`) for a leave-one-group-out scan whose per-chromosome relationship correction is **genomic** (VanRaden) while the reused variance components are pedigree-estimated — a scale mismatch that keeps it validation-scale (live-verified against the engine element-wise). The tabular `gwas_table()`/`qtl_table()`/`eqtl_table()`/`lod_scores()` extractors stay reserved for the planned map-annotated scan API. |
| factor-analytic and structured multivariate G matrices | partial (diagonal) / planned (FA) | the opt-in `engine_control = list(genetic_structure = "diagonal")` control + `covariance_structure_lrt()` (see the multivariate row above) | Phase 3-4. Unstructured and rotation-free **diagonal** genetic covariance are surfaced (experimental/partial) through the opt-in multivariate **engine control**, with the diagonal-vs-unstructured LRT fixture-verified against the twin `structured_covariance_parity` target (live fit skip-guarded; mirrors twin `V4-BRIDGE`/`V4-MV-REML` partial). The `cov = us()/diag()/lowrank()/fa()` formula **grammar** is still not R-surfaced, and `lowrank`/`factor_analytic` structured fits remain gated on a validated rotation/interpretation convention for the loadings (twin #42; the convention — bridge only rotation-invariant functionals, never loadings — is ratified on #61). The **rotation-invariant** G geometry is surfaced now (experimental): `eigen_G()` (genetic eigenstructure), `g_max()`, `mean_evolvability()`, and directional `evolvability()`/`respondability()`/`conditional_evolvability()`/`autonomy()` (Hansen & Houle 2008), computed R-side from the fitted `G` and live-verified to match the engine `evolvability.jl` (twin `#55`). |
| factor-analytic G matrices | planned | none | Phase 4. |
| genomic/single-step construction beyond supplied inverses | partial (R, single-step H⁻¹) | opt-in `single_step(1 \| id, pedigree = ped, markers = M)` with `target = "single_step_construct"` surfaces `HSquared.fit_single_step_reml()`: the engine builds `Ainv` + dense `A` from the pedigree and `G` from the genotyped-subset markers, assembles `H⁻¹` (Aguilar et al. 2010), and fits by REML; skip-guarded live tests (`test-single-step-construct.R`) cover the marker-row **reorder invariance** (the genotyped_rows alignment guard), id-labelled GEBVs for **all** pedigree animals (genotyped + ungenotyped), a **differs-from-pedigree-model** anchor (genomic info tracks but changes the predictions), and a `ridge` rank-deficient-G case; pure-R tests pin the parser/alignment + error contracts | Experimental, opt-in, REML-only, dense/validation-scale; mirrors the twin `V2-SSHINV` (partial). The construction `tau/omega/blend_weight/ridge` knobs are not comparator-validated. When `data` is an `hs_data()` container bundling a pedigree and genotypes, `single_step(1 \| id)` resolves both from the bundle (the `animal(1 \| id)` precedent; explicit `pedigree =`/`markers =` override), live-verified to fit identically to the explicit call. APY, low-rank m≫n solves, and AGHmatrix/BLUPF90 comparator parity remain planned; promotion past `partial` is twin-gated. |
| GLLVM-style models | planned | none | Phase 6. |
| unusual inheritance | planned | none | Phase 7. |
| CPU/GPU backend execution | planned | none | `hs_control()` stores planned backend names only; Julia execution backends remain future work. |
