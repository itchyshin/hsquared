# Validation Debt Register

| Capability | Status | Evidence | Reviewer | Notes |
| --- | --- | --- | --- | --- |
| R package scaffold | covered | R-CMD-check green on initial scaffold | Grace/Rose | Phase 0 package exists. |
| `hs_control()` validation | partial | local tests planned in current slice | Emmy/Grace | Phase 0 control object only. |
| `hs_control()` backend vocabulary | partial | local tests for CPU threads, CUDA, AMDGPU, Metal, and oneAPI names | Lovelace/Karpinski/Rose | Control metadata only; no backend execution. |
| `backend_info()` status diagnostics | partial | local tests check requested backend flags and `execution_available = FALSE` | Lovelace/Karpinski/Rose | Diagnostic only; no runtime backend probe or benchmark. |
| `formula_status()` grammar diagnostics | partial | local tests check parsed, reserved, planned, `hs_data()` animal-model shorthand, `single_step(1 | id)` `hs_data()` pedigree+genotype bundle shorthand, and planned structured-covariance status rows | Boole/Pat/Rose | Diagnostic only; no formula expansion or fitting; `cov = us()/diag()/lowrank()/fa()` remains planned grammar. |
| `validation_status()` evidence diagnostics | partial | local tests check partial/planned validation rows and claim-boundary wording | Curie/Fisher/Rose/Pat | Diagnostic only; does not run validation checks or promote capabilities. |
| `data_status()` diagnostics | partial | local tests check component, ID-overlap, pedigree-status, expression-status, genotype-status, marker-status, annotation-status, and environment-status output for `hs_data()` objects | Emmy/Pat/Rose | Diagnostic only; no genotype parsing, pedigree inverse, marker scanning, eQTL/omics models, environmental model terms, or fitting. |
| Genomic/QTL formula markers | partial | local tests check inert marker return values, explicit genomic parser paths, opt-in `single_step()`, and planned-not-implemented scan/QTL vocabulary | Boole/Jason/Rose | Narrow univariate Gaussian REML `genomic()` remains an explicit experimental target; `single_step()` also remains explicit. `markers()`, `marker_scan()`, and `qtl_scan()` remain syntax reservations; no formula-level QTL/eQTL fitting. |
| fitted estimated-VC animal-model anchor | partial | consumes the twin's serialized Julia-native fitted target (`HSquared.jl#46`, `test/fixtures/animal_model_fitted_target/`): a julia-free internal-consistency test (h2 = sigma_a2/(sigma_a2+sigma_e2); PEV/reliability mutually consistent with non-negative inbreeding, i.e. the implied A_ii = (PEV/sigma_a2)/(1-reliability) are all >= 1 with founders == 1) plus a skip-guarded **live** test fitting the default REML animal model on the fixture inputs and reproducing the engine's serialized `sigma_a2`/`sigma_e2`/`h2`/`loglik`/`beta`/EBVs/reliabilities | Mrode/Curie/Fisher/Henderson | Estimated-VC (not supplied-variance) anchor on a 20-animal multi-generation pedigree; the engine fits its own serialized target and R reproduces it via the read-only bridge. Complements the existing supplied-variance Mrode anchors + the gryphon recovery (which already back the `V1-MRODE-FIT`/`V1-COMPARATORS` covered_external rows); this adds a fixture-verified estimated-VC reproduction. Not an external-comparator claim (the JWAS scaffold has no comparator evidence yet, twin `#49`). |
| post-fit `gwas()` marker scan | partial | shape-verified normalizer test (marker-scan table columns, `lod = chisq/(2log10)`, the uncalibrated-`print` caveat), the HSquared.jl PR #142 (`f9fbbb1`) `marker_scan_result_payload()` fixture consumed as a Julia-free R payload-normalization test, fit-type/markers guards, plus skip-guarded **live** tests asserting `gwas()` matches the engine mixed-model, single-marker, and LOCO scan paths element-wise | Jason/Fisher/Henderson/Rose | Experimental, dense/validation-scale; surfaces the Julia-owned marker-scan kernels reusing the fit's `(sigma_a2, sigma_e2)` + relationship context. The nominal `p_value`/`bonferroni_p`/`bh_qvalue` columns are marker-by-marker Wald + Bonferroni/BH over the supplied markers only — **NOT genome-wide calibrated**. **GENOME-WIDE CALIBRATION is now ACTIVATED** (2026-06-30) for `gwas(fit, markers, method = "single", genome_wide = TRUE)`: a `genome_wide_p` column from the Julia-owned `HSquared.genome_wide_marker_scan()` — the **exact per-dataset add-one permutation rule** (the null is rebuilt from the analysed phenotype each call; significant when `genome_wide_p <= 0.05`), whose family-wise type-I control is validated at validation AND production scale on HSquared.jl (the per-dataset add-one REBUILD gate, type-I 0.0504/0.0542 at alpha=0.05; the anti-conservative `(1-alpha)` quantile rule and the fixed-null-reuse shortcut are NOT used). The result carries a `calibration` attribute (`permutation_addone`; `empirical_type1 = NA` — validity by construction + externally validated via `validation_reference`; chi-square threshold on the LOD scale). SCOPE: **fixed-effect / intercept-only only** (`genome_wide = TRUE` requires `method = "single"`); the relatedness-corrected mixed-model / LOCO genome-wide null is a different, not-yet-validated calibration and is rejected. Live-verified element-wise against the engine; pure-R validator tests pin the NA-empirical_type1 + validation_reference contract. The ENGINE genome-wide calibration is now **COVERED (scoped, validation-scale, opt-in)** on HSquared.jl (`V5-MARKER-THRESHOLD`, 2026-06-30; doc-33 substitutable gate — validation/production type-I gates + PLINK comparator + this R activation, Rose PROMOTE); the **R public surface STAYS experimental/`partial`** per the conservative twin-discipline (engine-covered ≠ R-public-covered; the V4-MV-REML / Rose-risk-5 pattern) — fixed-effect-scoped, inheriting the engine fences (no mixed-model genome-wide null, no broader-LD/covariate-adjusted, no power/coverage, no map-annotated formula API). LOCO is available with `method = "loco"` and `marker_groups`, but the background relationship is genomic while reused variance components are pedigree-estimated; still validation-scale and uncalibrated. |
| Quantitative-genetic effect markers | partial | local tests check inert marker return values, default-path rejections, and opt-in parser paths | Boole/Darwin/Rose | `permanent()`, `common_env()`, and `maternal_genetic()` fit only through opt-in experimental targets. The `direct_maternal` target (`target="direct_maternal"` via `maternal_genetic()`) is **covered at validation scale** (48-seed gate PASS + `sommer covm()` comparator AGREE; see `V4-DIRECT-MATERNAL`); the INDEPENDENT two-effect maternal leg (`target="two_effect"`) stays experimental. Paternal, maternal-environment, dominance, epistasis, custom-kernel, cytoplasmic, and imprinting syntax remains planned-only. |
| metafounder / `H^Gamma` R bridge | partial | `metafounder(..., group =, Gamma =)` has pure-R parser/payload tests for group/Gamma validation; skip-guarded live tests call the supplied-variance `metafounder_animal_model()` path, pin `Gamma = 0` reduction to ordinary Henderson MME, and show nonzero-`Gamma` prediction sensitivity. `single_step(..., group =, Gamma =)` has pure-R parser/payload tests for group/Gamma validation, marker ordering, and `genotyped_rows`; skip-guarded live tests call `fit_metafounder_single_step_reml()`, pin `Gamma = 0` reduction to ordinary single-step construction, show nonzero-`Gamma` prediction sensitivity with stable labels/dimensions, and assert the standard fitted-object surface (`fit_diagnostics()`, PEV, `reliability()`, and derived `accuracy()`) on the nonzero-`Gamma` result. `gamma_matrix(fit)` and `metafounder_groups(fit)` return the supplied `Gamma` matrix and group assignments as provenance for both paths | Boole/Noether/Hopper/Henderson/Curie/Fisher/Jason/Rose | Still no returned metafounder-specific effects, no BLUPF90 comparator evidence, no `Gamma` estimation, and no covered-status promotion. Animal-only `metafounder()` is supplied-variance only. External validation/comparator evidence remains pending. |
| R formula parser | partial | local parser tests for explicit v0.1 syntax, `hs_data()` pedigree shorthand, and unsupported errors | Boole/Noether | General fitting remains planned. |
| R-to-Julia bridge payload | partial | local tests for `y`, `X`, sparse `Z`, normalized pedigree order, parent indices, method, family, and target metadata | Hopper/Lovelace | Production bridge execution remains planned. |
| opt-in experimental Julia engine | partial | local JuliaCall tests over tiny payload; skipped when Julia/JuliaCall/sibling `HSquared.jl` is unavailable | Hopper/Lovelace/Grace | Sparse `Z` CSC marshalling now used; standard PEV/reliability fields are consumed from `result_payload()` when present, with dense fallback only for older local engines. Not production bridge or general fitting. |
| opt-in supplied-variance Henderson MME bridge | partial | local JuliaCall tests compare fixture fixed effects, EBVs, fitted values, h2, and dense validation-path PEV/reliability through `hsquared()` with `target = "henderson_mme"` | Hopper/Fisher/Henderson/Rose | Supplied-variance validation path only; PEV/reliability are attached unconditionally on this dense validation path. No variance-component estimation, log-likelihood, production sparse reliability, or production sparse fitting. |
| tiny deterministic Ainv validation fixture | partial | local tests pin R payload ordering and live Julia `pedigree_inverse()` agreement for a three-animal Henderson-style fixture when available | Curie/Gauss/Henderson | First validation atom only; Mrode/comparator validation remains planned. |
| Mrode9 pedigree Ainv comparator | partial | optional local tests compare Julia `pedigree_inverse()` with `nadiv::makeAinv()` for `nadiv::Mrode9` when available | Curie/Gauss/Mrode | Pedigree-Ainv comparator only; fitted Mrode animal-model outputs remain planned. |
| supplied-variance Henderson MME fixture | partial | local R reference solve and optional live Julia `henderson_mme()` comparison for fixed effects, EBVs, fitted values, and h2 | Curie/Henderson/Fisher | Supplied-variance BLUP/MME only; not REML/ML variance-component estimation or production sparse fitting. |
| sparse REML likelihood identity | partial | optional live Julia tests compare dense `gaussian_loglik()` REML, sparse `sparse_reml_loglik()`, and ML hand-check targets on a tiny three-founder fixture | Curie/Gauss/Fisher | Supplied-variance likelihood identity only; not a sparse optimizer, AI-REML, fitted Mrode validation, or production sparse fitting. |
| Mrode-style supplied-variance output fixture | partial | local R reference calculations and optional live Julia tests for Ainv, fixed effects, EBVs, fitted values, PEV, reliability, h2, ML logLik, and dense/sparse REML logLik; the reference EBVs are now also pinned to the **published** Mrode Example 3.1 textbook digits (Mrode 2014, 3rd ed., p.39) by a CI-runnable R anchor (`test-mrode-published-anchor.R`) and by Julia PR #139 (`934a91e`), where the Julia-native anchor checks supplied `sigma_a2 = 20`, `sigma_e2 = 40`, published EBVs for animals 1-8, and the invariant male-minus-female sex contrast | Curie/Fisher/Mrode/Rose | Supplied-variance validation atom only; no variance-component estimation, ASReml parity, same-estimand comparator parity, sire-model implementation, or general fitted Mrode output coverage. The published anchor is supplied-variance BLUP (alpha = 2), not variance-component estimation. |
| experimental sparse REML estimator bridge | partial | pure-R control/validator tests plus skip-guarded live tests running `HSquared.fit_sparse_reml()` through the opt-in `target = "sparse_reml"` bridge; checks positive estimated variances, finite REML logLik, h2 in (0,1), a two-start estimate-recovery check (start-independence), sparse-vs-dense REML optimizer agreement, a match against an independent pure-R REML optimizer, and an at-least-as-good (REML logLik) check vs the external `pedigreemm` package on a replicated dataset | Hopper/Lovelace/Gauss/Fisher/Curie/Mrode/Jason/Rose | Experimental opt-in path only; the estimator is Julia-owned and R only surfaces it; gated on twin `validation_status()`; start-independence only, no DGP-recovery, external-comparator, AI-REML, production-sparse, or ASReml-parity claim. |
| experimental AI-REML estimator bridge | partial | pure-R control/validator tests plus skip-guarded live tests running `HSquared.fit_ai_reml()` through the opt-in `target = "ai_reml"` bridge; checks positive estimated variances, finite REML logLik, h2 in (0,1), explicit `estimated_ai_reml` provenance, and same-REML-optimum agreement with the sparse optimizer on the Mrode fixture | Hopper/Lovelace/Gauss/Fisher/Curie/Rose | Experimental opt-in path only; the average-information REML estimator is Julia-owned and R only surfaces it; gated on twin `validation_status()`; no DGP-recovery, external-comparator, production-sparse, or ASReml-parity claim. |
| multivariate REML estimator bridge | covered (validation-scale) | parser/validator tests plus skip-guarded live tests running `HSquared.fit_multivariate_reml()` through the opt-in `target = "multivariate"` bridge; checks `cbind()` grammar, NA-preserving `Y` payload, R NA to Julia NaN marshalling, named `initial = list(G0 = ..., R0 = ...)`, rank-deficient-X guard, G/R covariance and correlation extractors, per-trait h2, cross-trait EBVs, non-converged logLik/AIC fencing, a shared deterministic Phase 4 fixture consumed by the R test suite to pin payload ordering and normalized result/extractor shape against serialized Julia REML targets, an optional `sommer` diagonal-residual comparator for G0, diag(R0), and diagonal-target h2, the rotation-free `genetic_structure = "diagonal"` control fixture-verified against the twin `structured_covariance_parity` target (off-diagonal genetic covariances fixed at zero; live fit skip-guarded), the diagonal-vs-unstructured `covariance_structure_lrt()` checked end-to-end on the two shared fixtures, a CI-runnable Mrode Example 5.1 multiple-trait supplied-G0/R0 BLUP/MME anchor, and a data-raw `MCMCglmm` Bayesian agreement probe | Hopper/Noether/Kirkpatrick/Falconer/Curie/Jason/Rose/Mrode | Experimental path only; the dense REML estimator is Julia-owned and R only surfaces it. Since MV-4 a `cbind()` Gaussian response with an `animal()` term auto-routes here on the DEFAULT path (the explicit `target = "multivariate"` spelling still works); default routing is reachability; G10 promoted the R-public claim (`public_covered_count` 5→6). Mirrors the twin V4 covered gates (`V4-MV-REML`/`V4-BRIDGE`). The shared fixtures are internal parity targets. Four R-lane evidence legs now back this row: (i) a 100-replicate **cold-start** known-truth recovery study (`data-raw/multivariate-recovery-study.R`) in which all six G0/R0 elements, the genetic correlation, and both per-trait h2 are within bias ± 2·MCSE (100/100 converged, EBV accuracy 0.79/0.74) — no detectable bias (a low-power non-rejection rather than a proof of unbiasedness), agreeing with the twin's 12-seed study (#78/#79); (ii) a **full-unstructured-residual** `sommer` external comparator, now an **in-suite** test (MV-1, the classic `mmer` + `vsr(..., Gtc = unsm(2))` route; the `mmes` in-suite check is diagonal-residual only) reproducing the engine's serialized G0/R0/β/h2/EBV on the twin `phase4_multitrait_parity` target to ≤ 8e-5, recovering the off-diagonal residual R0[2,1] the diagonal-residual `sommer` check cannot reach; **A26b:** MV-1 uses `hs_require_suggests("sommer")` — missing Suggests under `NOT_CRAN=true` fails loudly; CRAN still skips via `skip_on_cran()`; covered numeric claim scoped to k=2 unstructured, with k≥3 and `diagonal` staying experimental; (iii) a pure-R published Mrode Example 5.1 supplied-covariance BLUP/MME target (`test-mrode-multivariate-anchor.R`) reproducing fixed effects and animal BLUPs; and (iv) a `MCMCglmm` Bayesian agreement probe (`data-raw/multivariate-mcmcglmm-agreement-study.R`) in which the serialized Julia target lies inside 95% HPD intervals for all 8 covariance elements, all 4 fixed effects, and both per-trait h2 values, with posterior-mean EBV correlations > 0.9997. The `diagonal` control + structure LRT are fixture-verified (R unpack) only — the live diagonal fit and an external diagonal-G comparator remain pending (twin #61/#47). The **2nd same-estimand REML comparator is now discharged**: an executed `blupf90+` 2.60 AI-REML leg (engine-side, 2026-06-29; `HSquared.jl` `2026-06-29-v4-blupf90-comparator.md`) converged from a non-degenerate neutral start to the same full-unstructured optimum on the `phase4_multitrait_parity` fixture (G0/R0 to the ~1e-5 stdout floor, β/EBV to ~1e-7, EBV corr 1.0), so three same-estimand REML programs (HSquared.jl, `sommer`, `blupf90+`) agree on this fixture (ONE fixture, point-estimate). R-public covered flip landed under G10 (2026-09-02). Twin engine broader-DGP **C8 confirm** is banked (DRAC job `47925486`; 16×500; 500/500 conv; **14/16 pass**; fails only `rg_090_rec1`/`rg_095_rec1`; `base_inside` clean; `docs/dev-log/recovery-checkpoints/2026-07-10-coverage-recovery-results.md`) and engine full-sib + 3-trait 48-seed gates are discharged (2026-06-30); R-lane **MV-5** is **SUPERSEDED (A25, 2026-09-02)** (driver + doc 40 historical/gate-frozen; NOT RUN; engine full-sib + 3-trait + C8 cite instead). The 2/16 C8 fails fence uniform single-record × extreme-rg claims but do not by themselves flip readiness. **A26 R↔engine element-wise parity is DISCHARGED LOCALLY (DP-10 C recorded-commit honesty)** (`test-multivariate-engine-parity.R`; tolerances predeclared before the run in `docs/dev-log/check-log.d/2026-09-02-h2-a26-mv-bridge-parity-predeclaration.md`, results in the sibling `…-a26-mv-bridge-parity.md`): fitting the `phase4_multitrait_parity` k=2 target **through the bridge** reproduces G0/R0/r_g/r_e/h2/β/EBVs/logLik element-wise to **1.7e-14** from the engine's own start (declared 1e-8) and to **2.0e-5** from the bridge's shipped identity start (declared 5e-4 VC / 5e-3 EBV), and the estimates are invariant to pedigree row permutation to 1.7e-14 (declared 1e-6), which pins the R-`Z`-order ↔ Julia-`Ainv`-order contract that previously held only by parallel implementation; four injected marshalling defects violate the tolerance by 3–13 orders, so the legs bite. Scope: point-estimate, ONE k=2 unstructured fixture, per-file run. **DP-10 C (0.6 honesty):** local A26 at the recorded commit above satisfies the parity conjunct of design-41 §3 #8 (`2516490`); a Julia-free `R-CMD-check` green must **not** be read as that measurement. Tier-1 stub stays `if: false` unless the owner later chooses **B**. Enabling B is *not* delivered by pushing. Row is **covered** (G10); count is **6**. The previously recorded whole-suite blocker — a pre-existing JuliaCall 0.17.6 assign-then-eval segfault in `test-multivariate.R` — was **fixed at `becfa5b`** (R `NA` is converted to `NaN` before `julia_assign` via `hs_y_matrix_for_julia()`); measured at tip, `test-multivariate.R` runs live 95 pass / 0 fail / 0 skip with no crash (verified per-file, not whole-suite). **NO-ANCHOR DISCLOSURE (Standard-Tier covered-flip gate item 2):** the Mrode Example 5.1 leg is a **supplied-`G0`/`R0`** BLUP/MME anchor and does **NOT** anchor the **estimated** `G0`/`R0` this row claims; **no published textbook anchor for estimated multivariate `G0`/`R0` exists**, so the estimated-covariance evidence is comparator- and recovery-based (`sommer` 4.4.5, `blupf90+` 2.60, cold-start recovery), not textbook-anchored, and any covered flip carries this disclosure rather than an implied Mrode anchor (precedent: `docs/design/43-genomic-greml-g0.md` §1). Darwin/Rose/G10 discharged for this flip; standing debt remains the deep-inbreeding boundary; `MCMCglmm` is Bayesian/MCMC agreement evidence, not same-estimand REML parity. No factor-analytic/structured `cov = ...` R grammar, and no production deep-pedigree conditioning claim. |
| external published-REML recovery (gryphon) | covered | skip-guarded tests: hsquared's pure-R reference recovers the published gryphon REML VC + h2 (Wilson et al. 2010) and agrees with `sommer` within the signed-off band; the Julia engine (`fit_sparse_reml` + `fit_ai_reml`) recovers the published estimates within the signed-off band via supplied `A_gryphon` (engine-vs-pure-R agreement is to machine precision); data from CRAN `enhancer` | Curie/Fisher/Jason | Maintainer-signed-off `V1-MRODE-FIT` anchor (2026-06-13); backs the twin's `V1-MRODE-FIT` row (`covered_external`). The engine correctly rejects the pathological raw pedigree, so the anchor uses the published relationship matrix. Recovery is band-level (not bit-exact) against the published numbers. |
| known-truth DGP variance-component recovery | covered | ADEMP recovery study (`data-raw/dgp-recovery-study.R`) + skip-guarded pure-R regression test: simulate from known VC over a clean pedigree, recover σ²ₐ/σ²ₑ/h² near-unbiased (0 within bias ± 2·MCSE, 120 reps, 100% converged), EBV accuracy ≈ 0.74, engine matches pure-R reference to machine precision; near-unbiased across an h² grid (0.2/0.4/0.6), near-boundary h²=0.1 shows mild upward bias + 94% convergence + 5% pinning | Curie/Fisher/Gauss | Recovery (statistical-correctness) evidence for the default `ai_reml` estimator; R-lane study via the read-only bridge; backs the twin-owned `V1-AI-REML` gate (`covered`). Studied across an h² grid (0.2/0.4/0.6) plus a near-boundary cell and a fixed-effect model; no interval-coverage or production-robustness claim beyond the studied grid. Thresholds are maintainer-signed-off proposals; maintainer may widen replicate count or add settings. |
| `hsquared_fit` extractor contract | partial | local tests over mocked result fields, EBV/BLUP aliases, derived accuracy, fitted/residual extraction, and opt-in tiny Julia result | Emmy/Pat | PEV/reliability contract exists; `fitted()` and `residuals()` work when normalized predictions and response values are present; `accuracy()` derives square-root reliability; the bridge consumes standard PEV/reliability payload fields when present and keeps dense fallback for older engines. |
| `hs_data()` input container | partial | local ID-map, pedigree-status summary, expression-status diagnostics, genotype-status diagnostics, marker-map validation, genotype-marker alignment, annotation-feature diagnostics, environment-key diagnostics, ID-overlap summary, marker-status summary, parser integration including default pedigree lookup, input-shape tests, and a skip-guarded live JuliaCall parity test that marshals phenotype, pedigree, and genotype data-frame components into `HSquared.HSData` | Emmy/Jason/Darwin/Hopper | No large-file, file-backed, PLINK/VCF, genotype parsing, imputation, pedigree inverse or relationship construction, eQTL/omics model, environment-effect model, marker-scan, or fitting tests yet. |
| `hsquared()` bridge-boundary error | partial | local tests | Rose/Pat | Must not imply fitting. |
| sparse Ainv tiny pedigree | partial | Julia lane tests exact tiny `pedigree_inverse()` values; R lane checks the three-animal bridge path and optional Mrode9/nadiv comparator when available | Curie/Gauss/Henderson | R-side construction and production sparse fitting remain planned. |
| univariate Gaussian animal model | covered (v0.1) | engine recovers known-truth DGP (near-unbiased, 120 reps, 100% conv), the published gryphon REML anchor within the signed-off band via supplied A, and agrees with sommer; twin `V1-MRODE-FIT`/`V1-COMPARATORS` covered_external, `V1-AI-REML` covered; default `hsquared()` fits by REML (ML rejected) | Fisher/Henderson | v0.1 pedigree fitting remains covered. The genomic route remains explicit and partial/experimental; `public_covered_count` is **7** (G10 added multivariate). Repeatability, SNP-BLUP, and single-step remain explicit partial targets; two-effect / multi-effect / RR / direct-maternal / multivariate are covered at validation scale; non-Gaussian remains an explicit latent-scale target with no heritability. |
| experimental non-Gaussian (GLMM) animal model bridge | partial | shape-verified normalizer test plus a skip-guarded live test fitting `poisson(log)` and `binomial(logit)` (binary + `cbind` counts) animal models through `HSquared.fit_laplace_reml()` via the opt-in `target = "nongaussian"` path; checks the latent-scale variance component, breeding values, marginal logLik/ELBO labelling, the no-heritability honesty gate, and the gaussian/unimplemented-family rejections; live legs verified against `HSquared.jl` `origin/main`; the R test suite also mirrors the HSquared.jl PR #152 `non_gaussian_parity` fixture and consumes the serialized Poisson-Laplace and Binomial-variational payload targets without Julia, including vector `n_trials` preservation | Hopper/Fisher/Darwin/Rose | Experimental opt-in path only; the marginal-REML estimator is Julia-owned and R only surfaces it; mirrors the twin `V6-LAPLACE`/`VA` partial gate. Latent/liability scale only — no heritability is reported (no residual-variance scale). REML-only; the marginal is selected by `engine_control$marginal` = `"laplace"` (default) or `"variational"` (aliases `"la"`/`"va"`), both engine-validated and live-verified element-wise — the VA objective is the ELBO (a lower bound), so VA and Laplace `logLik`/`AIC` are not comparable. `binomial(logit)` accepts a binary 0/1 (Bernoulli) response **or** a `cbind(successes, failures)` counts response whose per-record row totals **may vary**: the R bridge parses and carries them as a per-record trial vector (pure-R tested — parser/payload/family classification, full suite green; equal totals remain the live-verified common-trial special case). The live varying-trial engine round-trip is the remaining verification gate. Not coverage-calibrated, no external comparator; Bernoulli `sigma_a2` is prone to a search-bound boundary at small scale. |
| G-matrix geometry / evolvability extractors | partial | hand-computed-value correctness tests for `eigen_G`/`g_max`/`mean_evolvability`/`evolvability`/`respondability`/`conditional_evolvability`/`autonomy` on a known `G`, input/singular-`G`/non-multivariate guards, plus a skip-guarded **live parity** test verifying R == engine `evolvability.jl` (`evolvability`/`respondability`/`conditional_evolvability`/`autonomy`/`mean_evolvability`/`genetic_pca`/`g_max`) on a random PD `G` | Kirkpatrick/Falconer/Noether/Rose | Rotation-invariant functionals of the estimated `G` (Hansen & Houle 2008), computed R-side from `genetic_covariance(fit)`; the engine owns the canonical definitions and the live parity test guards drift. Descriptive geometry only — no standard errors, no calibration; inherits the parent multivariate fit's experimental label (t=2 unstructured G0/R0 is now R-public covered at validation scale; G-geometry extractors themselves stay partial — no standard errors, no calibration). The agreed cross-lane convention (bridge only rotation-invariant functionals, never loadings) is ratified on #61. |
| EBVs/BLUPs | partial | opt-in tiny Julia bridge returns breeding values; Mrode-style supplied-variance validation fixture pins EBVs against R and Julia references | Fisher/Pat | Needs estimated variance-component and comparator validation. |
| PEV/reliability | partial | R extractor contract tests over mocked fields; current univariate bridge routes consume standard engine payload fields; Henderson MME attaches dense validation-path fields unconditionally; Mrode-style supplied-variance fixture pins dense validation-path PEV/reliability | Fisher/Pat | Univariate/default and Henderson surfacing exists at validation scale. Needs multivariate per-trait PEV/reliability, production sparse strategy, and external comparator validation. |
| genomic activation / GBLUP / SNP-BLUP fixtures | covered (validation-scale; GREML opt-in) / partial (default activation, SNP-BLUP) | The v0.7 activation fixture freezes sample-p VanRaden1, `K_lambda = G + 0.01I`, `Q_lambda`, provenance fingerprints, repeated records, extra genotyped IDs, and one REML fit. Pure-R tests independently reconstruct the symbolic quantities; commit-pinned local live tests prove the explicit marker route = exact supplied-Q route for fitted quantities and the precision fingerprint. A fresh hash-pinned `blupf90+` 2.60 run consumes the exact frozen construction and agrees with the Julia REML point estimate to the external program's printout floor. The fail-closed boundary candidate matched an independent base-R oracle on all 240 sealed holdouts, corrected 30 boundary classifications with 0 losses, and produced no invalid or unresolved fits. The older PR #140 fixture separately pins the unregularized supplied-frequency GBLUP/SNP-BLUP supplied-variance identity. | Jason/Fisher/Kirkpatrick/Rose | The exact marker-Q comparator link and boundary-classification debt are discharged, but default activation remains unimplemented. The sealed gate was `BOUNDARY_HOLDOUT_FAIL`: one cell (`n120_m600_r050`) had a 5.99x candidate/default p95 runtime ratio against the frozen 3x cap. The 240 seeds are spent; they cannot adjudicate a revised implementation, and the nine-cell recovery campaign did not run. Next debt is component/allocation profiling on already-open discovery data, output-equivalent performance work, a new preregistration, and a fresh untouched holdout block. Only after that gate passes may end-to-end recovery resume. Older rrBLUP/BGLR evidence is agreement-level and does not establish ridge-regularized identity. No robustness evidence for LD, structure, imputation, base-frequency misspecification, real panels, or production data; no APY/low-rank/weighted/Bayesian/production claim. R genomic GREML is **covered** opt-in (0.7; count **7**); default activation + SNP-BLUP remain partial. Design-44 G5 still owed before default route. **NO-ANCHOR DISCLOSURE (Standard-Tier covered-flip gate item 2):** no clean published Mrode (or other textbook) genomic-\(h^2\) pin exists for estimated GREML on \(K_\lambda\). The covered-candidate evidence is comparator- and recovery-based (Totoro exact-`G` sommer/rrBLUP; engine V2-GREML 48-seed + `blupf90+`; marker≡Q identity; design-53 SUPERSEDE for §3 #1), never textbook-anchored. Scale disclosures in design-51 travel with `genomic_variance_ratio`. Precedent: multivariate Mrode 5.1 supplied-covariance no-anchor (G10) and design-43 §1 / design-51. |
| marker/QTL/eQTL extractor names | partial | local tests over mocked `hsquared_fit` result fields, default planned-not-implemented errors, SNP-BLUP marker-variance-share algebra, `gwas_table(scan)` / `lod_scores(scan)` over already-computed `hs_gwas`, live mixed/single/LOCO `gwas()` bridge parity when Julia is available, and Julia-free marker-scan payload normalization parity from the HSquared.jl PR #142 (`f9fbbb1`) fixture | Jason/Fisher/Pat/Rose | `marker_effects()` and `marker_variance_explained()` are live only for opt-in SNP-BLUP. `gwas(fit, markers)` is live but experimental, dense/validation-scale, and uncalibrated; `gwas_table(scan)` and `lod_scores(scan)` are thin views of the same already-computed scan, and the PR #142 fixture pins row-aligned payload shape only. Formula-level marker scanning, fit-level QTL/GWAS/eQTL workflows, calibrated thresholds, map joins, and external PLINK/GenABEL-style comparator evidence remain open. |
| Mrode simple animal example | planned | none | Mrode/Curie | Mrode9 pedigree-Ainv comparator exists; fitted model outputs and estimands remain planned. |
| multivariate G matrix | partial | opt-in multivariate REML bridge row above; HSquared.jl PR #144 (`023c675`) synced the twin structured-covariance status split; HSquared.jl #292 records engine FA S4 evidence without R activation | Noether/Kirkpatrick | Phase 3 unstructured G/R covariance plus the rotation-free diagonal-G control are surfaced experimentally; `cov = fa()` is a reserved R stub; low-rank loading exposure and long-format `cov = us()/diag()/lowrank()` grammar remain planned. |
| single-step H⁻¹ construction (`V2-SSHINV` twin) | partial | R opt-in `target = "single_step_construct"` plus live `test-single-step-construct.R`. Engine evidence (HSquared.jl #295): AGHmatrix::Hmatrix construction AGREE (`max\|Hinv Δ\| = 4.24e-12`; Martini/Legarra τ=ω=1) and predeclared n=240 recovery GATE PASS (48/48). | Boole/Hopper/Henderson/Curie/Fisher/Mrode/Jason/Darwin/Rose | **Not planned-only.** Status stays **partial / experimental**. R does not auto-route `single_step()` on `engine = "fit"`. Darwin UNSIGNED; no covered flip; `public_covered_count` stays **7**. Mrode Ch.11 NO-ANCHOR. Owed for any later flip: Darwin SIGN, R↔engine element-wise parity on the frozen cell, Rose CLEAN. BLUPF90/preGSf90 and APY remain planned debt. |
| factor-analytic G matrix | partial | HSquared.jl #292 Totoro S4 `d4-k1` 8/10 `ok_recovery` PASS; R reserved `cov = fa()` stub | Noether/Fisher/Kirkpatrick | Engine-experimental only. R does not fit FA. Not covered; `public_covered_count` stays 7. Owed: R bridge payload, WOMBAT/ASReml comparator, loadings withheld by rotation convention. |
