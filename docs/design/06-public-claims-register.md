# Public Claims Register

Use this register before changing README, DESCRIPTION, GitHub issue text, or
public examples.

| Claim | Status | Evidence | Allowed wording |
| --- | --- | --- | --- |
| `hsquared` is an R package scaffold | covered | package loads; local and GitHub Actions R-CMD-check passed after Phase 0 placeholder API | implemented scaffold |
| `hsquared` parses the first animal-model formula contract | partial | local tests for `animal(1 \| id, pedigree = ped)`, `animal(1 \| id)` with an `hs_data()` pedigree bundle, and unsupported-syntax errors | early parser for the v0.1 grammar; the default `hsquared()` then fits the parsed v0.1 Gaussian animal model |
| `hsquared` builds the first internal R-to-Julia payload shape | partial | local tests for `y`, `X`, sparse `Z`, normalized pedigree parent indices, method, family, and target metadata | internal bridge payload; no Julia execution |
| opt-in experimental R-to-Julia fit path | partial | local JuliaCall tests pass when a sibling `HSquared.jl` checkout is available; CI skips when unavailable | `hs_control(engine = "julia")` tiny local examples only |
| opt-in supplied-variance Henderson MME bridge | partial | local JuliaCall tests pass through `hsquared()` with `engine_control = list(target = "henderson_mme", variance_components = ...)` when a sibling `HSquared.jl` checkout is available; optional dense validation-path PEV/reliability is attached when Julia exposes applicable extractors | tiny supplied-variance validation target only; no optimizer, log-likelihood, production sparse reliability, production sparse fitting, or general animal-model support |
| sparse `Z` marshalling through the R-Julia bridge | partial | local tests pass `Matrix::dgCMatrix` CSC slots into Julia `sparse_csc_matrix()` for the opt-in bridge | sparse `Z` bridge validation; not production sparse fitting |
| tiny deterministic Ainv validation fixture | partial | local test pins R payload ordering and live Julia `pedigree_inverse()` agreement for a three-animal fixture when a sibling `HSquared.jl` checkout is available | internal tiny validation fixture; not Mrode, ASReml, or production fitting validation |
| Mrode9 pedigree Ainv comparator | partial | optional local test compares Julia `pedigree_inverse()` with `nadiv::makeAinv()` for `nadiv::Mrode9` when `nadiv` and sibling `HSquared.jl` are available | Mrode9 pedigree-Ainv comparator only; not a fitted Mrode animal-model validation |
| supplied-variance Henderson MME validation fixture | partial | local tests compare an independent R MME solve with Julia `henderson_mme()` for fixed effects, EBVs, fitted values, and h2 when a sibling `HSquared.jl` checkout is available | validation fixture only; no general fitted animal-model, optimizer, Mrode fit-output, or production sparse claim |
| sparse REML likelihood identity | partial | optional local tests compare Julia dense REML and sparse REML likelihood evaluators on a tiny supplied-variance three-founder fixture | validation fixture only; no sparse optimizer, AI-REML, fitted Mrode output validation, production sparse fitting, or general animal-model claim |
| Mrode-style supplied-variance output fixture | partial | local R reference calculations and optional live Julia tests pin a twelve-animal example for Ainv, fixed effects, EBVs, fitted values, PEV, reliability, h2, ML logLik, and dense/sparse REML logLik | supplied-variance validation fixture only; no variance-component estimation, ASReml parity, or general fitted Mrode output claim |
| experimental sparse REML estimator bridge | partial | opt-in `engine = "julia", target = "sparse_reml"` surfaces the Julia-owned `fit_sparse_reml()`; a skip-guarded live test checks positive estimated variances and finite REML logLik on a tiny fixture | experimental opt-in validation path only; the REML estimator is Julia-owned and R only surfaces it; not the default `hsquared()` behaviour, not variance-component estimation in the public R interface, not production sparse fitting, AI-REML, or ASReml parity |
| experimental repeatability (permanent-environment) model | partial | opt-in `engine = "julia", target = "repeatability"` surfaces the Julia-owned `fit_repeatability_reml()` on `animal(1 \| id) + permanent(1 \| id)`; a skip-guarded live test on a repeated-records fixture checks three positive variance components, `repeatability()`/h2 in range, permanent-environment effects, and `estimated_repeatability_reml` provenance | experimental, opt-in, REML-only; Julia-owned and R only surfaces it; mirrors the twin `V3-REPEAT-REML` gate (partial); not the default, not ML, not production or comparator/known-truth-validated; σ²a and σ²pe need repeated records per individual |
| experimental two-effect models (common-environment, maternal) | partial | opt-in `engine = "julia", target = "two_effect"` surfaces the Julia-owned `fit_two_effect_reml()` on `animal(1 \| id) + common_env(1 \| group)` (IID environment) or `animal(1 \| id) + maternal_genetic(1 \| dam)` (pedigree maternal effect); skip-guarded live tests check three positive variance components, h2 in range, second-effect predictions, and `estimated_two_effect_reml` provenance | experimental, opt-in, REML-only; Julia-owned and R only surfaces it; mirrors the twin `V3-TWOEFFECT-REML` gate (partial); two independent effects (additive + IID common environment or pedigree maternal genetic); not the default, not ML, not production or comparator/known-truth-validated; correlated direct–maternal (2×2 G) is planned |
| experimental genomic GREML / single-step models | partial | opt-in `engine = "julia", target = "genomic"` on `genomic(1 \| id, Ginv = Ginv)` (or `markers = M`) or `target = "single_step"` on `single_step(1 \| id, Hinv = Hinv)` surfaces `fit_ai_reml()` on a user-supplied relationship inverse; skip-guarded live tests check two positive variance components (effect/residual), h2 in range, breeding values, and `estimated_<genomic\|single_step>_ai_reml` provenance | experimental, opt-in, REML-only; Julia-owned and R only surfaces it; mirrors the twin `V2-GREML` / `V2-SSHINV` gates (partial); genomic accepts a supplied `Ginv` or a marker matrix; single-step needs a supplied `Hinv` (its construction, low-rank solves, and comparator parity planned); not the default, not ML, not production or comparator/known-truth-validated |
| experimental SNP-BLUP marker-effect model | partial | opt-in `engine = "julia", target = "snp_blup"` on `genomic(1 \| id, markers = M)` with supplied `variance_components = c(sigma_g2, sigma_e2)` surfaces `fit_snp_blup()`; a skip-guarded live test checks per-marker effects via `marker_effects()` (one per marker, finite, labelled by marker), per-individual genomic breeding values, supplied-variance h2, and `variance_components_source = "supplied"` | experimental, opt-in, supplied-variance (not estimation); Julia-owned and R only surfaces it; mirrors the twin `V2-SNPBLUP` gate (partial; GBLUP↔SNP-BLUP GEBV equivalence); the user supplies the variances (e.g. from a prior GREML fit); REML estimation of the marker variance, weighted/Bayesian priors, low-rank m≫n solves, and JWAS/sommer/BLUPF90 parity are planned; not the default, not comparator/known-truth-validated |
| experimental multivariate Gaussian animal model | partial | opt-in `engine = "julia", target = "multivariate"` on `cbind(trait1, trait2) ~ animal(1 \| id, pedigree = ped)` surfaces `fit_multivariate_reml()`; tests cover `cbind` parsing, NA-to-NaN missing-trait marshalling, named G0/R0 initial values, rank-deficient-X and non-converged-logLik fences, G/R covariance and correlation extractors, per-trait h2, cross-trait EBVs, skip-guarded live bridge smoke, shared R/Julia fixture parity, and an optional `sommer` diagonal-residual comparator for G0, diag(R0), and diagonal-target h2 | experimental, opt-in, REML-only, animal-model-only, dense validation-scale; Julia-owned and R only surfaces it; mirrors the twin V4 partial gates; not the default, not ASReml-style production multivariate fitting, not full external-comparator validated, and not a t>=2 known-truth recovery claim; the optional `sommer` comparator constrains residual covariance to diagonal and does not validate off-diagonal R0 |
| `hsquared` defines the first fitted-object/extractor contract | partial | local tests over internal `hsquared_fit` objects, mocked result fields, EBV/BLUP aliases, `coef()`/`nobs()` S3 methods, derived accuracy, fitted/residual extraction, `fit_diagnostics()`, and opt-in tiny Julia bridge enrichment for PEV/reliability when available | extractor plumbing, diagnostics, and tiny dense validation enrichment only; no production sparse PEV/reliability |
| `hsquared` provides a data container for integrated inputs | partial | local tests for `hs_data()` ID maps, pedigree status, expression-status diagnostics, genotype-status diagnostics, marker-map validation, genotype-marker alignment, annotation-feature diagnostics, environment-key diagnostics, ID overlap and marker-status summaries, input validation, and v0.1 parser use including default pedigree lookup | lightweight data container with phenotype/pedigree parser integration and pedigree/expression/genotype/marker/annotation/environment metadata diagnostics; no file-backed storage, genotype parsing, marker scanning, eQTL/omics models, environmental model terms, or automatic genotype/omics/environment model construction |
| `data_status()` reports data-container diagnostics | partial | local tests over `hs_data()` component, ID-overlap, pedigree-status, expression-status, genotype-status, marker-status, annotation-status, and environment-status diagnostics | status helper only; no model fitting, genotype parsing, marker scanning, eQTL/omics models, environmental model terms, pedigree inverse, or relationship-matrix construction |
| `hs_control()` stores planned backend names | partial | local control-validation tests for `threads`, `cuda`, `amdgpu`, `metal`, and `oneapi` | control vocabulary only; no CPU/GPU execution |
| `backend_info()` reports backend status | partial | local tests check planned backend rows and `execution_available = FALSE` | diagnostic table only; no backend execution or benchmarking |
| `formula_status()` reports grammar status | partial | local tests check parsed, reserved, planned, and `hs_data()` shorthand rows | diagnostic table only; reports which terms fit (the v0.1 `animal()` term) but the helper itself does not expand formulas or fit |
| `validation_status()` reports validation status | partial | local tests check current validation atoms, planned comparator lanes, and claim-boundary wording | diagnostic table only; reports `covered`/`partial`/`planned` atoms but does not itself run checks or fit models |
| `model_spec()` previews the parsed v0.1 model contract | partial | local tests inspect dimensions, fixed columns, sparse `Z`, normalized IDs, and Julia targets | model preview only; no fitting |
| Genomic/QTL formula markers exist | partial | local tests check inert markers and planned-not-implemented parser errors | syntax reservation on the default path; `genomic()`/`single_step()` fit only via the opt-in experimental targets (see the genomic GREML / single-step row); no marker-scan, QTL, or eQTL fitting |
| Standard quantitative-genetic formula markers exist | partial | local tests check inert markers and planned-not-implemented parser errors | syntax reservation only; no permanent, common-environment, maternal/paternal, dominance, epistasis, custom-kernel, cytoplasmic, or imprinting fitting |
| `hsquared()` fits the v0.1 univariate Gaussian animal model by default | covered (v0.1) | default `engine = "fit"` fits `y ~ fixed + animal(1 \| id, pedigree = ped)` by REML through `HSquared.jl`; validated by known-truth DGP recovery, the published gryphon anchor (within the signed-off band), and sommer agreement (see `capability-status.md`); requires local Julia + `HSquared.jl` | fits the v0.1 Gaussian animal model by REML by default; ML is rejected on the fit path; requires the Julia engine |
| `hsquared` fits general production multivariate/genomic/non-Gaussian animal models | planned | none | roadmap beyond the current opt-in experimental genomic, SNP-BLUP, single-step, and multivariate targets |
| `hsquared` supports QTL/eQTL, GLLVM, or GPU workflows | planned | none | roadmap only |
| `HSquared.jl` is the Julia engine package identity | covered | public repo exists; Julia package scaffold and CI green | Julia engine scaffold |
| sparse Ainv construction | partial | Julia tiny tests plus R live bridge fixtures cover a three-animal expected `Ainv` path and optional Mrode9/nadiv comparator | HSquared.jl validation only; R-side construction and production sparse fitting remain planned |
| Gaussian animal model REML estimation | covered (v0.1) | default `hsquared()` fit estimates the two variance components by average-information REML through `HSquared.jl`; validated by DGP recovery, the gryphon anchor, and sommer | v0.1 REML estimation of the univariate Gaussian animal model; ML is not implemented (rejected on the fit path) |
| EBVs/BLUPs and heritability extraction | partial | opt-in tiny Julia bridge tests plus internal extractor tests | experimental tiny/local only |
| PEV and reliability extraction | partial | R extractor contract tests over mocked fields; opt-in tiny Julia bridge enrichment uses exported dense validation extractors when available | dense validation-path only; not production sparse PEV/reliability |
| marker/QTL/eQTL extractor names | partial | local tests over mocked `hsquared_fit` fields and default planned-not-implemented errors; `marker_effects()` also returns the per-marker effects of an opt-in SNP-BLUP fit (skip-guarded live test) | `marker_effects()` is live for the opt-in SNP-BLUP path only; `marker_variance_explained()`, `qtl_table()`, `gwas_table()`, `eqtl_table()`, `lod_scores()` remain output vocabulary; no marker-scan, QTL, GWAS, or eQTL fitting |
| structured or factor-analytic multivariate G matrices | planned | none | roadmap; the unstructured multivariate animal model is only partial/experimental via `target = "multivariate"` |
| factor-analytic G matrices | planned | none | roadmap |
| production genomic and single-step construction | planned | none | roadmap; current support is opt-in experimental with supplied `Ginv`/`Hinv` or a marker matrix |
| GLLVM-style animal models | planned | none | roadmap |
| non-standard inheritance systems | planned | none | roadmap |
