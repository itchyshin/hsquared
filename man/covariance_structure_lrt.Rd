% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{covariance_structure_lrt}
\alias{covariance_structure_lrt}
\title{Likelihood-ratio test for genetic covariance structure}
\usage{
covariance_structure_lrt(constrained, full, ...)
}
\arguments{
\item{constrained, full}{Two \code{hsquared_fit} objects from the opt-in
multivariate target; \code{full} must nest \code{constrained} (more genetic
covariance parameters).}

\item{...}{Unused.}
}
\value{
A one-row data frame with \code{statistic}, \code{df}, \code{pvalue}, \code{boundary}, and
the \code{constrained} / \code{full} genetic-structure labels.
}
\description{
\code{covariance_structure_lrt(constrained, full)} is an \strong{experimental} nested
likelihood-ratio test comparing two opt-in multivariate fits \strong{on the same
data}: a \code{constrained} genetic structure (currently \code{genetic_structure = "diagonal"}) against the \code{full} \code{"unstructured"} fit. The statistic is
\code{2 * (logLik(full) - logLik(constrained))} on
\code{df = n_genetic_params(full) - n_genetic_params(constrained)}.
}
\details{
For diagonal-vs-unstructured the null (off-diagonal genetic covariances = 0)
is interior, so the χ² reference is exact (\code{boundary = FALSE}). Structures
whose null lies on a rank/PSD boundary (low-rank / factor-analytic) would need
a χ²-mixture correction and are gated out of the R bridge for now.

It mirrors the engine row \code{V4-MV-REML} (\code{partial}): asymptotic, REML-only,
dense validation-scale, with the multivariate recovery calibration not yet
passed — a reported test, not a validated one. Both fits must be on the same
response, fixed effects, and pedigree.
}
