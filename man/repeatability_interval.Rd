% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{repeatability_interval}
\alias{repeatability_interval}
\title{Extract an experimental repeatability confidence interval}
\usage{
repeatability_interval(object, ...)
}
\arguments{
\item{object}{A fitted model object.}

\item{...}{Reserved for future arguments.}
}
\value{
A one-row data frame with \code{estimate} (the repeatability \code{t}), \code{lower},
\code{upper}, \code{level}, and \code{se}, for \code{hsquared_fit} objects that contain it.
}
\description{
\code{repeatability_interval()} returns an \strong{experimental} large-sample (logit
delta-method) confidence interval for the repeatability coefficient
\code{t = (Va + Vpe) / Vp} of the opt-in repeatability (permanent-environment)
model, available only when the fit contains it.
}
\details{
It mirrors the engine row \code{V3-REPEAT-REML} (\code{partial}): the engine's
repeatability REML estimator and this interval are engine-internal
self-consistency tested (recovery of \code{t} and interval bracketing / range /
level-nesting / point-estimate match on seeded fixtures), but there is no
external comparator, no \verb{h²} interval, and no deep-pedigree validation. It is
asymptotic, REML-only, unreliable at small \code{n} or near the (0, 1) boundary
(where the engine throws and the field is omitted), and not a validated
capability.
}
