% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hs_data.R
\name{hs_data}
\alias{hs_data}
\title{Create an hsquared data container}
\usage{
hs_data(
  phenotypes,
  pedigree = NULL,
  genotypes = NULL,
  markers = NULL,
  expression = NULL,
  annotation = NULL,
  annotation_id = NULL,
  environment = NULL,
  environment_id = NULL,
  id = "id"
)
}
\arguments{
\item{phenotypes}{A data frame of phenotypic records.}

\item{pedigree}{Optional pedigree data frame.}

\item{genotypes}{Optional genotype matrix or data frame. Matrix row names or
data-frame ID values identify individuals. When \code{markers} is supplied,
genotype marker column names must match marker-map IDs exactly.}

\item{markers}{Optional marker map data frame. When supplied, it must contain
marker ID, chromosome, and position columns. Recognized aliases include
\code{marker}, \code{snp}, or \code{id}; \code{chromosome}, \code{chr}, or \code{chrom}; and
\code{position}, \code{pos}, \code{bp}, or \code{base_pair}.}

\item{expression}{Optional expression matrix or data frame.}

\item{annotation}{Optional annotation data frame.}

\item{annotation_id}{Optional column name used to match \code{annotation} rows to
expression feature columns. When supplied, the column must exist in
\code{annotation}.}

\item{environment}{Optional environment/covariate data frame.}

\item{environment_id}{Optional column name used to match \code{environment} rows
to phenotype records. When supplied, the column must exist in both
\code{phenotypes} and \code{environment}.}

\item{id}{Name of the individual ID column in \code{phenotypes}.}
}
\value{
An \code{hs_data} object.
}
\description{
\code{hs_data()} collects phenotype, pedigree, genotype, marker, expression,
annotation, and environment inputs into one checked container. It is a
lightweight data-contract object for future genomic, QTL/eQTL, and
multi-omics workflows. It does not fit models. The v0.1 parser can use an
\code{hs_data} object directly as \code{data}, reading model variables from
\code{phenotypes} and making named components such as \code{pedigree} available to
formula terms.
}
\details{
\code{summary(hs_data(...))} reports ID overlap diagnostics, pedigree diagnostics,
and, when expression, genotype, or marker components are supplied,
expression-feature, genotype-column, marker-map, and genotype-marker
alignment diagnostics. When \code{annotation_id} is supplied, it reports
expression-feature annotation coverage diagnostics. When \code{environment_id} is
supplied, it also reports environment metadata coverage diagnostics.
}
\examples{
ped <- data.frame(
  id = c("a", "b", "c", "d"),
  sire = c(NA, NA, "a", "a"),
  dam = c(NA, NA, "b", "c")
)
dat <- data.frame(
  y = c(1, 2, 3),
  sex = c("f", "m", "f"),
  id = c("a", "c", "d")
)
bundle <- hs_data(phenotypes = dat, pedigree = ped)
bundle
summary(bundle)
}
