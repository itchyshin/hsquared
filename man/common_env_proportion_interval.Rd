% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{common_env_proportion_interval}
\alias{common_env_proportion_interval}
\alias{maternal_proportion_interval}
\title{Extract an experimental common-environment / maternal variance-ratio interval}
\usage{
common_env_proportion_interval(object, ...)

maternal_proportion_interval(object, ...)
}
\arguments{
\item{object}{A fitted model object.}

\item{...}{Reserved for future arguments.}
}
\value{
A one-row data frame with \code{estimate} (the ratio), \code{lower}, \code{upper},
\code{level}, \code{se}, \code{lower_clamped}, \code{upper_clamped}, and \code{boundary}, plus an
\code{"interpretation"} attribute, for two-effect \code{hsquared_fit} objects that
contain it.
}
\description{
\code{common_env_proportion_interval()} and \code{maternal_proportion_interval()}
return an \strong{experimental} large-sample (logit delta-method) confidence
interval for the second variance ratio (\code{c2} / \code{m2}, \code{ratio2}) of the opt-in
two-effect model, available only when the fit contains it. On the same
two-effect fit, \code{\link[=heritability_interval]{heritability_interval()}} returns the matching interval for
the direct heritability (\code{h2}, \code{ratio1}).
}
\details{
This mirrors the engine row \code{V3-TWOEFFECT-REML}: the interval is the asymptotic
delta-method CI built from the two-effect REML observed information (the
finite-difference Hessian of the two-effect REML log-likelihood at the
optimum). It is \strong{asymptotic, delta-method, REML only, and NOT
coverage-calibrated} — on small samples the REML surface is flat and the
interval is unreliable (the parametric bootstrap is the only finite-sample-
aware path). No calibrated coverage is claimed.

Boundary honesty: when the ratio's variance component sits on the boundary
(\code{sigma -> 0}) it is flagged (\code{boundary = TRUE}) and \code{lower}/\code{upper} are
\code{NA}, never a spuriously tight CI. \code{lower_clamped}/\code{upper_clamped} flag when
a bound reaches the numerical \verb{(0, 1)} rails.

Falconer fence: \code{c2} / \code{m2} is a \strong{variance ratio}, not a heritability (see
\code{\link[=common_env_proportion]{common_env_proportion()}} / \code{\link[=maternal_proportion]{maternal_proportion()}}).
}
