% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{maternal_proportion}
\alias{maternal_proportion}
\title{Extract the maternal variance ratio (m2)}
\usage{
maternal_proportion(object, ...)
}
\arguments{
\item{object}{A fitted model object.}

\item{...}{Reserved for future arguments.}
}
\value{
A one-row data frame with \code{term} and \code{estimate}, plus an
\code{"interpretation"} attribute, for maternal two-effect \code{hsquared_fit}
objects.
}
\description{
\code{maternal_proportion()} returns the estimated maternal variance ratio
\code{m2 = sigma_m2 / (sigma_a2 + sigma_m2 + sigma_e2)} from the opt-in,
experimental maternal two-effect model
(\code{target = "two_effect"} with a \code{maternal_genetic()} term).
}
\details{
Falconer fence: \code{m2} is a \strong{variance ratio} (the proportion of phenotypic
variance from the maternal (dam) genetic effect), \strong{not a heritability}. In
the same fit, \code{\link[=heritability]{heritability()}} reports the narrow-sense \emph{direct}
\code{h2 = sigma_a2 / (sigma_a2 + sigma_m2 + sigma_e2)} \emph{within this two-effect
model}; the direct and maternal genetic effects are modelled as
uncorrelated here (no direct-maternal genetic covariance). The returned data
frame carries this note as an \code{"interpretation"} attribute. For an interval
on \code{m2}, see \code{\link[=maternal_proportion_interval]{maternal_proportion_interval()}}.
}
