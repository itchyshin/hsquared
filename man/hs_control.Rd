% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hs_control.R
\name{hs_control}
\alias{hs_control}
\title{Create hsquared control options}
\usage{
hs_control(
  engine = c("fit", "validate", "julia"),
  backend = c("auto", "cpu", "threads", "cuda", "amdgpu", "metal", "oneapi"),
  accelerator = c("auto", "none", "gpu", "cuda", "amdgpu", "metal", "oneapi"),
  precision = c("float64", "float32"),
  save = c("minimal", "full", "tiny"),
  engine_control = list()
)
}
\arguments{
\item{engine}{Execution engine. \code{"fit"} (default) fits the v0.1 Gaussian
animal model via the \code{HSquared.jl} engine; this requires a local Julia,
the \code{JuliaCall} package, and an \code{HSquared.jl} checkout. \code{"validate"} stops
after parser and bridge payload validation without fitting. \code{"julia"}
exposes the advanced opt-in bridge with explicit \code{target} control.}

\item{backend}{Planned compute backend. One of \code{"auto"}, \code{"cpu"},
\code{"threads"}, \code{"cuda"}, \code{"amdgpu"}, \code{"metal"}, or \code{"oneapi"}.}

\item{accelerator}{Planned accelerator preference. One of \code{"auto"},
\code{"none"}, \code{"gpu"}, \code{"cuda"}, \code{"amdgpu"}, \code{"metal"}, or \code{"oneapi"}.}

\item{precision}{Planned numeric precision. One of \code{"float64"} or
\code{"float32"}.}

\item{save}{Planned fitted-object storage mode. One of \code{"minimal"},
\code{"full"}, or \code{"tiny"}.}

\item{engine_control}{A named list for engine-specific controls. The current
experimental Julia bridge recognizes \code{julia_project}, \code{initial},
\code{iterations}, \code{target}, and \code{variance_components}.
\code{target = "henderson_mme"} is a supplied-variance validation path and
requires \code{variance_components} with named \code{sigma_a2} and \code{sigma_e2} values.
\code{target = "sparse_reml"} is an experimental, opt-in validation path that
surfaces the Julia-owned \code{HSquared.fit_sparse_reml()} REML-only sparse
optimizer; it accepts \code{initial} (named \code{sigma_a2}/\code{sigma_e2}) and
\code{iterations}. It is not the default, not production fitting, and not a
variance-component estimation claim for the public R interface.
\code{target = "ai_reml"} exposes the same average-information REML estimator
(\code{HSquared.fit_ai_reml()}) that the default \code{engine = "fit"} path uses,
with explicit \code{initial} and \code{iterations} control. This is the validated
v0.1 estimator for the univariate Gaussian animal model; the \code{engine = "fit"}
default is the ordinary way to reach it.
\code{target = "repeatability"} is an experimental, opt-in path for the
repeatability (permanent-environment) model. It requires
\code{animal(1 | id, pedigree = ped) + permanent(1 | id)} in the formula and
surfaces the Julia-owned \code{HSquared.fit_repeatability_reml()} REML-only
optimizer (three-component \code{initial} with \code{sigma_a2}/\code{sigma_pe2}/\code{sigma_e2}).
It is REML only, not the default, and the additive and permanent-environment
variances are identifiable only with repeated records per individual.
\code{target = "two_effect"} is an experimental, opt-in path for two-effect
models. It requires \code{animal(1 | id, pedigree = ped)} plus a second random
effect — \code{common_env(1 | group)} (an IID common-environment effect) or
\code{maternal_genetic(1 | dam)} (a maternal genetic effect carrying the pedigree
relationship) — and surfaces the Julia-owned
\code{HSquared.fit_two_effect_reml()} REML-only optimizer (three-component
\code{initial} with \code{sigma_a2}/\code{sigma_c2}/\code{sigma_e2}). It is REML only and not
the default.
\code{target = "genomic"} and \code{target = "single_step"} are experimental, opt-in
paths that fit a single effect whose relationship is a user-supplied inverse:
\code{genomic(1 | id, Ginv = Ginv)} (a genomic relationship inverse) or
\code{genomic(1 | id, markers = M)} (a marker matrix the engine turns into a
genomic relationship), and \code{single_step(1 | id, Hinv = Hinv)} (a single-step
relationship inverse). All surface \code{HSquared.fit_ai_reml()}. They are REML
only and not the default.
\code{target = "snp_blup"} is an experimental, opt-in, supplied-variance path for
the SNP-BLUP / RR-BLUP marker-effect model. It requires
\code{genomic(1 | id, markers = M)} (a raw marker matrix) and
\code{variance_components} with named \code{sigma_g2} (genomic) and \code{sigma_e2}
(residual) values, and surfaces the Julia-owned \code{HSquared.fit_snp_blup()}.
It estimates per-marker effects (\code{marker_effects()}) and per-individual
genomic breeding values at the supplied variances; it does not estimate the
variance components and is not the default.}
}
\value{
An object of class \code{"hs_control"}.
}
\description{
\code{hs_control()} records execution and storage controls for hsquared model
calls. The default \code{engine = "fit"} fits the validated v0.1 Gaussian
animal model through the \code{HSquared.jl} engine (average-information REML);
\code{engine = "validate"} parses and validates the contract without fitting; and
\code{engine = "julia"} exposes advanced engine controls.
}
