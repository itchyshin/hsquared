% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genomic-markers.R
\name{genomic_markers}
\alias{genomic_markers}
\alias{genomic}
\alias{single_step}
\alias{markers}
\alias{marker_scan}
\alias{qtl_scan}
\title{Genomic and QTL formula markers}
\usage{
genomic(formula, G = NULL, Ginv = NULL, ...)

single_step(formula, H = NULL, Hinv = NULL, ...)

markers(M, model = c("random", "fixed", "scan"), ...)

marker_scan(M, map = NULL, ...)

qtl_scan(position, genotype_probs = NULL, ...)
}
\arguments{
\item{formula}{A random-effect expression such as \code{1 | id}.}

\item{G, Ginv, H, Hinv}{Relationship or precision matrices for future genomic
and single-step models.}

\item{...}{Reserved for future syntax.}

\item{M}{A marker or dosage matrix for future marker-effect and scan models.}

\item{model}{Planned marker-effect mode.}

\item{map}{A marker map for future marker scans.}

\item{position}{A chromosome-position table or variable for future QTL
scans.}

\item{genotype_probs}{Genotype probabilities for future interval/QTL scans.}
}
\value{
\code{NULL}, invisibly. Calls are interpreted by \code{\link[=hsquared]{hsquared()}} when they
appear inside model formulas.
}
\description{
These functions provide readable formula vocabulary for genomic, single-step,
marker-effect, GWAS, and QTL/eQTL models. Called directly they are inert (they
return \code{NULL}); they take meaning only inside an \code{\link[=hsquared]{hsquared()}} formula.
\code{genomic()} and \code{single_step()} now fit through an opt-in, experimental
engine path (\code{engine = "julia"}, REML-only or supplied-variance, not the
default, mirroring a \code{partial} validation gate): \code{genomic(1 | id, Ginv = Ginv)}
or \code{genomic(1 | id, markers = M)} (GREML, or SNP-BLUP via
\code{target = "snp_blup"}) and \code{single_step(1 | id, Hinv = Hinv)}. The remaining
markers (\code{markers()}, \code{marker_scan()}, \code{qtl_scan()}) are still inert syntax
reservations that the parser rejects with a planned-not-implemented message.
}
