% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R
\name{hsquared-autoplot}
\alias{hsquared-autoplot}
\alias{autoplot.hsquared_fit}
\alias{autoplot.hs_gwas}
\title{ggplot2 visualizations for hsquared results}
\usage{
\method{autoplot}{hsquared_fit}(
  object,
  type = c("variance", "breeding_values", "g_matrix", "g_geometry", "reaction_norm",
    "rr_eigenfunctions", "rr_surface"),
  ...
)

\method{autoplot}{hs_gwas}(object, type = c("manhattan", "qq"), ...)
}
\arguments{
\item{object}{An \code{hsquared_fit} or \code{hs_gwas} object.}

\item{type}{Which figure to draw (see Details).}

\item{...}{Figure-specific options passed through: \code{low_h2} (the
genetic-correlation heatmap flags off-diagonal cells involving a trait with
\code{h^2 < low_h2} as imprecise; default \code{0.1}), \code{at}/\code{n} (the reaction-norm
covariate evaluation points), and \code{correlation} (\code{rr_surface}: draw the
genetic-correlation surface instead of the covariance surface).}
}
\value{
A \code{ggplot} object.
}
\description{
\code{autoplot()} methods render the quantitative-genetic results an
\code{hsquared_fit} (or a \code{gwas()} scan) carries as \code{ggplot2} objects, in the
style of the \code{brms}/\code{bayesplot} ecosystem and consistent with the sister
packages \code{drmTMB}/\code{gllvmTMB}. They are \strong{uncertainty-first}: where the fit
carries the experimental standard errors / reliabilities, the figures show
them (clearly labelled experimental and asymptotic).
}
\details{
Available \code{type}s for \code{autoplot.hsquared_fit()}:
\itemize{
\item \code{"variance"} (default) -- a horizontal forest of the variance components
and per-trait \code{h^2}, each with approximate 95\% intervals (\verb{+/- 1.96 * SE})
when the fit carries the experimental standard errors.
\item \code{"breeding_values"} -- a sorted caterpillar of the estimated breeding
values, with \verb{+/- 1.96 * sqrt(PEV)} bands when prediction error variances
are available, faceted by trait for multivariate fits.
\item \code{"g_matrix"} -- a \strong{rotation-invariant} genetic-correlation heatmap of the
estimated \code{G} for multivariate fits (correlations are invariant to the
factor rotation; raw loadings are never plotted -- the ratified cross-lane
convention). Off-diagonal cells involving a low-\code{h^2} trait are flagged as
imprecise (threshold \code{low_h2}, default \code{0.1}).
\item \code{"g_geometry"} -- a scree of the \strong{rotation-invariant} genetic
eigenstructure (eigenvalues = variance per genetic axis, with percent
variance explained) for multivariate fits. Axis directions / loadings are
never drawn (rotation-arbitrary; span-ambiguous under repeated eigenvalues).
\item \code{"reaction_norm"} -- for random-regression fits, the genetic-variance and
heritability trajectories across the covariate (faceted). The heritability
trajectory carries the same caveat as \code{\link[=rr_heritability]{rr_heritability()}}: with a
homogeneous residual and no permanent-environment term it can overstate
\code{h^2(t)} for repeated-records designs.
\item \code{"rr_eigenfunctions"} -- for random-regression fits, the rotation-invariant
eigenfunctions \code{psi_j(t)} of \code{K_g} as covariate functions (faceted by axis,
labelled by percent genetic variance). Signs are arbitrary and the curves are
span-ambiguous under repeated eigenvalues (do not over-read).
\item \code{"rr_surface"} -- for random-regression fits, the genetic covariance
surface \verb{S(s, t) = phi(s)' K_g phi(t)} over the covariate grid as a heatmap
(pass \code{correlation = TRUE} for the genetic-correlation surface). Supplied-\code{K_g}
descriptive.
}

\code{autoplot()} on a \code{gwas()} scan (\code{hs_gwas}) draws \code{type = "manhattan"}
(default) or \code{type = "qq"} (observed vs expected \code{-log10(p)} with a \code{y = x}
null and the genomic-inflation \code{lambda_GC} as a diagnostic). Both carry the
EXPERIMENTAL, NOT-genome-wide-calibrated caveat. The Julia lane has a
fixed-panel calibration smoke harness, but R significance thresholds are not
activated.

The figure helpers are deliberately modular (each takes a tidy data frame and
returns a \code{ggplot}) so they can be factored into a shared visualization
package later.
}
