% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qg-effects.R
\name{qg_effect_markers}
\alias{qg_effect_markers}
\alias{permanent}
\alias{common_env}
\alias{maternal_genetic}
\alias{maternal_env}
\alias{paternal_genetic}
\alias{paternal_env}
\alias{cytoplasmic}
\alias{imprinting}
\alias{dominance}
\alias{epistasis}
\alias{relmat}
\alias{precision}
\alias{group}
\alias{unknown_parent_group}
\alias{metafounder}
\alias{inbreeding}
\title{Quantitative-genetic formula markers}
\usage{
permanent(formula, ...)

common_env(formula, ...)

maternal_genetic(formula, pedigree = NULL, ...)

maternal_env(formula, ...)

paternal_genetic(formula, pedigree = NULL, ...)

paternal_env(formula, ...)

cytoplasmic(formula, ...)

imprinting(formula, pedigree = NULL, parent = c("maternal", "paternal"), ...)

dominance(formula, pedigree = NULL, D = NULL, Dinv = NULL, ...)

epistasis(formula, pedigree = NULL, E = NULL, Einv = NULL, ...)

relmat(formula, K = NULL, Kinv = NULL, Q = NULL, ...)

precision(formula, Q = NULL, ...)

group(formula, ...)

unknown_parent_group(formula, ...)

metafounder(formula, pedigree = NULL, ...)

inbreeding(formula, ...)
}
\arguments{
\item{formula}{A random-effect expression such as \code{1 | id}.}

\item{...}{Reserved for future syntax.}

\item{pedigree}{A pedigree data frame for future parental and relationship
effects.}

\item{parent}{Planned parent-of-origin side for imprinting effects.}

\item{D, Dinv}{Dominance relationship or precision matrices.}

\item{E, Einv}{Epistatic relationship or precision matrices.}

\item{K, Kinv, Q}{User-supplied relationship or precision matrices.}
}
\value{
\code{NULL}, invisibly. Calls are interpreted by \code{\link[=hsquared]{hsquared()}} when they
appear inside model formulas.
}
\description{
These functions provide readable formula vocabulary for standard
quantitative-genetic, parental, inheritance, and custom-kernel models. Called
directly they are inert (they return \code{NULL}); they take meaning only inside an
\code{\link[=hsquared]{hsquared()}} formula. \code{permanent()}, \code{common_env()}, and \code{maternal_genetic()}
now fit through an opt-in, experimental engine path (\code{engine = "julia"},
REML-only, not the default, mirroring a \code{partial} validation gate) as the
second random effect alongside \code{animal()}. The remaining markers
(paternal/maternal-environment, dominance, epistasis, cytoplasmic,
imprinting, custom relationship/precision, genetic groups /
unknown-parent-groups, metafounders, and inbreeding) are still inert syntax
reservations that the parser rejects with a planned-not-implemented message.
}
\details{
Some markers use generic names (e.g. \code{group()}, \code{inbreeding()}). They are
formula-only tokens detected by call head and are not meant to be called
directly, so attaching \code{hsquared} alongside a package that exports a
same-named function (e.g. \code{pedigreemm::inbreeding()}) may print a masking
message; this is expected and harmless.
}
