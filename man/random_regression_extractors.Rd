% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{random_regression_extractors}
\alias{random_regression_extractors}
\alias{rr_covariance}
\alias{random_coefficients}
\alias{rr_genetic_variance}
\alias{rr_heritability}
\alias{rr_correlation}
\alias{rr_eigenfunctions}
\title{Random-regression (reaction-norm) extractors}
\usage{
rr_covariance(object, ...)

random_coefficients(object, ...)

rr_genetic_variance(object, at = NULL, n = 25L, ...)

rr_heritability(object, at = NULL, n = 25L, ...)

rr_correlation(object, at = NULL, n = 25L, ...)

rr_eigenfunctions(object, at = NULL, n = 25L, ...)
}
\arguments{
\item{object}{A random-regression \code{hsquared_fit} object.}

\item{...}{Reserved for future arguments.}

\item{at}{Covariate values on the original scale at which to evaluate the
trajectory. \code{NULL} (the default) uses an evenly spaced grid over the fitted
covariate range.}

\item{n}{Number of grid points used when \code{at = NULL}.}
}
\value{
\code{rr_covariance()} returns a numeric matrix; \code{random_coefficients()}
returns a data frame; the trajectory extractors return a data frame with a
\code{covariate} column and the evaluated \code{value}s.
}
\description{
These extractors summarize an opt-in, \strong{experimental} random-regression
(reaction-norm) fit (\code{target = "random_regression"}), fitted with an
\code{animal(rr(covariate, order = k) | id, pedigree = ped)} term. The model
estimates a \verb{k x k} genetic covariance matrix \code{K_g} among an animal's
normalized-Legendre random-regression coefficients plus a single homogeneous
residual variance.
}
\details{
\itemize{
\item \code{rr_covariance()} returns the estimated \verb{k x k} coefficient genetic
covariance matrix \code{K_g}.
\item \code{random_coefficients()} returns the per-animal predicted Legendre
coefficients (long format: \code{id}, \code{coefficient}, \code{value}).
\item \code{rr_genetic_variance()} returns the additive genetic variance trajectory
\verb{v_g(t) = phi(t)' K_g phi(t)} across covariate points.
\item \code{rr_heritability()} returns the heritability trajectory
\code{h^2(t) = v_g(t) / (v_g(t) + sigma_e^2)}. Because the residual is
homogeneous and there is no permanent-environment term yet, this can
OVERSTATE \code{h^2(t)} for repeated-records designs (test-day, growth curves).
\item \code{rr_correlation()} returns the genetic correlation surface among the
covariate points.
\item \code{rr_eigenfunctions()} returns the eigen-decomposition of the coefficient
covariance \code{K_g} evaluated as functions of the covariate: the eigenvalues
(additive genetic variance carried by each axis), the proportion of genetic
variance explained, the sign-canonicalized eigen-coefficients, and the
eigenfunctions \verb{psi_j(t) = phi(t)' v_j} over the covariate grid. These are
\strong{rotation-invariant} functionals of \code{K_g} (mirroring the engine's
\code{rr_eigenfunctions}); no raw, rotation-arbitrary loadings are returned.
}

The trajectories are computed in R from the estimated \code{K_g} and the recorded
covariate standardization range; \code{at} is supplied on the ORIGINAL covariate
scale (defaulting to a grid over the fitted range) and re-standardized to
\verb{[-1, 1]} internally, matching the Julia engine's basis convention.
}
\examples{
\dontshow{if (FALSE) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
fit_rr <- hsquared(
  weight ~ sex + animal(rr(age, order = 2) | id, pedigree = ped),
  data = long_records,
  family = gaussian(),
  REML = TRUE,
  control = hs_control(
    engine = "julia",
    engine_control = list(target = "random_regression")
  )
)

rr_covariance(fit_rr)
random_coefficients(fit_rr)
rr_genetic_variance(fit_rr)
rr_heritability(fit_rr)
rr_correlation(fit_rr, at = c(1, 3, 5))
rr_eigenfunctions(fit_rr)
\dontshow{\}) # examplesIf}
}
