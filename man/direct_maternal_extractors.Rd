% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{direct_maternal_extractors}
\alias{direct_maternal_extractors}
\alias{direct_heritability}
\alias{direct_variance}
\alias{partner_variance}
\alias{direct_maternal_covariance}
\alias{total_heritability}
\title{Direct-maternal correlated model extractors}
\usage{
direct_heritability(object, ...)

direct_variance(object, ...)

partner_variance(object, ...)

direct_maternal_covariance(object, ...)

total_heritability(object, ...)
}
\arguments{
\item{object}{An \code{hsquared_fit} from \code{target = "direct_maternal"}.}

\item{...}{Not used.}
}
\value{
\itemize{
\item \code{direct_heritability()}: a one-row data frame with \code{term} and \code{estimate},
plus an \code{"interpretation"} attribute.
\item \code{total_heritability()}: a one-row data frame with \code{term = "total_willham"}
and \code{estimate}, plus an \code{"interpretation"} attribute.
\item \code{direct_variance()}, \code{partner_variance()}, \code{direct_maternal_covariance()}:
single numerics (the raw variance component).
}
}
\description{
These extractors summarize an opt-in, \strong{experimental} direct-maternal
correlated model (\code{target = "direct_maternal"}), fitted with
\code{animal(1 | id, pedigree = ped) + maternal_genetic(1 | dam)}.
The model estimates a 2x2 genetic covariance matrix G_dm between the direct
additive and maternal additive effects, plus a residual variance.
}
\details{
Willham fence: the direct-maternal model (Willham 1963, 1972) distinguishes
the \strong{direct} narrow-sense heritability \code{h2_d = sigma_ad / sigma_P} from
the maternal variance ratio \code{m2 = sigma_am / sigma_P}, the Willham total
(selection-response) heritability
\code{h2_T = (sigma_ad + 1.5*sigma_dm + 0.5*sigma_am) / sigma_P}, and the
genetic covariance \code{sigma_dm}.
\code{sigma_P = sigma_ad + sigma_am + sigma_dm + sigma_e2 = Var(y_i)} for a
non-inbred base (coefficient 1 on \code{sigma_dm} because
\code{2 * A[i,dam] = 2 * (1/2) = 1}). \strong{h2 is denominator-dependent under
maternal effects; compare (co)variance components, not h2 values, across
software (ASReml/BLUPF90/WOMBAT/sommer/MCMCglmm all leave sigma_P to the
user).} A \strong{negative} genetic correlation \code{r_am} is real and biologically
expected in many livestock traits; it reflects an antagonistic
direct-maternal relationship and does NOT indicate a model failure.

Note: the 2x2 G_dm formulation is due to Willham (1963, 1972), not
Falconer (1965). Falconer's single-m model fixes \verb{r_am = +/-1} and is
a special case (Bijma 2011).

\code{direct_heritability()} returns the direct narrow-sense heritability with
an interpretation attribute. \code{total_heritability()} returns Willham's
selection-response heritability \code{h2_T}. \code{direct_variance()},
\code{partner_variance()}, and \code{direct_maternal_covariance()} return the raw
variance-component scalars. \code{genetic_correlation()} on a direct-maternal
fit returns the between-effect correlation r_am.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
fit_dm <- hsquared(
  y ~ 1 + animal(1 | id, pedigree = ped) + maternal_genetic(1 | dam),
  data = dat,
  family = gaussian(),
  REML = TRUE,
  control = hs_control(
    engine = "julia",
    engine_control = list(target = "direct_maternal")
  )
)

direct_heritability(fit_dm)    # h2_d (direct narrow-sense)
total_heritability(fit_dm)     # h2_T (Willham selection-response)
genetic_correlation(fit_dm)    # r_am (may be negative)
direct_variance(fit_dm)        # sigma_ad
partner_variance(fit_dm)       # sigma_am
direct_maternal_covariance(fit_dm)  # sigma_dm
variance_components(fit_dm)    # all four components
maternal_effects(fit_dm)       # maternal EBVs
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=variance_components]{variance_components()}}, \code{\link[=genetic_correlation]{genetic_correlation()}}, \code{\link[=maternal_effects]{maternal_effects()}},
\code{\link[=total_heritability]{total_heritability()}}
}
