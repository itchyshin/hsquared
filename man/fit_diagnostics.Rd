% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{fit_diagnostics}
\alias{fit_diagnostics}
\title{Inspect fitted-model diagnostics}
\usage{
fit_diagnostics(object, ...)
}
\arguments{
\item{object}{A fitted model object.}

\item{...}{Reserved for future arguments.}
}
\value{
A data frame with \code{metric} and \code{value} columns and class
\code{"hs_fit_diagnostics"}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{fit_diagnostics()} returns a compact diagnostics table for an
\code{hsquared_fit} object. It is an inspection helper over the current result
payload: it does not refit the model, rerun validation checks, or promote an
experimental bridge target to production support.

Two rows share the word "boundary" but report unrelated things
(hsquared#230):
\itemize{
\item \code{at_boundary} / \code{at_boundary_condition} derive from the fitted
\strong{estimates}: a variance component at or near zero, or an inadmissible
negative estimate (\code{hs_fit_boundary_class()}). Emitted for the Gaussian
animal-model / genomic / single-step routes that report one primary
variance-share layout; the \verb{_condition} row is dropped (not \code{NA}) when
the fit is interior, since there is nothing extra to say.
\item \code{search_boundary} / \code{search_boundary_condition} mirror the non-Gaussian
bridge's \strong{optimizer} state: \code{result$boundary} (documented as
\code{fit$result$boundary} in \code{?hs_control}) reports whether
\code{HSquared.jl}'s single-variance Brent search stopped on the rail of its
log-scale bracket (\verb{log(initial$sigma_a2) +/- 6}, HSquared.jl#327),
i.e. the estimate is a function of the search bracket, not the data.
Only fits whose result carries a \code{boundary} field (the non-Gaussian
route) gain this pair; a Gaussian \code{engine = "fit"} fit gains neither row
(not even \code{NA}). Unlike \code{at_boundary_condition}, \code{search_boundary_condition}
is always emitted alongside a present \code{search_boundary} flag, reading
\code{"interior"} rather than disappearing when the flag is \code{FALSE} -- the
search-bracket rail is a binary optimizer state with no benign-vs-
inadmissible split to only sometimes report. In practice a
\code{search_boundary = TRUE} fit is refused before it reaches an
\code{hsquared_fit} object at all, so a returned fit's \code{search_boundary} is
always \code{FALSE}; \code{TRUE} is reachable only on a hand-built \code{result} object.
}
}
